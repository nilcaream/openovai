[
  {
    "name": "a Worker may write a hard rule",
    "catches": "refuses a Worker's hard rule and writes no file",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (kind === HARD_RULE && caller.role !== LEADER) {",
        "to": "  if (kind === HARD_RULE && false) {"
      }
    ]
  },
  {
    "name": "a Worker may write source user",
    "catches": "refuses a Worker's source user",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (caller.role !== LEADER && (source !== \"team\" || scope !== \"team\")) {",
        "to": "  if (caller.role !== LEADER && (false || scope !== \"team\")) {"
      }
    ]
  },
  {
    "name": "a Worker may write scope leader",
    "catches": "refuses a Worker's scope leader",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (caller.role !== LEADER && (source !== \"team\" || scope !== \"team\")) {",
        "to": "  if (caller.role !== LEADER && (source !== \"team\" || false)) {"
      }
    ]
  },
  {
    "name": "scope leader is taken on any record",
    "catches": "refuses scope leader on a team rule",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (scope === \"leader\" && !(kind === HARD_RULE && source === \"user\")) {",
        "to": "  if (false) {"
      }
    ]
  },
  {
    "name": "knowledge takes a hard rule",
    "catches": "refuses a hard rule in knowledge",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "knowledge: [\"fact\", \"trap\"] };",
        "to": "knowledge: [\"hard-rule\", \"fact\", \"trap\"] };"
      }
    ]
  },
  {
    "name": "a hard rule at the length is refused",
    "catches": "refuses a hard rule one character over the length and takes one at it",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    if (text.length > limits.length) {",
        "to": "    if (text.length >= limits.length) {"
      }
    ]
  },
  {
    "name": "a hard rule one over the length passes",
    "catches": "refuses a hard rule one character over the length and takes one at it",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    if (text.length > limits.length) {",
        "to": "    if (text.length > limits.length + 1) {"
      }
    ]
  },
  {
    "name": "a hard rule may hold a newline",
    "catches": "refuses a hard rule holding a newline",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    if (/[\\r\\n]/.test(text)) {",
        "to": "    if (false) {"
      }
    ]
  },
  {
    "name": "the words are stored instead of the helper's moment",
    "catches": "stores the helper's absolute moment for until given in words, never the words",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  return { until: moment };\n}",
        "to": "  return { until: words };\n}"
      }
    ]
  },
  {
    "name": "words the helper cannot place are stored as no until",
    "catches": "refuses until words the helper cannot place, quoting them, and writes nothing",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (moment === null) {\n    return { refused: `until \"${words}\" could not be placed on the calendar; say when as a date or as plainer words` };\n  }",
        "to": "  if (moment === null) {\n    return { until: undefined };\n  }"
      }
    ]
  },
  {
    "name": "an absolute until in the past is taken",
    "catches": "refuses an absolute until that has passed",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    if (moment <= now.getTime()) {\n      return { refused: `until \"${words}\" has passed; give a moment ahead, or now to retire` };\n    }",
        "to": "    if (false) {\n      return { refused: `until \"${words}\" has passed; give a moment ahead, or now to retire` };\n    }"
      }
    ]
  },
  {
    "name": "the literal now goes to the helper",
    "catches": "takes until now from its own clock, asks the helper nothing, and so retires without a request on the account",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (words === NOW) {",
        "to": "  if (false) {"
      }
    ]
  },
  {
    "name": "the literal now is no until, so the rule stays in the render",
    "catches": "retires a rule with until now and brings it back with a renew",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (words === NOW) {\n    return { until: stamp(now, zone) };\n  }",
        "to": "  if (words === NOW) {\n    return { until: undefined };\n  }"
      }
    ]
  },
  {
    "name": "replaces is refused on an expired record, so a renew fails",
    "catches": "retires a rule with until now and brings it back with a renew",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    if (!current.includes(named)) {",
        "to": "    if (!current.includes(named) || expired(named, now)) {"
      }
    ]
  },
  {
    "name": "replaces skips the kind comparison",
    "catches": "refuses replaces naming a missing id or one of another store or kind",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    if (named === undefined || named.kind !== kind) {",
        "to": "    if (named === undefined) {"
      }
    ]
  },
  {
    "name": "the cap refusal comes without the set",
    "catches": "refuses the rule over the cap with the count and the numbered set, and takes a replacement of a current one at the cap",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "retire or merge one with the User first. The set:\\n${set}`,",
        "to": "retire or merge one with the User first.`,"
      }
    ]
  },
  {
    "name": "the cap is hardcoded",
    "catches": "reads the cap and the length from the workspace configuration, with the defaults when absent",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    count: positive(given.count) ?? HARD_RULES.count,",
        "to": "    count: HARD_RULES.count,"
      }
    ]
  },
  {
    "name": "the length is hardcoded",
    "catches": "reads the cap and the length from the workspace configuration, with the defaults when absent",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    length: positive(given.length) ?? HARD_RULES.length,",
        "to": "    length: HARD_RULES.length,"
      }
    ]
  },
  {
    "name": "only team-scoped rules count toward the cap",
    "catches": "counts Leader-scoped rules toward the cap",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    const held = sameKind.filter((record) => !expired(record, now));",
        "to": "    const held = sameKind.filter((record) => !expired(record, now) && record.scope !== \"leader\");"
      }
    ]
  },
  {
    "name": "a team write may replace the User's record when the Leader writes it",
    "catches": "refuses a team write over the User's record, the Leader included",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (replaces !== null && replaces.source === \"user\" && source !== \"user\") {",
        "to": "  if (replaces !== null && replaces.source === \"user\" && caller.role !== LEADER) {"
      }
    ]
  },
  {
    "name": "the cap refuses at the count rather than over it",
    "catches": "refuses the rule over the cap with the count and the numbered set, and takes a replacement of a current one at the cap",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    if (after > limits.count) {",
        "to": "    if (after >= limits.count) {"
      }
    ]
  },
  {
    "name": "a replacement counts as one more",
    "catches": "refuses the rule over the cap with the count and the numbered set, and takes a replacement of a current one at the cap",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    const after = held.length + 1 - (replaces !== null && held.includes(replaces) ? 1 : 0);",
        "to": "    const after = held.length + 1;"
      }
    ]
  },
  {
    "name": "expired rules count toward the cap",
    "catches": "leaves expired rules out of the count",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    const held = sameKind.filter((record) => !expired(record, now));",
        "to": "    const held = sameKind;"
      }
    ]
  },
  {
    "name": "a replacement gets a fresh number",
    "catches": "numbers a new rule past every number ever given and hands a replacement the number it replaces",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    record.number = replaces !== null ? replaces.number : nextNumber(sameKind);",
        "to": "    record.number = nextNumber(sameKind);"
      }
    ]
  },
  {
    "name": "the replaced text is not copied in",
    "catches": "writes provenance: by, at, supersedes, the replaced text byte for byte, the reason",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    record.replaced = replaces.text;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the reason is always given",
    "catches": "writes provenance: by, at, supersedes, the replaced text byte for byte, the reason",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    record.reason = reason;\n",
        "to": "    record.reason = TAG;\n"
      }
    ]
  },
  {
    "name": "a replaced record is still current",
    "catches": "lists a replaced record under neither all nor the render, and marks it by id",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    current: visible.filter((record) => !replaced.has(record.id)),",
        "to": "    current: visible,"
      }
    ]
  },
  {
    "name": "the refusal names the replaced id and not its successor",
    "catches": "names the current successor when replaces points at a replaced record",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "      return { refused: `${named.id} was replaced${to === undefined ? \"\" : ` by ${to}`}; replace the current record instead` };",
        "to": "      return { refused: `${named.id} was replaced; replace the current record instead` };"
      }
    ]
  },
  {
    "name": "writes are not serialized per store",
    "catches": "gives two rules remembered at once distinct numbers and holds the cap",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  return serialized(root, shape.store, () => write(root, shape, context));",
        "to": "  return write(root, shape, context);"
      }
    ]
  },
  {
    "name": "the file is opened for writing over whatever is there",
    "catches": "opens the file with wx: a file already at the next name fails the write and nothing is overwritten",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "serialise(record), { flag: \"wx\" });",
        "to": "serialise(record), { flag: \"w\" });"
      }
    ]
  },
  {
    "name": "the team's rules are printed before the User's",
    "catches": "renders the User's rules first, then the team's, by number inside each",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  for (const [source, heading] of [[\"user\", \"From the User:\"], [\"team\", \"From the team:\"]]) {",
        "to": "  for (const [source, heading] of [[\"team\", \"From the team:\"], [\"user\", \"From the User:\"]]) {"
      }
    ]
  },
  {
    "name": "the render keeps expired rules",
    "catches": "leaves expired rules out of the render and shows them marked on recall with expired",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    .filter(isCurrentHardRule)\n    .filter((record) => !expired(record, now))\n    .filter((record) => role === LEADER || record.scope !== \"leader\")",
        "to": "    .filter(isCurrentHardRule)\n    .filter((record) => role === LEADER || record.scope !== \"leader\")"
      }
    ]
  },
  {
    "name": "the read ignores the role",
    "catches": "keeps a Leader-scoped rule from a Worker on every path",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const visible = role === LEADER ? every : every.filter((record) => record.scope !== \"leader\");",
        "to": "  const visible = every;"
      }
    ]
  },
  {
    "name": "moments are said in UTC",
    "catches": "says until in the workspace zone with its label",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const parts = new Intl.DateTimeFormat(\"en-GB\", {\n    timeZone: zone,",
        "to": "  const parts = new Intl.DateTimeFormat(\"en-GB\", {\n    timeZone: \"UTC\","
      }
    ]
  },
  {
    "name": "the recall line says a date-only until",
    "catches": "says until the same way on the recall line and in the render",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    head.push(`until ${said(record.until, zone)}`);",
        "to": "    head.push(`until ${record.until.slice(0, 10)}`);"
      }
    ]
  },
  {
    "name": "the render collapses whitespace in a rule",
    "catches": "renders a rule's text byte for byte",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "      lines.push(`  ${record.number}. ${record.text}${untilSuffix(record, zone)}`);",
        "to": "      lines.push(`  ${record.number}. ${record.text.trim().replace(/\\s+/g, \" \")}${untilSuffix(record, zone)}`);"
      }
    ]
  },
  {
    "name": "both headings are always printed",
    "catches": "says none yet for an empty set and omits a heading with no rules",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    if (group.length === 0) {\n      continue;\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the version is computed over every kind",
    "catches": "versions the set by the newest current hard rule: a fact leaves it, a retire moves it",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const rules = current.filter(isCurrentHardRule);\n  return rules.length === 0 ? null : rules.reduce",
        "to": "  const rules = current;\n  return rules.length === 0 ? null : rules.reduce"
      }
    ]
  },
  {
    "name": "the update message is built for a Worker whatever the scope",
    "catches": "says nothing to a Worker about a rule scoped to the Leader",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (role !== LEADER && record.scope === \"leader\") {\n    return null;\n  }",
        "to": "  if (false) {\n    return null;\n  }"
      }
    ]
  },
  {
    "name": "the update message carries the set",
    "catches": "says the delta on an update: the rule, its old text, the version, never the set",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    return `${opening} rule ${record.number} now reads ${text} (replaced \"${record.replaced}\").`;",
        "to": "    return `${opening} rule ${record.number} now reads ${text} (replaced \"${record.replaced}\"). rule 8 and the rest of the set follow`;"
      }
    ]
  },
  {
    "name": "a retire is said as now reads",
    "catches": "says new for a new rule and retired for a retire",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    return `${opening} rule ${record.number} retired: \"${record.text}\".`;",
        "to": "    return `${opening} rule ${record.number} now reads ${text} (replaced \"${record.replaced}\").`;"
      }
    ]
  },
  {
    "name": "an identical text is put to the helper",
    "catches": "replaces an identical text without asking the helper",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    const same = sameKind.find((record) => collapse(record.text) === collapse(text));",
        "to": "    const same = undefined;"
      }
    ]
  },
  {
    "name": "every write is announced",
    "catches": "announces once per hard-rule write and never for a fact",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (kind === HARD_RULE) {\n    for (const listener of listeners) {",
        "to": "  if (true) {\n    for (const listener of listeners) {"
      }
    ]
  },
  {
    "name": "the answer to a hard-rule write drops the update line",
    "catches": "answers a hard-rule write with the record, the update line with the version, and what happens next",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    lines.push(updateMessage(record, { role: LEADER, zone }));\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a Worker's persona ends with the Leader's render",
    "catches": "ends the persona with the render for that role and returns the version",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const rules = hardRulesFor(root, role, options);",
        "to": "  const rules = hardRulesFor(root, LEADER, options);"
      }
    ]
  },
  {
    "name": "nothing is taken off a persona",
    "catches": "takes the render off a persona and leaves the rest",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const at = text.lastIndexOf(RENDER_BEGINS);\n  return at === -1 ? text : `${text.slice(0, at)}\\n`;",
        "to": "  return text;"
      }
    ]
  },
  {
    "name": "id and all are accepted together",
    "catches": "refuses a recall with two of query, id and all, or none",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (given.length === 1) {\n    by = given[0];",
        "to": "  if (given.length >= 1) {\n    by = given[0];"
      }
    ]
  },
  {
    "name": "kind hard-rule is accepted for knowledge on recall",
    "catches": "refuses kind hard-rule in knowledge on recall",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (kind === HARD_RULE && store !== \"memory\") {",
        "to": "  if (false) {"
      }
    ]
  },
  {
    "name": "recall is offered to the Leader only",
    "catches": "lists recall and remember for a Worker and for the Leader",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  recall: [LEADER, WORKER],",
        "to": "  recall: [LEADER],"
      }
    ]
  },
  {
    "name": "remember is not among BUILT_IN",
    "catches": "serves the Leader exactly what BUILT_IN names, recall and remember among them",
    "edits": [
      {
        "file": "tools/plugins.mjs",
        "from": "export const BUILT_IN = [\"message\", \"room\", \"recall\", \"remember\", \"write_desk\", \"restart_session\", \"stop_session\", \"park\", \"hire\", \"permission\"];",
        "to": "export const BUILT_IN = [\"message\", \"room\", \"recall\", \"write_desk\", \"restart_session\", \"stop_session\", \"park\", \"hire\", \"permission\"];"
      }
    ]
  },
  {
    "name": "a query sends replaced records to the helper too",
    "catches": "sends the helper exactly the current records of the store on a query and answers only ids it named",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const alive = current\n    .filter((record) => kind === undefined || record.kind === kind)",
        "to": "  const alive = records\n    .filter((record) => kind === undefined || record.kind === kind)"
      }
    ]
  },
  {
    "name": "an id the helper named and the store never sent is answered",
    "catches": "sends the helper exactly the current records of the store on a query and answers only ids it named",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const found = asked.answer.ids.filter((id) => known.has(id)).map((id) => known.get(id));",
        "to": "  const found = asked.answer.ids.map((id) => known.get(id) ?? { ...alive[0], id });"
      }
    ]
  },
  {
    "name": "the helper's list for a query is read from the files",
    "catches": "hands the helper nothing scoped to the Leader on a Worker's query",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const sent = alive.map((record) => ({ id: record.id, kind: record.kind, text: record.text }));",
        "to": "  const sent = recordsIn(root, store).map((record) => ({ id: record.id, kind: record.kind, text: record.text }));"
      }
    ]
  },
  {
    "name": "the helper's list for a replace is read from the files",
    "catches": "asks the helper about the same store and kind only, and takes what it names",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "        records: sameKind.map((record) => ({ id: record.id, text: record.text })),",
        "to": "        records: recordsIn(root, store).filter((record) => record.kind === kind).map((record) => ({ id: record.id, text: record.text })),"
      }
    ]
  },
  {
    "name": "all kinds are sent when asking whether a write replaces something",
    "catches": "asks the helper about the same store and kind only on a remember without replaces, and nothing when there is no record of that kind",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "        records: sameKind.map((record) => ({ id: record.id, text: record.text })),",
        "to": "        records: current.map((record) => ({ id: record.id, text: record.text })),"
      }
    ]
  },
  {
    "name": "the helper is asked even when the store holds nothing of that kind",
    "catches": "asks the helper about the same store and kind only on a remember without replaces, and nothing when there is no record of that kind",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "    } else if (sameKind.length > 0) {",
        "to": "    } else {"
      }
    ]
  },
  {
    "name": "the guard on the User's records is skipped when the helper found the record",
    "catches": "refuses a team write when the helper names the User's record, over the door",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (replaces !== null && replaces.source === \"user\" && source !== \"user\") {",
        "to": "  if (shape.replaces !== undefined && replaces !== null && replaces.source === \"user\" && source !== \"user\") {"
      }
    ]
  },
  {
    "name": "an id the helper named and was never sent is accepted",
    "catches": "refuses a helper answer that is not JSON or names an unsent id, and writes nothing",
    "edits": [
      {
        "file": "tools/helper.mjs",
        "from": "      if (answer.replaces !== null && !sent.has(answer.replaces)) {\n        return null;\n      }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the model's words go into the answer",
    "catches": "refuses the call in its own words when the helper is turned away or falls over, writes nothing, and keeps the model's words out of the answer",
    "edits": [
      {
        "file": "tools/helper.mjs",
        "from": "      return { refused: \"the helper answered badly\" };",
        "to": "      return { refused: `the helper answered badly: ${envelope.result}` };"
      }
    ]
  },
  {
    "name": "all goes through the helper",
    "catches": "asks the helper nothing for id, all or the numbered set",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  if (by === \"all\") {\n    return {",
        "to": "  if (by === \"all\") {\n    await helper(\"select\", { question: \"select\", query: \"\", records: [] });\n    return {"
      }
    ]
  },
  {
    "name": "the answer to a write drops the version",
    "catches": "answers a Leader's hard-rule write with the version and the update line",
    "edits": [
      {
        "file": "tools/store.mjs",
        "from": "  const opening = `Hard rules update (set ${record.id}):`;",
        "to": "  const opening = \"Hard rules update:\";"
      }
    ]
  }
]

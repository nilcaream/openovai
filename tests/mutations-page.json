[
  {
    "name": "two dark tokens swap values",
    "catches": "declares the light tokens in bare :root and the dark ones under the theme attribute, each with its colour-scheme",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    --bg: #0e1116; --panel: #161b22;",
        "to": "    --bg: #161b22; --panel: #0e1116;"
      }
    ]
  },
  {
    "name": "the head bubble's ground is a hex instead of a token",
    "catches": "carry no raw colour outside the token blocks",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "border: 2px solid var(--line); border-radius: var(--radius); background: var(--panel-2);",
        "to": "border: 2px solid var(--line); border-radius: var(--radius); background: #eef1f5;"
      }
    ]
  },
  {
    "name": "the bubble rule is renamed while the script still emits bubble",
    "catches": "name no class the page never emits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .bubble { width: 100%;",
        "to": "  .balloon { width: 100%;"
      }
    ]
  },
  {
    "name": "the row kinds are styled under a class the script never emits",
    "catches": "name no class the page never emits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.chat .bubble, .msg.silent .bubble { color: var(--fg-dim); }",
        "to": "  .row.chat .bubble, .row.silent .bubble { color: var(--fg-dim); }"
      }
    ]
  },
  {
    "name": "a row kind is renamed in the renderer while the stylesheet keeps the old one",
    "catches": "give every class the page emits at least one rule",
    "edits": [
      {
        "file": "lib/chat/render.mjs",
        "from": "kind: \"user\"",
        "to": "kind: \"person\""
      }
    ]
  },
  {
    "name": "the body font goes back to fourteen pixels on a shorter line",
    "catches": "set the page's type in rem on the sans stack, one and a half lines high",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "font: 1rem/1.5 var(--sans);",
        "to": "font: 14px/1.4 var(--sans);"
      }
    ]
  },
  {
    "name": "the empty state is emitted while both of its rules are gone",
    "catches": "give every class the page emits at least one rule",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .rows .empty { color: var(--fg-faint); font-size: .8rem; padding: 4px 0; }\n  #mid .rows .empty { font-size: inherit; text-align: center; padding: 20px 0; }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the dot of a panel whose process is gone takes the busy colour",
    "catches": "mark a panel whose process is gone, or whose page has no stream, with a red dot on its head",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .panel.gone .phead .name::before { background: var(--bad); }",
        "to": "  .panel.gone .phead .name::before { background: var(--warn); }"
      }
    ]
  },
  {
    "name": "the User's bubble takes the reply ground",
    "catches": "draw what the User typed on the User's own ground",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": ".msg.user .bubble { background: var(--me);",
        "to": ".msg.user .bubble { background: var(--reply);"
      }
    ]
  },
  {
    "name": "the head bubble's ground names a token the table does not declare",
    "catches": "name no token the table does not declare",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "border: 2px solid var(--line); border-radius: var(--radius); background: var(--panel-2);",
        "to": "border: 2px solid var(--line); border-radius: var(--radius); background: var(--panel-3);"
      }
    ]
  },
  {
    "name": "the row's stamp drops the weekday",
    "catches": "stamps a row with the reader's own day and time, the weekday written out, on a 24-hour clock, and the day pill with the same",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "day: \"2-digit\", weekday: \"long\", hour:",
        "to": "day: \"2-digit\", hour:"
      }
    ]
  },
  {
    "name": "a reply's markdown is pasted as text instead of parsed into elements",
    "catches": "draws a reply's markdown as elements — a link, a code span — never as the words of the markup",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      body.innerHTML = shown.html;",
        "to": "      body.textContent = shown.html;"
      }
    ]
  },
  {
    "name": "a link in a reply takes a raw hex instead of the accent token",
    "catches": "carry no raw colour outside the token blocks",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": ".md a, .bubble a { color: var(--accent); }",
        "to": ".md a, .bubble a { color: #5eaeff; }"
      }
    ]
  },
  {
    "name": "inline code takes a ground other than the one grey the rules allow",
    "catches": "carry no raw colour outside the token blocks",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "background: rgba(127,127,127,.18); padding: .1em .35em;",
        "to": "background: rgba(0,0,0,.18); padding: .1em .35em;"
      }
    ]
  },
  {
    "name": "the stamp is formatted in a zone of the page's own",
    "catches": "stamps a row with the reader's own day and time, the weekday written out, on a 24-hour clock, and the day pill with the same",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "new Intl.DateTimeFormat(\"en-GB\", { year: \"numeric\",",
        "to": "new Intl.DateTimeFormat(\"en-GB\", { timeZone: \"Europe/Warsaw\", year: \"numeric\","
      }
    ]
  },
  {
    "name": "the deny button of a question takes the User's own ground",
    "catches": "draw the deny button of a question on the ground of what goes to the session",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": ".msg.perm .acts button[data-decision=deny] { background: var(--to); border-color: var(--to-line); }",
        "to": ".msg.perm .acts button[data-decision=deny] { background: var(--me); border-color: var(--to-line); }"
      }
    ]
  },
  {
    "name": "the question card is emitted as perm while every one of its rules is renamed",
    "catches": "give every class the page emits at least one rule",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.perm .bubble { background: var(--panel-2); border-color: var(--warn); border-width: 2px; }\n  .msg.perm .desc { font-size: .92rem; margin: 2px 0 4px; overflow-wrap: anywhere; }\n  .msg.perm .reason { font-size: .78rem; color: var(--fg-dim); font-family: var(--mono); margin: 0 0 4px; }\n  .msg.perm .raw { font-family: var(--mono); font-size: .76rem; background: var(--bg); border: 1px solid var(--line); border-radius: 6px; padding: 6px 8px; margin: 4px 0; white-space: pre-wrap; overflow-wrap: anywhere; max-height: 18em; overflow: auto; }\n  .msg.perm .acts { display: flex; flex-wrap: wrap; gap: 6px; align-items: center; margin: 6px 0 2px; }\n  .msg.perm .acts button { font: 600 .82rem var(--sans); padding: 4px 12px; border-radius: 8px; border: 1px solid var(--line); background: var(--panel); color: var(--fg); cursor: pointer; }\n  .msg.perm .acts button[data-decision=allow] { background: var(--me); border-color: var(--me-line); }\n  .msg.perm .acts button[data-decision=deny] { background: var(--to); border-color: var(--to-line); }\n  .msg.perm .acts input { flex: 1 1 200px; min-width: 120px; font: .82rem var(--sans); padding: 4px 8px; border-radius: 8px; border: 1px solid var(--line); background: var(--bg); color: var(--fg); }\n",
        "to": "  .msg.card .bubble { background: var(--panel-2); border-color: var(--warn); border-width: 2px; }\n  .msg.card .desc { font-size: .92rem; margin: 2px 0 4px; overflow-wrap: anywhere; }\n  .msg.card .reason { font-size: .78rem; color: var(--fg-dim); font-family: var(--mono); margin: 0 0 4px; }\n  .msg.card .raw { font-family: var(--mono); font-size: .76rem; background: var(--bg); border: 1px solid var(--line); border-radius: 6px; padding: 6px 8px; margin: 4px 0; white-space: pre-wrap; overflow-wrap: anywhere; max-height: 18em; overflow: auto; }\n  .msg.card .acts { display: flex; flex-wrap: wrap; gap: 6px; align-items: center; margin: 6px 0 2px; }\n  .msg.card .acts button { font: 600 .82rem var(--sans); padding: 4px 12px; border-radius: 8px; border: 1px solid var(--line); background: var(--panel); color: var(--fg); cursor: pointer; }\n  .msg.card .acts button[data-decision=allow] { background: var(--me); border-color: var(--me-line); }\n  .msg.card .acts button[data-decision=deny] { background: var(--to); border-color: var(--to-line); }\n  .msg.card .acts input { flex: 1 1 200px; min-width: 120px; font: .82rem var(--sans); padding: 4px 8px; border-radius: 8px; border: 1px solid var(--line); background: var(--bg); color: var(--fg); }\n"
      }
    ]
  },
  {
    "name": "the reason line of a question is styled under a class the script never emits",
    "catches": "name no class the page never emits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.perm .reason { font-size: .78rem;",
        "to": "  .msg.perm .why { font-size: .78rem;"
      }
    ]
  },
  {
    "name": "a decision button carries no data-decision attribute",
    "catches": "select no attribute the script never sets",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      button.dataset.decision = decision;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the composer box can be dragged taller",
    "catches": "let the composer grow with what is typed and never by a drag",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  textarea { display: block; width: 100%; resize: none;",
        "to": "  textarea { display: block; width: 100%; resize: vertical;"
      }
    ]
  },
  {
    "name": "the empty box says nothing about whom it reaches or which key sends",
    "catches": "says in the empty box whom a message reaches and which key sends it, the whole sentence only where it fits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      const whole = busy ? `${name} is working — Enter still sends` : `Message ${name} — Enter sends, Shift+Enter for a new line`;\n",
        "to": "      const whole = busy ? `${name} is working — Enter still sends` : `Message ${name}`;\n"
      }
    ]
  },
  {
    "name": "Enter no longer sends: the keydown handler is gone",
    "catches": "sends on Enter through the form, and grows the box with what is typed, capped at 40vh",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    box.addEventListener(\"keydown\", (event) => {\n      if (keyAction(event) === \"send\") {\n        event.preventDefault();\n        composer.requestSubmit();\n      }\n    });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the box no longer grows with what is typed: the input listener is gone",
    "catches": "sends on Enter through the form, and grows the box with what is typed, capped at 40vh",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    box.addEventListener(\"input\", autosize);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the box grows past 40vh: the cap is gone",
    "catches": "sends on Enter through the form, and grows the box with what is typed, capped at 40vh",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      box.style.height = `${Math.min(box.scrollHeight, max)}px`;",
        "to": "      box.style.height = `${box.scrollHeight}px`;"
      }
    ]
  },
  {
    "name": "the box never shrinks back: the height reset before the measure is gone",
    "catches": "sends on Enter through the form, and grows the box with what is typed, capped at 40vh",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      box.style.height = \"auto\";\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the head script that sets the theme before the first paint is gone",
    "catches": "sets the stored theme above the stylesheet, flips it on the toggle, keeps it and repaints the title bar",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "<script>\n  // The theme before the first paint, so a page kept dark never flashes light: the stored choice,\n  // light when there is none or when storage throws. It sits above the stylesheet on purpose.\n  try { document.documentElement.dataset.theme = localStorage.getItem(\"openovai-theme\") === \"dark\" ? \"dark\" : \"light\"; }\n  catch (error) { document.documentElement.dataset.theme = \"light\"; }\n</script>\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the head script sits below the stylesheet, so a page kept dark paints light first",
    "catches": "sets the stored theme above the stylesheet, flips it on the toggle, keeps it and repaints the title bar",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "<script>\n  // The theme before the first paint, so a page kept dark never flashes light: the stored choice,\n  // light when there is none or when storage throws. It sits above the stylesheet on purpose.\n  try { document.documentElement.dataset.theme = localStorage.getItem(\"openovai-theme\") === \"dark\" ? \"dark\" : \"light\"; }\n  catch (error) { document.documentElement.dataset.theme = \"light\"; }\n</script>\n",
        "to": ""
      },
      {
        "file": "lib/chat/page.html",
        "from": "</style>\n",
        "to": "</style>\n<script>\n  // The theme before the first paint, so a page kept dark never flashes light: the stored choice,\n  // light when there is none or when storage throws. It sits above the stylesheet on purpose.\n  try { document.documentElement.dataset.theme = localStorage.getItem(\"openovai-theme\") === \"dark\" ? \"dark\" : \"light\"; }\n  catch (error) { document.documentElement.dataset.theme = \"light\"; }\n</script>\n"
      }
    ]
  },
  {
    "name": "the dark tokens sit under the system's colour-scheme query instead of the page's own attribute",
    "catches": "declares the light tokens in bare :root and the dark ones under the theme attribute, each with its colour-scheme",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  :root[data-theme=\"dark\"] {\n",
        "to": "  @media (prefers-color-scheme: dark) { :root {\n"
      },
      {
        "file": "lib/chat/page.html",
        "from": "    color-scheme: dark;\n  }\n",
        "to": "    color-scheme: dark;\n  } }\n"
      }
    ]
  },
  {
    "name": "the toggle no longer flips the theme: its click listener is gone",
    "catches": "sets the stored theme above the stylesheet, flips it on the toggle, keeps it and repaints the title bar",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      themeToggle.addEventListener(\"click\", () => applyTheme(document.documentElement.dataset.theme === \"dark\" ? \"light\" : \"dark\"));\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the theme is applied but never kept: the store is gone",
    "catches": "sets the stored theme above the stylesheet, flips it on the toggle, keeps it and repaints the title bar",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    try { localStorage.setItem(\"openovai-theme\", theme); } catch (error) {}\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the installed app's title bar no longer follows the theme: the meta repaint is gone",
    "catches": "sets the stored theme above the stylesheet, flips it on the toggle, keeps it and repaints the title bar",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    themeMeta.content = getComputedStyle(document.documentElement).getPropertyValue(\"--panel-2\").trim() || themeMeta.content;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the pill never shows: the draw that appends rows no longer marks it",
    "catches": "shows the pill when rows land below a reader who is not near the newest, and takes them there on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "        } else if (panel.jump !== null && !near) {\n          panel.jump.classList.add(\"show\");\n        }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the pill stays once shown: the scroll listener that hides it near the newest is gone",
    "catches": "shows the pill when rows land below a reader who is not near the newest, and takes them there on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      rows.addEventListener(\"scroll\", () => {\n        if (nearTheNewest(rows)) jump.classList.remove(\"show\");\n      });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a click on the pill hides it without going to the newest row",
    "catches": "shows the pill when rows land below a reader who is not near the newest, and takes them there on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "        rows.scrollTop = rows.scrollHeight;\n        jump.classList.remove(\"show\");\n",
        "to": "        jump.classList.remove(\"show\");\n"
      }
    ]
  },
  {
    "name": "near the newest means anywhere: the 80px band is gone",
    "catches": "shows the pill when rows land below a reader who is not near the newest, and takes them there on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  const nearTheNewest = (rows) => rows.scrollHeight - rows.scrollTop - rows.clientHeight < 80;\n",
        "to": "  const nearTheNewest = (rows) => true;\n"
      }
    ]
  },
  {
    "name": "the placeholder is measured once and never again when the box changes width",
    "catches": "says in the empty box whom a message reaches and which key sends it, the whole sentence only where it fits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    new ResizeObserver(fitPlaceholder).observe(box);\n",
        "to": "    fitPlaceholder();\n"
      }
    ]
  },
  {
    "name": "the whole sentence is the placeholder however narrow the box",
    "catches": "says in the empty box whom a message reaches and which key sends it, the whole sentence only where it fits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      box.placeholder = textWidth(whole, box) <= room ? whole : busy ? `${name} is working` : `Message ${name}`;\n",
        "to": "      box.placeholder = whole;\n"
      }
    ]
  },
  {
    "name": "the connection word is marked while the page HAS a stream",
    "catches": "draws the connection word and the quota line on the Leader's head, and marks a lost stream",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      panel.facts.conn.classList.toggle(\"off\", state.connection !== CONNECTED);\n",
        "to": "      panel.facts.conn.classList.toggle(\"off\", state.connection === CONNECTED);\n"
      }
    ]
  },
  {
    "name": "the connection word never reaches the head",
    "catches": "draws the connection word and the quota line on the Leader's head, and marks a lost stream",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      panel.facts.conn.textContent = state.connection;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the quota line is drawn without its tooltip",
    "catches": "draws the connection word and the quota line on the Leader's head, and marks a lost stream",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      panel.facts.quota.title = quotaTitle(state.quota, (iso) => stamp(iso).whole);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the tooltip says the stamp object, not its words",
    "catches": "draws the connection word and the quota line on the Leader's head, and marks a lost stream",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "(iso) => stamp(iso).whole);\n",
        "to": "(iso) => stamp(iso));\n"
      }
    ]
  },
  {
    "name": "the model and the context are cut to a fragment on a narrow head",
    "catches": "pin every word of a head, clip the head at its edge, and push the theme toggle to it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .phead .info { font-family: var(--mono); font-size: .78rem; color: var(--fg-dim); white-space: nowrap; flex: 0 0 auto; }",
        "to": "  .phead .info { font-family: var(--mono); font-size: .78rem; color: var(--fg-dim); white-space: nowrap; flex: 0 1 auto; min-width: 0; overflow: hidden; text-overflow: ellipsis; }"
      }
    ]
  },
  {
    "name": "the theme toggle sits where the words end rather than at the edge",
    "catches": "pin every word of a head, clip the head at its edge, and push the theme toggle to it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .phead .theme { margin-left: auto; align-self: center;",
        "to": "  .phead .theme { margin-left: 10px; align-self: center;"
      }
    ]
  },
  {
    "name": "the quota gives way on a narrow head",
    "catches": "pin every word of a head, clip the head at its edge, and push the theme toggle to it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .phead .conn, .phead .quota { font-family: var(--mono); font-size: .78rem; color: var(--fg-dim); white-space: nowrap; flex: 0 0 auto; }",
        "to": "  .phead .conn, .phead .quota { font-family: var(--mono); font-size: .78rem; color: var(--fg-dim); white-space: nowrap; flex: 0 1 auto; }"
      }
    ]
  },
  {
    "name": "the gauge font has no size: measureText measures the canvas default, not the box",
    "catches": "says in the empty box whom a message reaches and which key sends it, the whole sentence only where it fits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "gauge.font = `${style.fontStyle} ${style.fontWeight} ${style.fontSize} ${style.fontFamily}`;",
        "to": "gauge.font = `${style.fontStyle} ${style.fontWeight} ${style.fontFamily}`;"
      }
    ]
  },
  {
    "name": "the room ignores the box padding: the fit is judged 20px too wide",
    "catches": "says in the empty box whom a message reaches and which key sends it, the whole sentence only where it fits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "const room = box.clientWidth - parseFloat(style.paddingLeft) - parseFloat(style.paddingRight);",
        "to": "const room = box.clientWidth;"
      }
    ]
  },
  {
    "name": "the pill is fixed to the viewport again: it sits over an open card",
    "catches": "keep the pill inside the rows, stuck 24px above their bottom edge",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "#jump { position: sticky; left: 50%;",
        "to": "#jump { position: fixed; left: 50%;"
      }
    ]
  },
  {
    "name": "the pill has no bottom offset: it sticks to the very edge of the rows",
    "catches": "keep the pill inside the rows, stuck 24px above their bottom edge",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "transform: translateX(-50%); bottom: 24px; background: var(--panel);",
        "to": "transform: translateX(-50%); background: var(--panel);"
      }
    ]
  },
  {
    "name": "the pill is appended to the body, outside the rows: sticky has nothing to stick to",
    "catches": "shows the pill when rows land below a reader who is not near the newest, and takes them there on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      rows.append(jump);\n",
        "to": "      document.body.append(jump);\n"
      }
    ]
  },
  {
    "name": "the pill is never put back last: appended rows carry it up into the panel",
    "catches": "shows the pill when rows land below a reader who is not near the newest, and takes them there on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      // Rows and cards are appended after the pill; it goes back to the end so it sticks to the\n      // bottom edge.\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the put-back-last line kept verbatim but moved ABOVE the append loop: rows appended after it carry the pill up again",
    "catches": "shows the pill when rows land below a reader who is not near the newest, and takes them there on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      panel.empty.hidden = about.rows.length > 0;\n      const added = [];\n      if (about.rows.length > panel.shown) {\n",
        "to": "      panel.empty.hidden = about.rows.length > 0;\n      const added = [];\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n      if (about.rows.length > panel.shown) {\n"
      },
      {
        "file": "lib/chat/page.html",
        "from": "      }\n      // Rows and cards are appended after the pill; it goes back to the end so it sticks to the\n      // bottom edge.\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n",
        "to": "      }\n"
      }
    ]
  },
  {
    "name": "the shown pill is made static by the .show rule: the #jump rule still says sticky, the pill sits in the flow at the end of the rows",
    "catches": "keep the pill inside the rows, stuck 24px above their bottom edge",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "#jump.show { display: block; }",
        "to": "#jump.show { display: block; position: static; }"
      }
    ]
  },
  {
    "name": "the busy dot takes the idle colour",
    "catches": "mark a panel whose turn is running with an amber dot, and let a gone process outrank it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .panel.busy .phead .name::before { background: var(--warn); }",
        "to": "  .panel.busy .phead .name::before { background: var(--ok); }"
      }
    ]
  },
  {
    "name": "the idle dot takes the busy colour",
    "catches": "mark a panel whose turn is running with an amber dot, and let a gone process outrank it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .panel.idle .phead .name::before { background: var(--ok); }",
        "to": "  .panel.idle .phead .name::before { background: var(--warn); }"
      }
    ]
  },
  {
    "name": "the gone rule moves above the state rules: a process gone mid-turn keeps the amber dot",
    "catches": "mark a panel whose turn is running with an amber dot, and let a gone process outrank it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .panel.idle .phead .name::before { background: var(--ok); }\n  .panel.busy .phead .name::before { background: var(--warn); }\n  .panel.gone .phead .name::before { background: var(--bad); }",
        "to": "  .panel.gone .phead .name::before { background: var(--bad); }\n  .panel.idle .phead .name::before { background: var(--ok); }\n  .panel.busy .phead .name::before { background: var(--warn); }"
      }
    ]
  },
  {
    "name": "a second .panel.busy dot rule later in the sheet sets the dot green behind the pinned amber",
    "catches": "declare no selector twice",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .panel.asking .phead .name::before { animation: pulse 1.2s ease-in-out infinite; }",
        "to": "  .panel.asking .phead .name::before { animation: pulse 1.2s ease-in-out infinite; }\n  .panel.busy .phead .name::before { background: var(--ok); }"
      }
    ]
  },
  {
    "name": "the page never reads the dot: the idle, busy and gone marks are gone from the draw",
    "catches": "marks a panel idle, busy or gone from its dot on every draw, and refits the empty box then",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    const colour = dot(state, panel.name);\n    panel.section.classList.toggle(\"idle\", colour === GREEN);\n    panel.section.classList.toggle(\"busy\", colour === AMBER);\n    panel.section.classList.toggle(\"gone\", colour === RED);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the empty box is refit only on a change of width: the draw no longer refits it",
    "catches": "marks a panel idle, busy or gone from its dot on every draw, and refits the empty box then",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    panel.box.disabled = !composersEnabled(state, panel.name);\n    panel.fitPlaceholder();",
        "to": "    panel.box.disabled = !composersEnabled(state, panel.name);"
      }
    ]
  },
  {
    "name": "a busy box says the idle sentence",
    "catches": "says in the empty box whom a message reaches and which key sends it, the whole sentence only where it fits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      const whole = busy ? `${name} is working — Enter still sends` : `Message ${name} — Enter sends, Shift+Enter for a new line`;",
        "to": "      const whole = `Message ${name} — Enter sends, Shift+Enter for a new line`;"
      }
    ]
  },
  {
    "name": "the stamp is judged by the meta row, which never overflows on its left: the label check is gone",
    "catches": "gives a row's stamp up to the time alone where the whole day would cut the label, on append and on every change of width, and the status beside it up to its glyph",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "const cut = [...stamps].filter((time) => { const label = time.parentElement.querySelector(\".lbl\"); return label.scrollWidth > label.clientWidth; });",
        "to": "const cut = [...stamps].filter((time) => { const meta = time.parentElement; return meta.scrollWidth > meta.clientWidth; });"
      }
    ]
  },
  {
    "name": "appended rows are never fitted: only a change of width measures the stamps",
    "catches": "gives a row's stamp up to the time alone where the whole day would cut the label, on append and on every change of width, and the status beside it up to its glyph",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "        fitStamps(added);\n        panel.shown = about.rows.length;",
        "to": "        panel.shown = about.rows.length;"
      }
    ]
  },
  {
    "name": "the status never shortens with the stamp",
    "catches": "gives a row's stamp up to the time alone where the whole day would cut the label, on append and on every change of width, and the status beside it up to its glyph",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "if (st !== null && st.dataset.glyph !== undefined) st.textContent = short ? st.dataset.glyph : st.dataset.whole;",
        "to": "if (st !== null && st.dataset.glyph !== undefined) st.textContent = st.dataset.whole;"
      }
    ]
  },
  {
    "name": "the status shortens on every row, the stamp on none",
    "catches": "gives a row's stamp up to the time alone where the whole day would cut the label, on append and on every change of width, and the status beside it up to its glyph",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    time.textContent = short ? time.dataset.clock : time.dataset.whole;",
        "to": "    time.textContent = time.dataset.whole;"
      }
    ]
  },
  {
    "name": "the Leader's sent glyph is shortened to nothing",
    "catches": "gives a row's stamp up to the time alone where the whole day would cut the label, on append and on every change of width, and the status beside it up to its glyph",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "if (st !== null && st.dataset.glyph !== undefined) st.textContent",
        "to": "if (st !== null) st.textContent"
      }
    ]
  },
  {
    "name": "the cut rows are set whole again",
    "catches": "gives a row's stamp up to the time alone where the whole day would cut the label, on append and on every change of width, and the status beside it up to its glyph",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    for (const time of cut) setStamp(time, true);",
        "to": "    for (const time of cut) setStamp(time, false);"
      }
    ]
  },
  {
    "name": "the stamps are never measured again when the rows change width: the observer is gone",
    "catches": "gives a row's stamp up to the time alone where the whole day would cut the label, on append and on every change of width, and the status beside it up to its glyph",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    new ResizeObserver(() => fitStamps(rows.querySelectorAll(\".t\"))).observe(rows);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a tool line takes the body ink",
    "catches": "draw a tool line in the dim mono of a machine word",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "font-size: .76rem; color: var(--fg-dim); padding: 3px 8px;",
        "to": "font-size: .76rem; color: var(--fg); padding: 3px 8px;"
      }
    ]
  },
  {
    "name": "a tool line is emitted while its rules are renamed",
    "catches": "give every class the page emits at least one rule",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .rows .line { display: block; width: 100%; border: 1px solid var(--line); border-radius: var(--radius); background: var(--panel-2); font-family: var(--mono); font-size: .76rem; color: var(--fg-dim); padding: 3px 8px; margin: 3px 0; overflow-wrap: anywhere; }\n  .rows .line.err { color: var(--bad); }\n  .rows .line .n { color: var(--fg-faint); }\n  /* a line that is one end of a message between two sessions: a click on it finds the message on the Leader's panel */\n  .rows .line.peer { cursor: pointer; border-left: 3px solid var(--accent); }\n",
        "to": "  .rows .tool { display: block; width: 100%; border: 1px solid var(--line); border-radius: var(--radius); background: var(--panel-2); font-family: var(--mono); font-size: .76rem; color: var(--fg-dim); padding: 3px 8px; margin: 3px 0; overflow-wrap: anywhere; }\n  .rows .tool.err { color: var(--bad); }\n  .rows .tool .n { color: var(--fg-faint); }\n  /* a line that is one end of a message between two sessions: a click on it finds the message on the Leader's panel */\n  .rows .tool.peer { cursor: pointer; border-left: 3px solid var(--accent); }\n"
      }
    ]
  },
  {
    "name": "the counter is styled under a class the script never emits",
    "catches": "name no class the page never emits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .rows .line .n { color: var(--fg-faint); }\n",
        "to": "  .rows .line .count { color: var(--fg-faint); }\n"
      }
    ]
  },
  {
    "name": "a failed call never turns its line red: the err mark is gone from the draw",
    "catches": "marks the line of a call that failed red, by its index, with the reason for a tooltip",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "          element.classList.add(\"err\");\n          element.title = about.rows[index].why ?? \"\";\n",
        "to": "          element.title = about.rows[index].why ?? \"\";\n"
      }
    ]
  },
  {
    "name": "identical neighbouring calls are drawn one under the other: the merge is gone",
    "catches": "merges a repeated call into one line with a counter, never a message's line",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "          if (shown.kind === \"line\" && shown.msg === undefined && panel.last !== null && panel.last.text === shown.text) {\n            panel.last.count += 1;\n            panel.last.n.textContent = ` ×${panel.last.count}`;\n            panel.lines.set(index, panel.last.el);\n            continue;\n          }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a Worker panel draws every row it holds: the cap is gone",
    "catches": "keeps the last hundred rows of a Worker panel and every row of the Leader's",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "          while (drawn.length > 100) drawn.shift().remove();\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the first draw builds every row before trimming: the start index is gone",
    "catches": "keeps the last hundred rows of a Worker panel and every row of the Leader's",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "Math.max(panel.shown, about.rows.length - 100)",
        "to": "panel.shown"
      }
    ]
  },
  {
    "name": "a line's missing stamp is pushed to the fitter",
    "catches": "pushes only a stamp it found to the fitter",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "          const time = line.querySelector(\".t\");\n          if (time !== null) added.push(time);\n",
        "to": "          added.push(line.querySelector(\".t\"));\n"
      }
    ]
  },
  {
    "name": "a message the Leader sent takes the ground of one it received",
    "catches": "draw a message the Leader sent on the ground of what goes to the session",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.peer-out .bubble { background: var(--to); border-color: var(--to-line); }\n",
        "to": "  .msg.peer-out .bubble { background: var(--from); border-color: var(--from-line); }\n"
      }
    ]
  },
  {
    "name": "the peer kinds are emitted while their rules are gone",
    "catches": "give every class the page emits at least one rule",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.peer-in .bubble, .msg.peer-out .bubble { overflow-wrap: anywhere; }\n",
        "to": ""
      },
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.peer-in .bubble { background: var(--from); border-color: var(--from-line); }\n",
        "to": ""
      },
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.peer-out .bubble { background: var(--to); border-color: var(--to-line); }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the outcome glyph's bad class is styled but never emitted",
    "catches": "name no class the page never emits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      if (shown.status.bad === true) st.classList.add(\"bad\");\n",
        "to": "      if (shown.status.bad === true) st.classList.add(\"st\");\n"
      }
    ]
  },
  {
    "name": "the compact markdown class is styled but never put on a body",
    "catches": "name no class the page never emits",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      if (shown.tight === true) body.classList.add(\"tight\");\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the stop glyph lands on the head",
    "catches": "puts the stop glyph in the box, shows it while the turn can be stopped, and stops the turn on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    composer.append(box, stop);",
        "to": "    composer.append(box);\n    headLine.append(stop);"
      }
    ]
  },
  {
    "name": "the stop glyph is always there",
    "catches": "puts the stop glyph in the box, shows it while the turn can be stopped, and stops the turn on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    panel.stop.hidden = !stoppable;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the box never makes room for the glyph",
    "catches": "puts the stop glyph in the box, shows it while the turn can be stopped, and stops the turn on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    panel.composer.classList.toggle(\"stoppable\", stoppable);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a click on the glyph stops nothing",
    "catches": "puts the stop glyph in the box, shows it while the turn can be stopped, and stops the turn on a click",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      stop.hidden = true;\n      call(`/sessions/${encodeURIComponent(name)}/stop`, { method: \"POST\" }).catch(() => {});",
        "to": "      stop.hidden = true;"
      }
    ]
  },
  {
    "name": "the glyph sits in the flow of the composer, under the box",
    "catches": "put the stop glyph at the right edge of the box and keep the room for it while it is there",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .composer .stop { position: absolute; right: 9px;",
        "to": "  .composer .stop { position: static; right: 9px;"
      }
    ]
  },
  {
    "name": "the glyph takes a display of its own, so the hidden attribute no longer hides it",
    "catches": "put the stop glyph at the right edge of the box and keep the room for it while it is there",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .composer .stop { position: absolute; right: 9px; bottom: 9px; width: 22px; height: 22px; padding: 4px;",
        "to": "  .composer .stop { position: absolute; right: 9px; bottom: 9px; width: 22px; height: 22px; padding: 4px; display: inline-flex;"
      }
    ]
  },
  {
    "name": "the box keeps no room for the glyph",
    "catches": "put the stop glyph at the right edge of the box and keep the room for it while it is there",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .composer.stoppable textarea { padding-right: 36px; }\n",
        "to": "  .composer.stoppable textarea { padding-right: 10px; }\n"
      }
    ]
  },
  {
    "name": "the head does not clip: its words overflow the panel",
    "catches": "pin every word of a head, clip the head at its edge, and push the theme toggle to it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    display: flex; align-items: baseline; gap: 0; min-width: 0; overflow: hidden; white-space: nowrap;",
        "to": "    display: flex; align-items: baseline; gap: 0; min-width: 0; white-space: nowrap;"
      }
    ]
  },
  {
    "name": "the quota line is drawn without the line panels.mjs makes",
    "catches": "draws the connection word and the quota line on the Leader's head, and marks a lost stream",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      panel.facts.quota.textContent = quotaLine(state.quota);\n",
        "to": "      panel.facts.quota.textContent = state.quota === null ? \"\" : state.quota.session;\n"
      }
    ]
  },
  {
    "name": "the tab title is never drawn",
    "catches": "draws the connection word and the quota line on the Leader's head, and marks a lost stream",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    document.title = title(state);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a divider row is drawn as a bubble",
    "catches": "draws a divider row as the pill between two days, built by the one pill function",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    if (shown.kind === \"divider\") return pill(shown.text);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a dropped stream says nothing",
    "catches": "says disconnected when the stream drops or the server is stopping, keeps asking, and connects again",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      stream.close();\n      lost();\n      setTimeout(probe, 1000);",
        "to": "      stream.close();\n      setTimeout(probe, 1000);"
      }
    ]
  },
  {
    "name": "a server that answers nothing is not called gone",
    "catches": "says disconnected when the stream drops or the server is stopping, keeps asking, and connects again",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      () => {\n        lost();\n        setTimeout(probe, 1000);\n      },",
        "to": "      () => {\n        setTimeout(probe, 1000);\n      },"
      }
    ]
  },
  {
    "name": "a stopping server is connected to again at once",
    "catches": "says disconnected when the stream drops or the server is stopping, keeps asking, and connects again",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      (answered) => (answered.status === 503 ? setTimeout(probe, 1000) : connect()),",
        "to": "      () => connect(),"
      }
    ]
  },
  {
    "name": "a 503 leaves the page connected",
    "catches": "says disconnected when the stream drops or the server is stopping, keeps asking, and connects again",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    if (answered.status === 503) {\n      applyEvent(state, { name: \"stopping\", data: {} });\n      draw();\n    }",
        "to": "    if (answered.status === 503) {\n      draw();\n    }"
      }
    ]
  },
  {
    "name": "the page never listens for the quota event",
    "catches": "says disconnected when the stream drops or the server is stopping, keeps asking, and connects again",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  const EVENTS = [\"snapshot\", \"rows\", \"row\", \"asking\", \"seat\", \"quota\", \"stopping\"];",
        "to": "  const EVENTS = [\"snapshot\", \"rows\", \"row\", \"asking\", \"seat\", \"stopping\"];"
      }
    ]
  },
  {
    "name": "a message's line is merged into the line before it",
    "catches": "merges a repeated call into one line with a counter, never a message's line",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "if (shown.kind === \"line\" && shown.msg === undefined && panel.last !== null",
        "to": "if (shown.kind === \"line\" && panel.last !== null"
      }
    ]
  },
  {
    "name": "a message's line starts a run the next line merges into",
    "catches": "merges a repeated call into one line with a counter, never a message's line",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "            panel.last = shown.msg === undefined ? { text: shown.text, el: line, count: 1, n: line.lastElementChild } : null;",
        "to": "            panel.last = { text: shown.text, el: line, count: 1, n: line.lastElementChild };"
      }
    ]
  },
  {
    "name": "a failed line is marked red with no reason",
    "catches": "marks the line of a call that failed red, by its index, with the reason for a tooltip",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "          element.classList.add(\"err\");\n          element.title = about.rows[index].why ?? \"\";\n",
        "to": "          element.classList.add(\"err\");\n"
      }
    ]
  },
  {
    "name": "a line carries no reason for a tooltip",
    "catches": "builds a tool line as a div with its summary as text, the err class and the reason from the row, and a counter span after it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    line.title = shown.why;\n    line.textContent = shown.text;",
        "to": "    line.textContent = shown.text;"
      }
    ]
  },
  {
    "name": "a message's line is no click",
    "catches": "marks the line of a message with its id and takes a click on it to the message on the Leader's panel",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      line.onclick = () => focusMessage(shown.msg);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a message's line is not marked as one",
    "catches": "marks the line of a message with its id and takes a click on it to the message on the Leader's panel",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      line.classList.add(\"peer\");\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a bubble carries no id for the click to find",
    "catches": "marks the line of a message with its id and takes a click on it to the message on the Leader's panel",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    if (shown.msg !== undefined) line.dataset.msg = shown.msg;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the message a click found is brought to the top rather than into view",
    "catches": "marks the line of a message with its id and takes a click on it to the message on the Leader's panel",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    found.scrollIntoView({ block: \"center\" });",
        "to": "    found.scrollIntoView({ block: \"start\" });"
      }
    ]
  },
  {
    "name": "the message a click found stays lit",
    "catches": "marks the line of a message with its id and takes a click on it to the message on the Leader's panel",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    setTimeout(() => found.classList.remove(\"focus\"), FOCUS_FOR);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a message is folded before it is on the page",
    "catches": "folds a message to a session or from one to three lines, once it is on the page, with a word that opens it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "          const line = rowElement(entry, shown, panel);\n          panel.rows.append(line);\n",
        "to": "          const line = rowElement(entry, shown, panel);\n          if (shown.kind === \"peer-in\" || shown.kind === \"peer-out\") collapsible(line.querySelector(\".md\"));\n          panel.rows.append(line);\n"
      },
      {
        "file": "lib/chat/page.html",
        "from": "          if (shown.kind === \"peer-in\" || shown.kind === \"peer-out\") collapsible(line.querySelector(\".md\"));\n          const time",
        "to": "          const time"
      }
    ]
  },
  {
    "name": "a message that fits stays folded",
    "catches": "folds a message to a session or from one to three lines, once it is on the page, with a word that opens it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    if (body.scrollHeight <= body.clientHeight) {\n      body.classList.remove(\"collapsed\");\n      return;\n    }\n",
        "to": "    if (body.scrollHeight <= body.clientHeight) {\n      return;\n    }\n"
      }
    ]
  },
  {
    "name": "the word under a folded message opens it and never folds it again",
    "catches": "folds a message to a session or from one to three lines, once it is on the page, with a word that opens it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      const folded = body.classList.toggle(\"collapsed\");\n      more.textContent = folded ? \"show all\" : \"collapse\";",
        "to": "      body.classList.remove(\"collapsed\");\n      more.textContent = \"collapse\";"
      }
    ]
  },
  {
    "name": "only a message to a session is folded",
    "catches": "folds a message to a session or from one to three lines, once it is on the page, with a word that opens it",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "if (shown.kind === \"peer-in\" || shown.kind === \"peer-out\") collapsible(",
        "to": "if (shown.kind === \"peer-out\") collapsible("
      }
    ]
  },
  {
    "name": "the renderer is not told who the User is",
    "catches": "draws the glyph and a line with the renderer's words, and the compact markdown under its own class",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "rowOf(entry, { chat: state.chat, seat: panel.name, leader: state.leader, user: state.user })",
        "to": "rowOf(entry, { chat: state.chat, seat: panel.name, leader: state.leader })"
      }
    ]
  },
  {
    "name": "what the User typed to a Worker sits on the User's own ground",
    "catches": "draw what the User typed to a Worker on a ground of its own on the Leader's panel, as the User's words",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.typed .bubble { background: var(--typed); border-color: var(--typed-line); white-space: pre-wrap; overflow-wrap: anywhere; }",
        "to": "  .msg.typed .bubble { background: var(--me); border-color: var(--me-line); white-space: pre-wrap; overflow-wrap: anywhere; }"
      }
    ]
  },
  {
    "name": "a message's line looks like any other line",
    "catches": "mark the line of a message as a click, and light the message a click found",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .rows .line.peer { cursor: pointer; border-left: 3px solid var(--accent); }",
        "to": "  .rows .line.peer { cursor: pointer; }"
      }
    ]
  },
  {
    "name": "a folded message shows more than three lines",
    "catches": "clip a folded message to three lines, faded at its edge, and draw the word that opens it as a link",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": ".bubble .md.collapsed { max-height: 4.5em;",
        "to": ".bubble .md.collapsed { max-height: 22em;"
      }
    ]
  },
  {
    "name": "the word that opens a folded message looks like a button",
    "catches": "clip a folded message to three lines, faded at its edge, and draw the word that opens it as a link",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .more { font-size: .78rem; color: var(--accent); cursor: pointer; margin-top: 4px; background: none; border: 0; padding: 0; font-family: var(--sans); }",
        "to": "  .more { font-size: .78rem; color: var(--accent); cursor: pointer; margin-top: 4px; background: none; padding: 0; font-family: var(--sans); }"
      }
    ]
  },
  {
    "name": "a second formatter for the pill",
    "catches": "stamps a row with the reader's own day and time, the weekday written out, on a 24-hour clock, and the day pill with the same",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "\n  function stamp(at) {",
        "to": "\n  const dayOf = new Intl.DateTimeFormat(\"en-GB\", { weekday: \"short\", day: \"2-digit\", month: \"short\" });\n\n  function stamp(at) {"
      }
    ]
  },
  {
    "name": "the month is a word cut short",
    "catches": "stamps a row with the reader's own day and time, the weekday written out, on a 24-hour clock, and the day pill with the same",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "month: \"2-digit\", day: \"2-digit\", weekday: \"long\"",
        "to": "month: \"short\", day: \"2-digit\", weekday: \"long\""
      }
    ]
  },
  {
    "name": "a pill before the first row",
    "catches": "puts the pill between two days only, with the whole stamp of the first row of the new day on it, and never first",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "            if (panel.day !== null) {\n              panel.last = null;\n              panel.rows.append(dayPill(when));\n            }",
        "to": "            panel.last = null;\n            panel.rows.append(dayPill(when));"
      }
    ]
  },
  {
    "name": "the pill carries the day alone",
    "catches": "puts the pill between two days only, with the whole stamp of the first row of the new day on it, and never first",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    const element = pill(when.whole);",
        "to": "    const element = pill(when.day);"
      }
    ]
  },
  {
    "name": "a leading pill survives the trim",
    "catches": "puts the pill between two days only, with the whole stamp of the first row of the new day on it, and never first",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "          while (drawn.length > 0 && drawn[0].dataset.day !== undefined) drawn.shift().remove();\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a waiting row keeps the User's ground",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "        who.append(tag);\n        line.classList.add(\"pending\");",
        "to": "        who.append(tag);"
      }
    ]
  },
  {
    "name": "the draw never says queued",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "delivery(shown.delivered, panel.section.classList.contains(\"busy\"), panel.name)",
        "to": "delivery(shown.delivered, false, panel.name)"
      }
    ]
  },
  {
    "name": "a delivered row keeps the waiting ground",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    element.classList.remove(\"pending\");\n    element.querySelector(\".tag\").remove();",
        "to": "    element.querySelector(\".tag\").remove();"
      }
    ]
  },
  {
    "name": "the waiting words go in the meta beside the stamp",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "        who.append(tag);\n        line.classList.add(\"pending\");",
        "to": "        meta.append(tag);\n        line.classList.add(\"pending\");"
      }
    ]
  },
  {
    "name": "the delivered glyph goes in the label row",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "        meta.append(deliveredElement(said));",
        "to": "        who.append(deliveredElement(said));"
      }
    ]
  },
  {
    "name": "the waiting words stay beside the label once delivered",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    element.querySelector(\".tag\").remove();\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a row delivered after its draw gets the glyph in the label row",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    time.parentElement.append(deliveredElement(delivery(true, false, panel.name)));",
        "to": "    element.querySelector(\".who\").append(deliveredElement(delivery(true, false, panel.name)));"
      }
    ]
  },
  {
    "name": "a row delivered after its draw is not fitted",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    fitStamps([time]);\n  }",
        "to": "  }"
      }
    ]
  },
  {
    "name": "the delivered status carries no glyph for the fit",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    st.dataset.glyph = said.glyph;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a row delivered after its draw stays waiting",
    "catches": "draws the User's row waiting on the dashed ground until the server says delivered, then changes the row it has",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "        if (about.rows[index].delivered === true) deliveredRow(panel, index);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the click reads the stamp off the screen",
    "catches": "makes every stamp a click that puts a reference to its row into the composer, spelled out on send",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "reference(panel.refs, time.dataset.whole, who, body.textContent)",
        "to": "reference(panel.refs, time.textContent, who, body.textContent)"
      }
    ]
  },
  {
    "name": "what is sent carries the tokens unspelled",
    "catches": "makes every stamp a click that puts a reference to its row into the composer, spelled out on send",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      const text = spellReferences(refs, box.value);",
        "to": "      const text = box.value;"
      }
    ]
  },
  {
    "name": "the stamp is no click",
    "catches": "makes every stamp a click that puts a reference to its row into the composer, spelled out on send",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    time.addEventListener(\"click\", () => pointAt(panel, time, shown.who, body));\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the stamp does not look like a click",
    "catches": "make the stamp a click, and say so under the pointer",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .meta .t { flex: 0 0 auto; order: 1; cursor: pointer; }",
        "to": "  .meta .t { flex: 0 0 auto; order: 1; }"
      }
    ]
  },
  {
    "name": "the waiting ground is not dashed",
    "catches": "draw the User's row on the waiting ground, dashed and dimmed, until it is delivered, and its waiting words small and faint beside the label",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.pending .bubble { background: var(--pending); border-style: dashed; color: var(--fg-dim); }",
        "to": "  .msg.pending .bubble { background: var(--pending); color: var(--fg-dim); }"
      }
    ]
  },
  {
    "name": "the waiting ground keeps the full ink",
    "catches": "draw the User's row on the waiting ground, dashed and dimmed, until it is delivered, and its waiting words small and faint beside the label",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .msg.pending .bubble { background: var(--pending); border-style: dashed; color: var(--fg-dim); }",
        "to": "  .msg.pending .bubble { background: var(--pending); border-style: dashed; }"
      }
    ]
  },
  {
    "name": "the waiting words are as dark as the label",
    "catches": "draw the User's row on the waiting ground, dashed and dimmed, until it is delivered, and its waiting words small and faint beside the label",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .who .tag { font-weight: 400; color: var(--fg-faint); font-family: var(--mono); font-size: .72rem; }",
        "to": "  .who .tag { font-weight: 400; font-family: var(--mono); font-size: .72rem; }"
      }
    ]
  },
  {
    "name": "the waiting words are as big and as bold as the label",
    "catches": "draw the User's row on the waiting ground, dashed and dimmed, until it is delivered, and its waiting words small and faint beside the label",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .who .tag { font-weight: 400; color: var(--fg-faint); font-family: var(--mono); font-size: .72rem; }",
        "to": "  .who .tag { color: var(--fg-faint); font-family: var(--mono); }"
      }
    ]
  },
  {
    "name": "a code block one and a half lines high",
    "catches": "set a code block at .85rem, 1.4 lines high, on the block itself, the code inside inheriting",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "overflow-x: auto; font-size: .85rem; line-height: 1.4; }",
        "to": "overflow-x: auto; font-size: .85rem; line-height: 1.5; }"
      }
    ]
  },
  {
    "name": "the code inside a block sets its own size",
    "catches": "set a code block at .85rem, 1.4 lines high, on the block itself, the code inside inheriting",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  .md pre code { background: none; padding: 0; font-size: inherit; }",
        "to": "  .md pre code { background: none; padding: 0; font-size: .85rem; }"
      }
    ]
  },
  {
    "name": "the questions go back into a block of their own between the rows and the composer",
    "catches": "draws a question as a card among the rows, never in a block of its own below them",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    section.append(headLine, rows, composer);",
        "to": "    const asking = document.createElement(\"div\");\n    asking.className = \"asking\";\n    section.append(headLine, rows, asking, composer);"
      },
      {
        "file": "lib/chat/page.html",
        "from": "          panel.rows.append(card);\n          panel.last = null;\n          asked = true;",
        "to": "          panel.section.lastElementChild.before(card);\n          panel.last = null;\n          asked = true;"
      }
    ]
  },
  {
    "name": "a card does not end a run of tool lines: the next repeated call counts itself into a line above the card",
    "catches": "draws a question as a card among the rows, never in a block of its own below them",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "          panel.rows.append(card);\n          panel.last = null;\n          asked = true;",
        "to": "          panel.rows.append(card);\n          asked = true;"
      }
    ]
  },
  {
    "name": "the scroll is decided before the cards are appended, so a card never comes into view and never shows the pill",
    "catches": "draws a question as a card among the rows, never in a block of its own below them",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      // The questions, after the rows: a card answered or taken down leaves, a card not yet\n      // drawn goes where the newest row went and ends a run of tool lines.\n      let asked = false;\n      for (const [id, card] of panel.drawn) {\n        if (!about.asking.some((request) => request.id === id)) {\n          card.remove();\n          panel.drawn.delete(id);\n        }\n      }\n      for (const request of about.asking) {\n        if (!panel.drawn.has(request.id)) {\n          const card = question(panel.name, request);\n          panel.drawn.set(request.id, card);\n          panel.rows.append(card);\n          panel.last = null;\n          asked = true;\n        }\n      }\n      // Rows and cards are appended after the pill; it goes back to the end so it sticks to the\n      // bottom edge.\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n      // A card scrolls into view as a row does: when the reader was at the newest, the newest\n      // is followed; otherwise the pill says something landed below.\n      if (added.length > 0 || asked) {\n        if (atTheNewest) {\n          panel.rows.scrollTop = panel.rows.scrollHeight;\n        } else if (panel.jump !== null && !near) {\n          panel.jump.classList.add(\"show\");\n        }\n      }\n",
        "to": "      let asked = false;\n      // A card scrolls into view as a row does: when the reader was at the newest, the newest\n      // is followed; otherwise the pill says something landed below.\n      if (added.length > 0 || asked) {\n        if (atTheNewest) {\n          panel.rows.scrollTop = panel.rows.scrollHeight;\n        } else if (panel.jump !== null && !near) {\n          panel.jump.classList.add(\"show\");\n        }\n      }\n      // The questions, after the rows: a card answered or taken down leaves, a card not yet\n      // drawn goes where the newest row went and ends a run of tool lines.\n      for (const [id, card] of panel.drawn) {\n        if (!about.asking.some((request) => request.id === id)) {\n          card.remove();\n          panel.drawn.delete(id);\n        }\n      }\n      for (const request of about.asking) {\n        if (!panel.drawn.has(request.id)) {\n          const card = question(panel.name, request);\n          panel.drawn.set(request.id, card);\n          panel.rows.append(card);\n          panel.last = null;\n          asked = true;\n        }\n      }\n      // Rows and cards are appended after the pill; it goes back to the end so it sticks to the\n      // bottom edge.\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n"
      }
    ]
  },
  {
    "name": "the cards are appended after the pill is put back, so the pill sits above the newest card",
    "catches": "draws a question as a card among the rows, never in a block of its own below them",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      // The questions, after the rows: a card answered or taken down leaves, a card not yet\n      // drawn goes where the newest row went and ends a run of tool lines.\n      let asked = false;\n      for (const [id, card] of panel.drawn) {\n        if (!about.asking.some((request) => request.id === id)) {\n          card.remove();\n          panel.drawn.delete(id);\n        }\n      }\n      for (const request of about.asking) {\n        if (!panel.drawn.has(request.id)) {\n          const card = question(panel.name, request);\n          panel.drawn.set(request.id, card);\n          panel.rows.append(card);\n          panel.last = null;\n          asked = true;\n        }\n      }\n      // Rows and cards are appended after the pill; it goes back to the end so it sticks to the\n      // bottom edge.\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n",
        "to": "      // Rows and cards are appended after the pill; it goes back to the end so it sticks to the\n      // bottom edge.\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n      // The questions, after the rows: a card answered or taken down leaves, a card not yet\n      // drawn goes where the newest row went and ends a run of tool lines.\n      let asked = false;\n      for (const [id, card] of panel.drawn) {\n        if (!about.asking.some((request) => request.id === id)) {\n          card.remove();\n          panel.drawn.delete(id);\n        }\n      }\n      for (const request of about.asking) {\n        if (!panel.drawn.has(request.id)) {\n          const card = question(panel.name, request);\n          panel.drawn.set(request.id, card);\n          panel.rows.append(card);\n          panel.last = null;\n          asked = true;\n        }\n      }\n"
      }
    ]
  },
  {
    "name": "the cards are drawn while the rows are still on their way, so the first draw puts them above the rows",
    "catches": "draws a question as a card among the rows, never in a block of its own below them",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      // The questions, after the rows: a card answered or taken down leaves, a card not yet\n      // drawn goes where the newest row went and ends a run of tool lines.\n      let asked = false;\n      for (const [id, card] of panel.drawn) {\n        if (!about.asking.some((request) => request.id === id)) {\n          card.remove();\n          panel.drawn.delete(id);\n        }\n      }\n      for (const request of about.asking) {\n        if (!panel.drawn.has(request.id)) {\n          const card = question(panel.name, request);\n          panel.drawn.set(request.id, card);\n          panel.rows.append(card);\n          panel.last = null;\n          asked = true;\n        }\n      }\n      // Rows and cards are appended after the pill; it goes back to the end so it sticks to the\n      // bottom edge.\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n      // A card scrolls into view as a row does: when the reader was at the newest, the newest\n      // is followed; otherwise the pill says something landed below.\n      if (added.length > 0 || asked) {\n        if (atTheNewest) {\n          panel.rows.scrollTop = panel.rows.scrollHeight;\n        } else if (panel.jump !== null && !near) {\n          panel.jump.classList.add(\"show\");\n        }\n      }\n    }\n  }\n",
        "to": "      // Rows and cards are appended after the pill; it goes back to the end so it sticks to the\n      // bottom edge.\n      if (panel.jump !== null && panel.jump !== panel.rows.lastElementChild) panel.rows.append(panel.jump);\n      if (added.length > 0) {\n        if (atTheNewest) {\n          panel.rows.scrollTop = panel.rows.scrollHeight;\n        } else if (panel.jump !== null && !near) {\n          panel.jump.classList.add(\"show\");\n        }\n      }\n    }\n      // The questions, after the rows: a card answered or taken down leaves, a card not yet\n      // drawn goes where the newest row went and ends a run of tool lines.\n      for (const [id, card] of panel.drawn) {\n        if (!about.asking.some((request) => request.id === id)) {\n          card.remove();\n          panel.drawn.delete(id);\n        }\n      }\n      for (const request of about.asking) {\n        if (!panel.drawn.has(request.id)) {\n          const card = question(panel.name, request);\n          panel.drawn.set(request.id, card);\n          panel.rows.append(card);\n          panel.last = null;\n          asked = true;\n        }\n      }\n  }\n"
      }
    ]
  },
  {
    "name": "rows drawn again from nothing keep the cards they no longer show",
    "catches": "draws a question as a card among the rows, never in a block of its own below them",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "        panel.waiting.clear();\n        panel.drawn.clear();\n",
        "to": "        panel.waiting.clear();\n"
      }
    ]
  },
  {
    "name": "a question's card is one of the hundred rows a Worker panel lets go",
    "catches": "keeps the last hundred rows of a Worker panel and every row of the Leader's",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "child.matches(\".msg:not(.perm), .line, .divider\")",
        "to": "child.matches(\".msg, .line, .divider\")"
      }
    ]
  }
]

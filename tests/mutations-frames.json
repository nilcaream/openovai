[
  {
    "name": "a body is framed as typed",
    "catches": "neutralises the closing and the opening form inside the body",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  return String(text).replace(FRAME_TAG, \"&lt;$1$2\");",
        "to": "  return String(text);"
      }
    ]
  },
  {
    "name": "only the exact letter case of a frame name is neutralised",
    "catches": "in any letter case",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "(?![A-Za-z0-9_])/gi;",
        "to": "(?![A-Za-z0-9_])/g;"
      }
    ]
  },
  {
    "name": "whitespace around the slash is not tolerated",
    "catches": "with any whitespace around the slash",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "/<(\\s*\\/?\\s*)(user|message|server-event|queue|ref)",
        "to": "/<(\\/?)(user|message|server-event|queue|ref)"
      }
    ]
  },
  {
    "name": "a server event is not one of the names",
    "catches": "when it is a server event",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "(user|message|server-event|queue|ref)",
        "to": "(user|message|queue|ref)"
      }
    ]
  },
  {
    "name": "a message is not one of the names",
    "catches": "when it is a message",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "(user|message|server-event|queue|ref)",
        "to": "(user|server-event|queue|ref)"
      }
    ]
  },
  {
    "name": "the slash form is not caught",
    "catches": "with a slash form of every name",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "(?![A-Za-z0-9_])/gi;",
        "to": "(?![A-Za-z0-9_/])/gi;"
      }
    ]
  },
  {
    "name": "every angle bracket is escaped",
    "catches": "leaves every other angle bracket as typed",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  return String(text).replace(FRAME_TAG, \"&lt;$1$2\");",
        "to": "  return String(text).replace(/</g, \"&lt;\");"
      }
    ]
  },
  {
    "name": "the look-ahead is dropped",
    "catches": "leaves every other angle bracket as typed",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "(user|message|server-event|queue|ref)(?![A-Za-z0-9_])/gi;",
        "to": "(user|message|server-event|queue|ref)/gi;"
      }
    ]
  },
  {
    "name": "an ampersand is escaped too",
    "catches": "leaves an ampersand as typed",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  return String(text).replace(FRAME_TAG, \"&lt;$1$2\");",
        "to": "  return String(text).replace(/&/g, \"&amp;\").replace(FRAME_TAG, \"&lt;$1$2\");"
      }
    ]
  },
  {
    "name": "a message body is framed as typed",
    "catches": "neutralises the body of a message",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": ">${neutralise(text)}</message>`",
        "to": ">${text}</message>`"
      }
    ]
  },
  {
    "name": "a server event body is framed as typed",
    "catches": "neutralises the body of a server event",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "`${opening}>${withRefs(body, refs, \"</server-event>\")}`",
        "to": "`${opening}>${body}${withRefs(\"\", refs, \"</server-event>\")}`"
      }
    ]
  },
  {
    "name": "a server event with no body is not self-closing",
    "catches": "frames a server event without a body as one self-closing tag",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "body === undefined ? `${opening}/>` : ",
        "to": ""
      }
    ]
  },
  {
    "name": "the sender is not framed",
    "catches": "frames a message with who it is from",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "`<message from=\"${token(\"from\", from)}\"${marked}>",
        "to": "`<message${marked}>"
      }
    ]
  },
  {
    "name": "a frame does not say its kind",
    "catches": "says which kind of frame it is",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "    this.kind = kind;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "an attribute value is taken as free text",
    "catches": "refuses an attribute value that is not a plain token",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  if (typeof value !== \"string\" || !TOKEN.test(value)) {",
        "to": "  if (typeof value !== \"string\") {"
      }
    ]
  },
  {
    "name": "the sender is taken as free text",
    "catches": "refuses a sender that is not a plain token",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "`<message from=\"${token(\"from\", from)}\"${marked}>",
        "to": "`<message from=\"${from}\"${marked}>"
      }
    ]
  },
  {
    "name": "the event kind is taken as free text",
    "catches": "refuses an event kind that is not a plain token",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  if (!EVENTS.includes(kind)) {\n    throw new Error(`${JSON.stringify(kind)} is not an event the server says`);\n  }\n",
        "to": ""
      },
      {
        "file": "lib/chat/frames.mjs",
        "from": "`<server-event type=\"${token(\"type\", kind)}\"",
        "to": "`<server-event type=\"${kind}\""
      }
    ]
  },
  {
    "name": "an attribute name is taken as free text",
    "catches": "refuses an attribute name that is not a plain lower-case word",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "      if (!ATTRIBUTE.test(key)) {",
        "to": "      if (false) {"
      }
    ]
  },
  {
    "name": "a frame is known by its shape",
    "catches": "refuses a string shaped like a frame",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  return value instanceof Frame;",
        "to": "  return value instanceof Frame || (typeof value?.text === \"string\" ? /^<(user|message|server-event)[ >]/.test(value.text) : /^<(user|message|server-event)[ >]/.test(String(value)));"
      }
    ]
  },
  {
    "name": "the constructor takes anybody's frame",
    "catches": "cannot be built through the constructor of one it made",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "    if (key !== BUILT_HERE) {",
        "to": "    if (false) {"
      }
    ]
  },
  {
    "name": "a queue is not one of the names",
    "catches": "when it is a queue",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "(user|message|server-event|queue|ref)(?![A-Za-z0-9_])/gi;",
        "to": "(user|message|server-event|ref)(?![A-Za-z0-9_])/gi;"
      }
    ]
  },
  {
    "name": "a queue frame puts the server's events on top instead of in their arrival place",
    "catches": "is the envelope and every item in its arrival order, each stamped, nothing else",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  const inner = items.map(",
        "to": "  const inner = [...items.filter((item) => item.frame.kind === \"server-event\"), ...items.filter((item) => item.frame.kind !== \"server-event\")].map("
      }
    ]
  },
  {
    "name": "a queue frame reverses its items",
    "catches": "is the envelope and every item in its arrival order, each stamped, nothing else",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  const inner = items.map(",
        "to": "  const inner = items.toReversed().map("
      }
    ]
  },
  {
    "name": "a queue frame stamps no item with when it arrived",
    "catches": "is the envelope and every item in its arrival order, each stamped, nothing else",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "frame.text.replace(OPENING_TAG, `<$1$2 at=\"${token(\"at\", hhmm(at))}\"$3>`)",
        "to": "frame.text"
      }
    ]
  },
  {
    "name": "a queue frame stamps the moment in full rather than as the clock on the wall",
    "catches": "is the envelope and every item in its arrival order, each stamped, nothing else",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "token(\"at\", hhmm(at))",
        "to": "token(\"at\", new Date(at).toISOString())"
      }
    ]
  },
  {
    "name": "a queue frame's envelope says what the reader can count",
    "catches": "is the envelope and every item in its arrival order, each stamped, nothing else",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "`<queue>\\n",
        "to": "`<queue n=\"${items.length}\">\\n"
      }
    ]
  },
  {
    "name": "a queue frame leaves its children flush with the envelope",
    "catches": "is the envelope and every item in its arrival order, each stamped, nothing else",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "`  ${frame.text.replace(",
        "to": "`${frame.text.replace("
      }
    ]
  },
  {
    "name": "a queue frame puts a text node between its elements",
    "catches": "is the envelope and every item in its arrival order, each stamped, nothing else",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "\\n${inner.join(\"\\n\")}\\n</queue>`",
        "to": "\\n${items.length} items were waiting while you were busy.\\n${inner.join(\"\\n\")}\\n</queue>`"
      }
    ]
  },
  {
    "name": "a queue of none is allowed",
    "catches": "is one frame or more: one alone is a queue of one, never bare",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "items.length < 1",
        "to": "items.length < 0"
      }
    ]
  },
  {
    "name": "a queue takes a queue",
    "catches": "holds frames built here and never a queue",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "if (!isFrame(frame) || frame.kind === \"queue\") {",
        "to": "if (!isFrame(frame)) {"
      }
    ]
  },
  {
    "name": "a frame alone goes in bare, without the envelope",
    "catches": "is one frame or more: one alone is a queue of one, never bare",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  const inner = items.map(",
        "to": "  if (items.length === 1) return items[0].frame;\n  const inner = items.map("
      }
    ]
  },
  {
    "name": "a <ref> the User typed poses as the server's",
    "catches": "neutralises a <ref> in the words or in a row, and is the plain frame with no rows",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "(user|message|server-event|queue|ref)",
        "to": "(user|message|server-event|queue)"
      }
    ]
  },
  {
    "name": "the rows a reference names are left out of the frame",
    "catches": "is the words, then one <ref> per row, in the shape a queue indents",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  if (refs.length === 0) return",
        "to": "  if (true) return"
      }
    ]
  }
]

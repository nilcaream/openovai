[
  {
    "name": "the numbers of a version are compared as words",
    "catches": "compares the numbers rather than the words",
    "edits": [
      {
        "file": "lib/version.mjs",
        "from": ".split(\".\").map(Number);",
        "to": ".split(\".\").map(String);"
      }
    ]
  },
  {
    "name": "the earlier version is the larger number",
    "catches": "compares the numbers rather than the words",
    "edits": [
      {
        "file": "lib/version.mjs",
        "from": "      return mine < theirs;",
        "to": "      return mine > theirs;"
      }
    ]
  },
  {
    "name": "a version written with fewer numbers is earlier than the same version written with more",
    "catches": "compares the numbers rather than the words",
    "edits": [
      {
        "file": "lib/version.mjs",
        "from": "    const mine = one[at] ?? 0;",
        "to": "    const mine = one[at] ?? -1;"
      }
    ]
  },
  {
    "name": "a version written with more numbers is later than the same version written with fewer",
    "catches": "compares the numbers rather than the words",
    "edits": [
      {
        "file": "lib/version.mjs",
        "from": "    const theirs = other[at] ?? 0;",
        "to": "    const theirs = other[at] ?? 99;"
      }
    ]
  },
  {
    "name": "a candidate that is not a row of numbers is placed anyway",
    "catches": "calls nothing older that it cannot place",
    "edits": [
      {
        "file": "lib/version.mjs",
        "from": "  if (one === null || other === null) {",
        "to": "  if (other === null) {"
      }
    ]
  },
  {
    "name": "an instance with no version to compare with is placed anyway",
    "catches": "calls nothing older that it cannot place",
    "edits": [
      {
        "file": "lib/version.mjs",
        "from": "  if (one === null || other === null) {",
        "to": "  if (one === null) {"
      }
    ]
  },
  {
    "name": "an older release is not noticed as older",
    "catches": "refuses, saying both versions",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  if (!downgrade && isOlderThan(release.version, here)) {",
        "to": "  if (!downgrade && false) {"
      }
    ]
  },
  {
    "name": "the refusal says what was found and not what the instance is on",
    "catches": "refuses, saying both versions",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "`this instance is on ${here} and ${from} is ${release.version}",
        "to": "`this instance and ${from} is ${release.version}"
      }
    ]
  },
  {
    "name": "the refusal says what the instance is on and not what was found",
    "catches": "refuses, saying both versions",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "${from} is ${release.version}, which is older",
        "to": "${from} is one, which is older"
      }
    ]
  },
  {
    "name": "going backwards is refused as a command line, under a usage nobody mistyped",
    "catches": "refuses it as a release and not as a command line",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "    throw new ReleaseError(\n      `this instance is on ${here}",
        "to": "    throw new UsageError(\n      `this instance is on ${here}"
      }
    ]
  },
  {
    "name": "going backwards is said out loud and then done anyway",
    "catches": "does not take the instance back to the older one",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "    throw new ReleaseError(\n      `this instance is on ${here}",
        "to": "    console.error(\n      `this instance is on ${here}"
      }
    ]
  },
  {
    "name": "the flag is not read, so nothing can be taken on purpose",
    "catches": "takes it when told to go backwards on purpose",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  const downgrade = argv.includes(DOWNGRADE);",
        "to": "  const downgrade = false;"
      }
    ]
  },
  {
    "name": "the guard does not ask whether it was told to go backwards",
    "catches": "takes it when told to go backwards on purpose",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  if (!downgrade && isOlderThan(release.version, here)) {",
        "to": "  if (isOlderThan(release.version, here)) {"
      }
    ]
  },
  {
    "name": "the flag is left in what is read as the place to look",
    "catches": "takes it when told to go backwards on purpose",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  const rest = argv.filter((word) => word !== DOWNGRADE);",
        "to": "  const rest = argv;"
      }
    ]
  },
  {
    "name": "update takes no more words than it did before the flag",
    "catches": "takes it when told to go backwards on purpose",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  update: { most: 3, shape:",
        "to": "  update: { most: 2, shape:"
      }
    ]
  },
  {
    "name": "an update seeds nothing the person lacks",
    "catches": "seeds the missing file as a fresh install would",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  const seeded = seedUserContent(root, readConfig(root).leader);",
        "to": "  const seeded = [];"
      }
    ]
  },
  {
    "name": "an update writes the person's files over as though they were missing",
    "catches": "leaves a file the person has exactly as it is",
    "edits": [
      {
        "file": "lib/seed.mjs",
        "from": "  if (fs.existsSync(target)) {\n    return [];\n  }\n  fs.mkdirSync(path.dirname(target), { recursive: true });",
        "to": "  fs.mkdirSync(path.dirname(target), { recursive: true });"
      }
    ]
  },
  {
    "name": "an update seeds and says nothing about it",
    "catches": "says what it seeded, and why",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  if (seeded.length > 0) {",
        "to": "  if (seeded.length < 0) {"
      }
    ]
  },
  {
    "name": "an update does not look for the sessions of this instance at all",
    "catches": "refuses",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  const processes = runningHere(root);",
        "to": "  const processes = [];"
      }
    ]
  },
  {
    "name": "the refusal names the session but not what ends it",
    "catches": "names each session with the command that ends it, and the command it is running",
    "edits": [
      {
        "file": "lib/running.mjs",
        "from": "      ...sessions.map(({ pid }) => `  kill ${pid}   # ${commandOf(pid)}`),",
        "to": "      ...sessions.map(({ pid }) => `  ${pid}   # ${commandOf(pid)}`),"
      }
    ]
  },
  {
    "name": "the refusal names what ends the session but not what it is running",
    "catches": "names each session with the command that ends it, and the command it is running",
    "edits": [
      {
        "file": "lib/running.mjs",
        "from": "      ...sessions.map(({ pid }) => `  kill ${pid}   # ${commandOf(pid)}`),",
        "to": "      ...sessions.map(({ pid }) => `  kill ${pid}`),"
      }
    ]
  },
  {
    "name": "a command that cannot be read is left blank",
    "catches": "says ? for a command that cannot be read",
    "edits": [
      {
        "file": "lib/running.mjs",
        "from": "  return found.length === 0 ? \"?\" : found.join(\" \");",
        "to": "  return found.join(\" \");"
      }
    ]
  },
  {
    "name": "a session of any instance on the machine is taken for one of ours",
    "catches": "names nothing of another instance's",
    "edits": [
      {
        "file": "lib/running.mjs",
        "from": "    if (environment === null || environment.get(CONFIG_DIR) !== ours) {",
        "to": "    if (environment === null || environment.get(CONFIG_DIR) === undefined) {"
      }
    ]
  },
  {
    "name": "the running sessions are said out loud and the update goes on",
    "catches": "leaves the instance on the version it was on while the sessions run",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "    throw new ReleaseError(describeRunning(processes));",
        "to": "    console.error(describeRunning(processes));"
      }
    ]
  },
  {
    "name": "the update counts its own process as a session when it is started in one's environment",
    "catches": "does not count itself as a session, whatever environment it was started in",
    "edits": [
      {
        "file": "lib/running.mjs",
        "from": "    if (pid === process.pid) {\n      continue;\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the server is started with whatever home the command inherited",
    "catches": "goes through: that server is a session of its own instance, not of this one",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "    env: { ...process.env, CLAUDE_CONFIG_DIR: home(root) },\n",
        "to": ""
      }
    ]
  },
  {
    "name": "bin/ is swapped for a new directory like the rest of the payload",
    "catches": "leaves a shell standing in bin/ able to run ./ovai from there afterwards",
    "edits": [
      {
        "file": "lib/release.mjs",
        "from": "  for (const entry of PAYLOAD.filter((one) => one !== COPIED_OVER)) {",
        "to": "  for (const entry of PAYLOAD) {"
      }
    ]
  },
  {
    "name": "the launcher is written anew as bytes, with whatever mode a new file gets",
    "catches": "keeps bin/ovai executable",
    "edits": [
      {
        "file": "lib/release.mjs",
        "from": "  fs.cpSync(path.join(tree, COPIED_OVER), over, { recursive: true, force: true });",
        "to": "  for (const name of fs.readdirSync(path.join(tree, COPIED_OVER))) {\n    fs.rmSync(path.join(over, name), { force: true });\n    fs.writeFileSync(path.join(over, name), fs.readFileSync(path.join(tree, COPIED_OVER, name)));\n  }"
      }
    ]
  },
  {
    "name": "the skill an earlier version shipped is left in the instance",
    "catches": "takes away the skill an earlier version shipped and leaves the skill beside it that the person wrote",
    "edits": [
      {
        "file": "lib/release.mjs",
        "from": "  for (const entry of RETIRED) {\n    fs.rmSync(path.join(root, entry), { recursive: true, force: true });\n  }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a release directory is asked its version before it is asked whether it is a workspace",
    "catches": "refuses a package that is not an OpenOv AI instance, saying what it is missing",
    "edits": [
      {
        "file": "lib/release.mjs",
        "from": "    const wrong = notAWorkspace(from);\n    if (wrong !== null) {\n      throw new ReleaseError(wrong);\n    }\n    return { version: version(from), package: from, unpacked: true };",
        "to": "    const said = version(from);\n    if (said === null) {\n      throw new ReleaseError(`${from} has no version in it`);\n    }\n    return { version: said, package: from, unpacked: true };"
      }
    ]
  },
  {
    "name": "an update leaves the harness's attribution on in settings that say nothing about it",
    "catches": "turns the attribution off in those settings",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  const quieted = turnAttributionOff(root);",
        "to": "  const quieted = [];"
      }
    ]
  },
  {
    "name": "an update turns the attribution off without saying so",
    "catches": "says it did, and where",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  if (quieted.length > 0) {",
        "to": "  if (quieted.length < 0) {"
      }
    ]
  },
  {
    "name": "an update fetches nothing after the swap",
    "catches": "runs the new payload's runtime.sh ensure after the swap, and prints what it said",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "const fetched = spawnSync(\"sh\", [path.join(root, \"lib\", \"runtime.sh\"), \"ensure\"], { stdio: \"inherit\" });",
        "to": "const fetched = { status: 0 };"
      }
    ]
  },
  {
    "name": "an update reports success over a runtime that could not be fetched",
    "catches": "fails, saying what fetches it again, when the new payload's runtime cannot be fetched",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  if (fetched.status !== 0) {\n    throw new ReleaseError(",
        "to": "  if (fetched.status !== 0 && false) {\n    throw new ReleaseError("
      }
    ]
  },
  {
    "name": "everything a seat carries is called a session, whatever it runs",
    "catches": "goes through once the session is gone, whatever the seat left running",
    "edits": [
      {
        "file": "lib/running.mjs",
        "from": "session: seat === null || runsClaude(argumentsOf(pid))",
        "to": "session: true"
      }
    ]
  },
  {
    "name": "a leftover is named by its pid alone, without the seat that left it",
    "catches": "names each leftover by its seat, and ends all of them in one line",
    "edits": [
      {
        "file": "lib/running.mjs",
        "from": "`${seat} left this running: ${commandOf(pid)} (pid ${pid})`",
        "to": "`left running: ${commandOf(pid)} (pid ${pid})`"
      }
    ]
  },
  {
    "name": "a seat's session is named among what the seat left running",
    "catches": "refuses while the seat's session runs, naming the session and not calling it a leftover",
    "edits": [
      {
        "file": "lib/running.mjs",
        "from": "  const leftovers = running.filter(({ session }) => session === false);",
        "to": "  const leftovers = running.filter(({ seat }) => seat !== null);"
      }
    ]
  }
]

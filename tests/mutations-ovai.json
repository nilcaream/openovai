[
  {
    "name": "the budget in every brief is raised past anything worth calling a budget",
    "catches": "tells the worker how much a brief it writes may spend",
    "edits": [
      {
        "file": "lib/desks.mjs",
        "from": "- Answer in at most 15 tool calls; if you cannot, report what you have and say what is missing.",
        "to": "- Answer in at most 500 tool calls; if you cannot, report what you have and say what is missing."
      }
    ]
  },
  {
    "name": "the budget is given without the three rules that keep it",
    "catches": "tells the worker the three rules that keep that budget",
    "edits": [
      {
        "file": "lib/desks.mjs",
        "from": "\n- When several are working at once, split the files between them; never let two read the same one.\n- Search first, then read the part that matched, rather than reading a whole file to find it.\n- What has been filed away is history, not a place to look things up. Read what is there now.",
        "to": ""
      }
    ]
  },
  {
    "name": "the brief is left to inherit the rule rather than carry it",
    "catches": "tells the worker that only the brief carries it",
    "edits": [
      {
        "file": "lib/desks.mjs",
        "from": "What is not\nin the brief did not reach anybody: the cheapest kinds of agent arrive with none of what you are\nreading now, no workspace memory and no instructions, so a rule that lives anywhere but the text\nyou hand them is one they never see.",
        "to": "The workspace's instructions and its memory reach every agent it\nstarts, so a rule written in either of them is one they have all been given already."
      }
    ]
  },
  {
    "name": "the budget is described to the reader rather than put in the brief",
    "catches": "tells the worker to copy the budget into every brief it writes",
    "edits": [
      {
        "file": "lib/desks.mjs",
        "from": "So copy the four sentences below into every brief you write, word for word, before you send it —\nnot a summary of them, not the gist of them, and never left for the agent to work out.",
        "to": "So keep them in mind whenever you hand work on, and brief accordingly."
      }
    ]
  },
  {
    "name": "print mode leaves the run that serves a seat",
    "catches": "never starts Claude Code in a shape its background reaper is armed for",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    \"--print\",\n    \"--input-format\",",
        "to": "    \"--input-format\","
      }
    ]
  },
  {
    "name": "a fourth way to start Claude Code appears, and it is a session with a terminal",
    "catches": "never starts Claude Code in a shape its background reaper is armed for",
    "edits": [
      {
        "file": "lib/claude.mjs",
        "from": "// Hand the terminal to Claude Code so a person can sign this instance in.",
        "to": "export function pickUpWhereItLeftOff(root, auth) {\n  return spawnSync(\"claude\", [\"--resume\"], { cwd: root, env: environment(root, auth), stdio: \"inherit\" });\n}\n\n// Hand the terminal to Claude Code so a person can sign this instance in."
      }
    ]
  },
  {
    "name": "the walk is pointed somewhere no process is started",
    "catches": "starts Claude Code somewhere, so that what follows is about something",
    "edits": [
      {
        "file": "tests/ovai.test.mjs",
        "from": "const starts = processStarts(repo);",
        "to": "const starts = processStarts(path.join(repo, \"lib\", \"templates\"));"
      }
    ]
  },
  {
    "name": "a shell script learns a shortcut that starts a session itself",
    "catches": "never reaches Claude Code from a shell script, where the shape it runs in cannot be read",
    "edits": [
      {
        "file": "bin/ovai",
        "from": "exec \"$(sh \"${runtime}\" node-path)\" \"${root}/lib/ovai.mjs\" --root \"${root}\" \"$@\"",
        "to": "if [ \"${1:-}\" = \"resume\" ]; then\n    exec claude --resume\nfi\n\nexec \"$(sh \"${runtime}\" node-path)\" \"${root}/lib/ovai.mjs\" --root \"${root}\" \"$@\""
      }
    ]
  },
  {
    "name": "status names the Claude Code home instead of the store",
    "catches": "says where this instance keeps it",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "    [\"store\", path.join(root, STORE_DIRECTORY)],",
        "to": "    [\"store\", path.join(root, \".local\")],"
      }
    ]
  },
  {
    "name": "the arguments stop being readable where the run is started",
    "catches": "says what command and what arguments every process it starts is given",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  const child = spawn(claudeCommand(instance.root), args, {",
        "to": "  const child = spawn(claudeCommand(instance.root), args.slice(), {"
      }
    ]
  },
  {
    "name": "the run is started through a name of its own rather than by the command",
    "catches": "says what command and what arguments every process it starts is given",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  const child = spawn(claudeCommand(instance.root), args, {",
        "to": "  const own = claudeCommand(instance.root);\n  const child = spawn(own, args, {"
      }
    ]
  },
  {
    "name": "a bare command is the status",
    "catches": "prints the help when given no command",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "    const command = rest[0] ?? \"help\";",
        "to": "    const command = rest[0] ?? \"status\";"
      }
    ]
  },
  {
    "name": "status trusts the record without trying the address",
    "catches": "finds a stale record out by trying",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  const health = await healthAt(record.url);\n  if (health === null || path.resolve(String(health.instance)) !== path.resolve(root)) {\n    return null;\n  }\n  return record;",
        "to": "  return record;"
      }
    ]
  },
  {
    "name": "start does not look for a server already running",
    "catches": "does not start a second server over the first",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  const running = await serving(root);\n  if (running !== null) {\n    console.log(`already running at ${running.url}`);\n    return;\n  }\n\n  // Appended, never written over",
        "to": "  // Appended, never written over"
      }
    ]
  },
  {
    "name": "status exits 0 while nothing runs",
    "catches": "says not running, and exits 3, while nothing runs",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "const NOT_RUNNING = 3;",
        "to": "const NOT_RUNNING = 0;"
      }
    ]
  },
  {
    "name": "the version is read from under lib twice over, where nothing is",
    "catches": "says which version this instance is on",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "    [\"version\", version(root) ?? ",
        "to": "    [\"version\", version(path.join(root, \"lib\")) ?? "
      }
    ]
  },
  {
    "name": "the log is written over at every start",
    "catches": "keeps the replaced run's account in the log under the new run's started row",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  const log = fs.openSync(where, \"a\");",
        "to": "  const log = fs.openSync(where, \"w\");"
      }
    ]
  },
  {
    "name": "the started row names no process",
    "catches": "begins the run with the started row: the moment with its offset, the pid the server recorded, and nothing of the start's",
    "edits": [
      {
        "file": "lib/chat/log.mjs",
        "from": "pid ${process.pid} host",
        "to": "host"
      }
    ]
  },
  {
    "name": "the start writes a header of its own into the log",
    "catches": "begins the run with the started row: the moment with its offset, the pid the server recorded, and nothing of the start's",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "  fs.closeSync(log);\n  child.unref();",
        "to": "  fs.writeSync(log, `--- ovai start: server pid ${child.pid}\\n`);\n  fs.closeSync(log);\n  child.unref();"
      }
    ]
  },
  {
    "name": "the moment of a row has no milliseconds",
    "catches": "begins the run with the started row: the moment with its offset, the pid the server recorded, and nothing of the start's",
    "edits": [
      {
        "file": "lib/chat/log.mjs",
        "from": "  return `${whole.slice(0, 19)}.${String(date.getMilliseconds()).padStart(3, \"0\")}${whole.slice(19)}`;",
        "to": "  return whole;"
      }
    ]
  },
  {
    "name": "a failed start quotes the whole log rather than this run's part of it",
    "catches": "quotes only this run's output when the server exits before it is up",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "fs.readFileSync(where).subarray(from)",
        "to": "fs.readFileSync(where).subarray(0)"
      }
    ]
  },
  {
    "name": "the README names a model",
    "catches": "names no model of its own",
    "edits": [
      {
        "file": "README.md",
        "from": "## Requirements\n",
        "to": "## Requirements\n\nThe Leader runs on Opus.\n"
      }
    ]
  },
  {
    "name": "the instance command runs on whatever node the PATH has",
    "catches": "runs the instance command on the toolkit's own node, by path",
    "edits": [
      {
        "file": "bin/ovai",
        "from": "exec \"$(sh \"${runtime}\" node-path)\" \"${root}/lib/ovai.mjs\"",
        "to": "exec node \"${root}/lib/ovai.mjs\""
      }
    ]
  },
  {
    "name": "the launcher goes on when the runtime could not be fetched, saying nothing of its own",
    "catches": "refuses to run when the runtime is not there and cannot be fetched",
    "edits": [
      {
        "file": "bin/ovai",
        "from": "sh \"${runtime}\" ensure || die \"the runtime could not be fetched, so nothing can run\"",
        "to": "sh \"${runtime}\" ensure || true"
      }
    ]
  },
  {
    "name": "the server is started on whatever node the PATH has",
    "catches": "starts the server on the toolkit's own node, by path",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "const child = spawn(nodeCommand(root), [SERVER, \"--root\", root], {",
        "to": "const child = spawn(\"node\", [SERVER, \"--root\", root], {"
      }
    ]
  },
  {
    "name": "the credential is asked of whatever claude the PATH has",
    "catches": "is the toolkit's own claude, by path, and not one on the PATH",
    "edits": [
      {
        "file": "lib/claude.mjs",
        "from": "asked = spawnSync(claudeCommand(root), [\"auth\", \"status\"], {",
        "to": "asked = spawnSync(\"claude\", [\"auth\", \"status\"], {"
      }
    ]
  },
  {
    "name": "Claude Code is started with its background updater on",
    "catches": "runs with its background updater off",
    "edits": [
      {
        "file": "lib/claude.mjs",
        "from": "  env.DISABLE_AUTOUPDATER = \"1\";\n",
        "to": ""
      }
    ]
  },
  {
    "name": "configuration says nothing about the runtime",
    "catches": "says which node and claude this instance runs on, and where they are kept",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "    [\"runtime\", describeRuntime(root)],\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the sign-in refusal sends the person to a claude on the PATH to mint a token",
    "catches": "is told where a token comes from instead, with the command that mints one by path",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "not with an account of its own — mint a token with: ${runtimePaths(root).claude} setup-token`,",
        "to": "not with an account of its own — mint a token with: claude setup-token`,"
      }
    ]
  },
  {
    "name": "configuration sends the person to a claude on the PATH to mint a token",
    "catches": "says the same in configuration while no token is set",
    "edits": [
      {
        "file": "lib/ovai.mjs",
        "from": "      : `CLAUDE_CODE_OAUTH_TOKEN, which is not set here — mint one with: ${runtimePaths(root).claude} setup-token`,",
        "to": "      : \"CLAUDE_CODE_OAUTH_TOKEN, which is not set here — mint one with: claude setup-token\","
      }
    ]
  }
]

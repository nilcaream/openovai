[
  {
    "name": "the release stops waiting for the checks",
    "catches": "runs the checks before anything is tagged",
    "edits": [
      {
        "file": ".github/workflows/release.yml",
        "from": "    name: Release\n    needs: tests\n",
        "to": "    name: Release\n"
      }
    ]
  },
  {
    "name": "CI is left with nothing another workflow can call",
    "catches": "leaves CI something a release can call",
    "edits": [
      {
        "file": ".github/workflows/ci.yml",
        "from": "  workflow_call:\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the version is read from the root, where it no longer is",
    "catches": "refuses a tree with no version in it",
    "edits": [
      {
        "file": "lib/version.mjs",
        "from": "export const VERSION_FILE = path.join(\"lib\", \"VERSION\");",
        "to": "export const VERSION_FILE = \"VERSION\";"
      }
    ]
  }
]

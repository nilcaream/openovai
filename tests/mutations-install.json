[
  {
    "name": "a fresh instance leaves a refused session waiting on the limit dialog",
    "catches": "brings a refused session back by itself rather than leaving it on a dialog",
    "edits": [
      {
        "file": "tools/seed.mjs",
        "from": "  autoContinueAtUsageLimit: true,",
        "to": "  autoContinueAtUsageLimit: false,"
      }
    ]
  },
  {
    "name": "the settings are written but left out of what the installer says it wrote",
    "catches": "says it wrote those settings",
    "edits": [
      {
        "file": "tools/seed.mjs",
        "from": "    ...seed(homeSettingsFile(root), () => asJson(HOME_SETTINGS)),",
        "to": "    ...seed(homeSettingsFile(root), () => asJson(HOME_SETTINGS)).slice(1),"
      }
    ]
  },
  {
    "name": "the lead's persona is written without the delegation budget",
    "catches": "tells the lead how much a brief it writes may spend",
    "edits": [
      {
        "file": "templates/leader.md",
        "from": "{{BUDGET}}\n\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the skill is left out of what the toolkit ships",
    "catches": "places the skill an instance explains itself with where a session reads it",
    "edits": [
      {
        "file": "tools/payload.mjs",
        "from": "export const PAYLOAD = [\"bin\", \"tools\", \"templates\", SKILL, VERSION_FILE];",
        "to": "export const PAYLOAD = [\"bin\", \"tools\", \"templates\", VERSION_FILE];"
      }
    ]
  },
  {
    "name": "the lead's persona says nothing about where the answer comes from",
    "catches": "tells the leader to answer what the workspace allows from the skill, never from memory",
    "edits": [
      {
        "file": "templates/leader.md",
        "from": "And when anyone asks what this workspace allows — on that first turn or a month later — run the\n`allowed` skill and report what it answers. Never answer that question from memory:",
        "to": "And when anyone asks what this workspace allows — on that first turn or a month later — tell them\nwhat you know of it:"
      }
    ]
  },
  {
    "name": "the skill goes by a name nothing sends a session to",
    "catches": "is found under the name the workspace sends a session to",
    "edits": [
      {
        "file": ".claude/skills/allowed/SKILL.md",
        "from": "name: allowed",
        "to": "name: permissions"
      }
    ]
  },
  {
    "name": "the report loses the block that is not about the rules at all",
    "catches": "lays down seven blocks and their order",
    "edits": [
      {
        "file": ".claude/skills/allowed/SKILL.md",
        "from": "### E. The other mechanism: the working directory",
        "to": "### The other mechanism: the working directory"
      }
    ]
  },
  {
    "name": "the trust check reads as the runtime speaking rather than as our inference",
    "catches": "makes the trust check admit it is a reconstruction",
    "edits": [
      {
        "file": ".claude/skills/allowed/SKILL.md",
        "from": "**Say plainly, in the report, that this check is our reconstruction and not the runtime speaking,\nand mark it Assumes.**",
        "to": "**Say what this check found.**"
      }
    ]
  },
  {
    "name": "the denied block stops naming the shell rule that reaches what a deny protects",
    "catches": "has the denied block cross its paths against the shell rules that reach them",
    "edits": [
      {
        "file": ".claude/skills/allowed/SKILL.md",
        "from": "name the pair in one line and do not soften it: *\"`Bash(sed:*)` reaches the paths\n`Edit(.claude/**)` protects\"*",
        "to": "say so"
      }
    ]
  },
  {
    "name": "the report claims a write will go through on the strength of a rule",
    "catches": "names the working directory as a refusal with no rule in it",
    "edits": [
      {
        "file": ".claude/skills/allowed/SKILL.md",
        "from": "**this report may not claim a write will be allowed on rule evidence alone.**",
        "to": "**this report says whether a write will be allowed.**"
      }
    ]
  },
  {
    "name": "the calls that never stop are reported as beyond forbidding too",
    "catches": "says the calls that never stop are out of reach of allow and not of deny",
    "edits": [
      {
        "file": ".claude/skills/allowed/SKILL.md",
        "from": "This class is out of reach of\n`allow`; it is **not** out of reach of `deny`.",
        "to": "This class is out of reach of\nthe permission rules altogether."
      }
    ]
  },
  {
    "name": "the report is allowed to suggest rules worth pressing next",
    "catches": "names no rule the person could press next",
    "edits": [
      {
        "file": ".claude/skills/allowed/SKILL.md",
        "from": "It does not name rules the person could usefully press next. It is a report, not advice, and a\nlead suggesting rules is a lead improvising grants.",
        "to": "It names the rules the person could usefully press next."
      }
    ]
  },
  {
    "name": "the refusals are written without the rules a fresh instance grants",
    "catches": "keeps what it grants while writing what it refuses",
    "edits": [
      {
        "file": "tools/seed.mjs",
        "from": "      allow: [deskRule(leader), ...TOOL_RULES],\n      deny: OWN_ACCOUNT_RULES,",
        "to": "      deny: OWN_ACCOUNT_RULES,"
      }
    ]
  },
  {
    "name": "the trust record is left reachable by the tools that write files",
    "catches": "closes its own account of what it allows to the tools that write files",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "  \"Edit(.claude-home/settings.json)\",\n  \"Edit(.claude-home/.claude.json)\",",
        "to": "  \"Edit(.claude-home/settings.json)\","
      }
    ]
  },
  {
    "name": "what the instance remembers is refused along with what it allows",
    "catches": "leaves what the instance remembers alone",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "  \"Edit(.claude-home/.claude.json)\",\n];",
        "to": "  \"Edit(.claude-home/.claude.json)\",\n  \"Edit(.claude-home/projects/**)\",\n];"
      }
    ]
  },
  {
    "name": "a fresh instance refuses writes to everybody's work as well",
    "catches": "refuses nothing about anybody's work",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "export const OWN_ACCOUNT_RULES = [\n  \"Edit(.claude/**)\",",
        "to": "export const OWN_ACCOUNT_RULES = [\n  \"Edit(work/**)\",\n  \"Edit(.claude/**)\","
      }
    ]
  },
  {
    "name": "the refusals are written into the ledger as though somebody had asked for them",
    "catches": "accounts for none of them, since a refusal is not something somebody asked for",
    "edits": [
      {
        "file": "tools/seed.mjs",
        "from": "import { DESK_TEMPLATE, OWN_ACCOUNT_RULES, TOOL_RULES, deskFile, deskRule, readTemplate, render } from \"./desks.mjs\";",
        "to": "import { DESK_TEMPLATE, OWN_ACCOUNT_RULES, TOOL_RULES, account, deskFile, deskRule, readTemplate, render } from \"./desks.mjs\";"
      },
      {
        "file": "tools/seed.mjs",
        "from": "    ...seed(settingsFile(root), () => asJson(settingsToStartWith(leader))),",
        "to": "    ...seed(settingsFile(root), () => asJson(settingsToStartWith(leader))),\n    ...OWN_ACCOUNT_RULES.flatMap((rule) => account(root, { rule, session: \"the installer\", call: rule, day: \"2026-01-01\" })),"
      }
    ]
  },
  {
    "name": "a file the person has is written over as though it were missing",
    "catches": "keeps .claude/settings.json as the person left it",
    "edits": [
      {
        "file": "tools/seed.mjs",
        "from": "  if (fs.existsSync(target)) {\n    return [];\n  }\n  fs.mkdirSync(path.dirname(target), { recursive: true });",
        "to": "  fs.mkdirSync(path.dirname(target), { recursive: true });"
      }
    ]
  },
  {
    "name": "the payload is written over what is there rather than replacing it",
    "catches": "replaces the payload whole, taking away what the source has not got",
    "edits": [
      {
        "file": "tools/release.mjs",
        "from": "    fs.rmSync(target, { recursive: true, force: true });\n    fs.renameSync(beside, target);",
        "to": "    fs.cpSync(beside, target, { recursive: true });\n    fs.rmSync(beside, { recursive: true, force: true });"
      }
    ]
  },
  {
    "name": "a file the person has is named as written",
    "catches": "names what it seeded and what it replaced, and nothing it kept",
    "edits": [
      {
        "file": "tools/seed.mjs",
        "from": "  if (fs.existsSync(target)) {\n    return [];\n  }",
        "to": "  if (fs.existsSync(target)) {\n    return [target];\n  }"
      }
    ]
  },
  {
    "name": "the installer makes a directory for personas again",
    "catches": "writes no persona, and makes no directory for one",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "const LAYOUT = [\"work\", \".claude\", \".claude-home\", PLUGINS];",
        "to": "const LAYOUT = [\"work\", \"personas\", \".claude\", \".claude-home\", PLUGINS];"
      }
    ]
  },
  {
    "name": "installing over an instance takes away what an older toolkit rendered",
    "catches": "leaves what an older toolkit rendered under personas/ exactly as it found it",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "    report(plan, [\n      ...createLayout(plan),",
        "to": "    fs.rmSync(path.join(plan.root, \"personas\"), { recursive: true, force: true });\n    report(plan, [\n      ...createLayout(plan),"
      }
    ]
  },
  {
    "name": "what the person added to a persona is dropped",
    "catches": "comes after everything the toolkit puts in, the budget included",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "  const added = customization(root, what);\n  return added === null ? rendered",
        "to": "  const added = null;\n  return added === null ? rendered"
      }
    ]
  },
  {
    "name": "what the person added is put in front of the template rather than after it",
    "catches": "comes after everything the toolkit puts in, the budget included",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "`${rendered.replace(/\\s*$/, \"\")}\\n\\n${added.replace(/\\s*$/, \"\")}\\n`",
        "to": "`${added.replace(/\\s*$/, \"\")}\\n\\n${rendered.replace(/\\s*$/, \"\")}\\n`"
      }
    ]
  },
  {
    "name": "what the person added is read as a template and refused for its braces",
    "catches": "is read as it is, braces and all",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "  const added = customization(root, what);",
        "to": "  const raw = customization(root, what);\n  const added = raw === null ? null : render(what, raw, {});"
      }
    ]
  },
  {
    "name": "the lead is given the addition written for everybody else",
    "catches": "comes after everything the toolkit puts in, the budget included",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "  const added = customization(root, what);",
        "to": "  const added = customization(root, \"worker\");"
      }
    ]
  },
  {
    "name": "an answer given over an instance is dropped because the description already had one",
    "catches": "writes the answers given into the description the instance has, and keeps the rest of it",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "    for (const key of plan.given) {",
        "to": "    for (const key of []) {"
      }
    ]
  },
  {
    "name": "the person's own additions to the description go when an answer is written in",
    "catches": "writes the answers given into the description the instance has, and keeps the rest of it",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "    config = { ...plan.existing, models: { ...plan.existing.models } };",
        "to": "    config = { schema: plan.existing.schema, createdAt: plan.existing.createdAt, models: { ...plan.existing.models } };"
      }
    ]
  },
  {
    "name": "an answer left off the command line is required even over an instance that has it",
    "catches": "takes an answer left off the command line from the description the instance has",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "    if (parsed[key] === undefined && existing !== null && IN_CONFIG[key] !== undefined) {",
        "to": "    if (false) {"
      }
    ]
  },
  {
    "name": "the description is named as written when nothing in it changed",
    "catches": "takes an answer left off the command line from the description the instance has",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "    if (JSON.stringify(config) === JSON.stringify(plan.existing)) {\n      return [];\n    }",
        "to": "    if (JSON.stringify(config) === JSON.stringify(plan.existing)) {\n      return [target];\n    }"
      }
    ]
  },
  {
    "name": "the skill says an edit to it lasts for ever",
    "catches": "says it is the toolkit's and an edit to it lasts until the next update",
    "edits": [
      {
        "file": ".claude/skills/allowed/SKILL.md",
        "from": "An edit made to it here lasts\nuntil the next update and no longer:",
        "to": "An edit made to it here is\nyours to keep:"
      }
    ]
  },
  {
    "name": "only the block is told what a trip has to do, and not the persona a lead runs",
    "catches": "tells the leader that a trip only counts when the call stops",
    "edits": [
      {
        "file": "templates/leader.md",
        "from": "A trip only counts when it stops, and you cannot see whether it did. Much of what you could run is\nsettled by the frame itself — a status or a listing reads and returns, and nothing parks — so reach\nfor the form of their command that parks rather than the form that reads. A call coming back tells\nyou nothing either way: it returns the same whether the frame let it through or whether they pressed\nthe rule, you are not shown their panel, and no press is reported back to you. What was granted is a\nfile: every rule this workspace holds beyond a desk is one line in `.claude/allowed.md`, written in\nthe same act as the rule and saying who asked and what for. Read it when the calls are made and\nreport what is on its lines, and never tell them what did or did not stop.",
        "to": "A trip is a trip."
      }
    ]
  },
  {
    "name": "only the block is told where what was granted is written, and not the persona a lead runs",
    "catches": "tells the leader the panel is not shown to it and the ledger is the record",
    "edits": [
      {
        "file": "templates/leader.md",
        "from": "What was granted is a\nfile: every rule this workspace holds beyond a desk is one line in `.claude/allowed.md`, written in\nthe same act as the rule and saying who asked and what for. Read it when the calls are made and\nreport what is on its lines, and never tell them what did or did not stop.",
        "to": "Report what happened."
      }
    ]
  }
]

[
  {
    "name": "a fresh instance leaves a refused session waiting on the limit dialog",
    "catches": "brings a refused session back by itself rather than leaving it on a dialog",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "  autoContinueAtUsageLimit: true,",
        "to": "  autoContinueAtUsageLimit: false,"
      }
    ]
  },
  {
    "name": "the settings are written but left out of what the installer says it wrote",
    "catches": "says it wrote those settings",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "  fs.writeFileSync(target, `${JSON.stringify(HOME_SETTINGS, null, 2)}\\n`);\n  return [target];",
        "to": "  fs.writeFileSync(target, `${JSON.stringify(HOME_SETTINGS, null, 2)}\\n`);\n  return [];"
      }
    ]
  },
  {
    "name": "the lead's persona is written without the delegation budget",
    "catches": "tells the lead how much a brief it writes may spend",
    "edits": [
      {
        "file": "templates/leader.md",
        "from": "{{BUDGET}}\n\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the payload is copied without the skills under it",
    "catches": "copies the skill an instance explains itself with in",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "    fs.cpSync(path.join(plan.source, entry), target, { recursive: true });",
        "to": "    fs.cpSync(path.join(plan.source, entry), target, { recursive: true, filter: (from) => !from.includes(\"skills\") });"
      }
    ]
  },
  {
    "name": "the installer writes the skill directory itself, where no update will ever correct it",
    "catches": "leaves the writing of it to the chat rather than doing it here",
    "edits": [
      {
        "file": "tools/install.mjs",
        "from": "      ...allowTools(plan.root),",
        "to": "      ...allowTools(plan.root),\n      ...[fs.mkdirSync(path.join(plan.root, \".claude\", \"skills\"), { recursive: true })].slice(1),"
      }
    ]
  },
  {
    "name": "the lead's persona says nothing about where the answer comes from",
    "catches": "tells the leader to answer what the workspace allows from the skill, never from memory",
    "edits": [
      {
        "file": "templates/leader.md",
        "from": "And when anyone asks what this workspace allows — on that first turn or a month later — run the\n`allowed` skill and report what it answers. Never answer that question from memory:",
        "to": "And when anyone asks what this workspace allows — on that first turn or a month later — tell them\nwhat you know of it:"
      }
    ]
  },
  {
    "name": "the skill goes by a name nothing sends a session to",
    "catches": "is found under the name the workspace sends a session to",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "name: allowed",
        "to": "name: permissions"
      }
    ]
  },
  {
    "name": "the report loses the block that is not about the rules at all",
    "catches": "lays down seven blocks and their order",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "### E. The other mechanism: the working directory",
        "to": "### The other mechanism: the working directory"
      }
    ]
  },
  {
    "name": "the trust check reads as the runtime speaking rather than as our inference",
    "catches": "makes the trust check admit it is a reconstruction",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "**Say plainly, in the report, that this check is our reconstruction and not the runtime speaking,\nand mark it Assumes.**",
        "to": "**Say what this check found.**"
      }
    ]
  },
  {
    "name": "the denied block stops naming the shell rule that reaches what a deny protects",
    "catches": "has the denied block cross its paths against the shell rules that reach them",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "name the pair in one line and do not soften it: *\"`Bash(sed:*)` reaches the paths\n`Edit(.claude/**)` protects\"*",
        "to": "say so"
      }
    ]
  },
  {
    "name": "the report claims a write will go through on the strength of a rule",
    "catches": "names the working directory as a refusal with no rule in it",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "**this report may not claim a write will be allowed on rule evidence alone.**",
        "to": "**this report says whether a write will be allowed.**"
      }
    ]
  },
  {
    "name": "the calls that never stop are reported as beyond forbidding too",
    "catches": "says the calls that never stop are out of reach of allow and not of deny",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "This class is out of reach of\n`allow`; it is **not** out of reach of `deny`.",
        "to": "This class is out of reach of\nthe permission rules altogether."
      }
    ]
  },
  {
    "name": "the report is allowed to suggest rules worth pressing next",
    "catches": "names no rule the person could press next",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "It does not name rules the person could usefully press next. It is a report, not advice, and a\nlead suggesting rules is a lead improvising grants.",
        "to": "It names the rules the person could usefully press next."
      }
    ]
  }
]

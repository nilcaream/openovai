[
  {
    "name": "every band is raised past anything a fixture reports",
    "catches": "names a conversation that has entered a band",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  Object.freeze({ named: \"0.95\", above: 0.95, strong: true }),",
        "to": "  Object.freeze({ named: \"0.95\", above: 0.99, strong: true }),"
      },
      {
        "file": "tools/chat/session.mjs",
        "from": "  Object.freeze({ named: \"0.90\", above: 0.9, strong: true }),",
        "to": "  Object.freeze({ named: \"0.90\", above: 0.97, strong: true }),"
      },
      {
        "file": "tools/chat/session.mjs",
        "from": "  Object.freeze({ named: \"0.85\", above: 0.85, strong: false }),",
        "to": "  Object.freeze({ named: \"0.85\", above: 0.95, strong: false }),"
      },
      {
        "file": "tools/chat/session.mjs",
        "from": "  Object.freeze({ named: \"0.80\", above: 0.8, strong: false }),",
        "to": "  Object.freeze({ named: \"0.80\", above: 0.93, strong: false }),"
      }
    ]
  },
  {
    "name": "the block is built whatever the list came to",
    "catches": "says nothing while every conversation is under it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (large.length === 0) {\n    return null;\n  }",
        "to": "  if (large.length === -1) {\n    return null;\n  }"
      }
    ]
  },
  {
    "name": "the reading is the top of the frame, which adds a whole turn together",
    "catches": "says the size at the end of the last turn and not what the turn added up to",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  const last = answer.usage?.iterations?.at(-1);",
        "to": "  const last = answer.usage;"
      }
    ]
  },
  {
    "name": "the block is handed to everybody",
    "catches": "is handed to the session that leads and to nobody else",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (name !== instance.config.leader) {\n    return null;\n  }\n  const large = sessions(instance).filter((session) => bandIn(instance.root, session.name) !== null);",
        "to": "  if (name !== instance.config.leader && false) {\n    return null;\n  }\n  const large = sessions(instance).filter((session) => bandIn(instance.root, session.name) !== null);"
      }
    ]
  },
  {
    "name": "the reader is left out of its own list",
    "catches": "names the session that leads about its own conversation",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const large = sessions(instance).filter((session) => bandIn(instance.root, session.name) !== null);",
        "to": "  const large = sessions(instance).filter((session) => session.name !== name && bandIn(instance.root, session.name) !== null);"
      }
    ]
  },
  {
    "name": "a session in the middle of a turn is left out, the way the block about who has stopped rightly leaves it",
    "catches": "names a session that is in the middle of a turn",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const large = sessions(instance).filter((session) => bandIn(instance.root, session.name) !== null);",
        "to": "  const large = sessions(instance).filter((session) => turnsGoing(session.name) === 0 && bandIn(instance.root, session.name) !== null);"
      }
    ]
  },
  {
    "name": "a session with no conversation at all is read as the largest there is",
    "catches": "says nothing about a session with no conversation to carry on",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return shareOf(contextIn(root, name), windowIn(root, name));",
        "to": "  return shareOf(contextIn(root, name), windowIn(root, name)) ?? BANDS[0];"
      }
    ]
  },
  {
    "name": "a reading nobody has taken is read as the largest there is",
    "catches": "says nothing about a session that has never reported a reading",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": ".context;\n    return typeof held === \"number\" ? held : null;\n  } catch {\n    return null;\n  }",
        "to": ".context;\n    return typeof held === \"number\" ? held : Number.MAX_SAFE_INTEGER;\n  } catch {\n    return null;\n  }"
      }
    ]
  },
  {
    "name": "the block says no moment",
    "catches": "carries the moment the turn began",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "`${each.join(\", and \")}, read as this turn began at ${read}.`",
        "to": "`${each.join(\", and \")}.`"
      }
    ]
  },
  {
    "name": "the block does not say who is speaking",
    "catches": "says who is speaking",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    \"<size>\",\n    \"The chat is telling you this. Nobody typed it.\",\n",
        "to": "    \"<size>\",\n"
      }
    ]
  },
  {
    "name": "a thread that has been handed over keeps its reading",
    "catches": "is gone the turn after that session has been handed over",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "export function forget(root, name) {\n  fs.rmSync(sessionFile(root, name), { force: true });\n}",
        "to": "export function forget(root, name) {\n  try {\n    const held = JSON.parse(fs.readFileSync(sessionFile(root, name), \"utf8\"));\n    fs.writeFileSync(sessionFile(root, name), `${JSON.stringify({ ...held, sessionId: null }, null, 2)}\\n`);\n  } catch {\n    fs.rmSync(sessionFile(root, name), { force: true });\n  }\n}"
      }
    ]
  },
  {
    "name": "a message to a conversation over the line is held back",
    "catches": "a message to a session over the line is delivered exactly as any other",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "async function deliver(instance, name, text, signed, shown = null) {\n  if (typeof text !== \"string\" || text.trim() === \"\") {\n    return { status: 400, body: { error: \"a message needs some text\" } };\n  }",
        "to": "async function deliver(instance, name, text, signed, shown = null) {\n  if (typeof text !== \"string\" || text.trim() === \"\") {\n    return { status: 400, body: { error: \"a message needs some text\" } };\n  }\n  if (bandIn(instance.root, name) !== null) {\n    return { status: 409, body: { error: \"that conversation is too big to take a message\" } };\n  }"
      }
    ]
  },
  {
    "name": "handing over is refused because the conversation is big",
    "catches": "nothing is refused, queued, hired or ended because a conversation is big",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "async function postHandover(instance, name, response) {\n  const done = await handOver(",
        "to": "async function postHandover(instance, name, response) {\n  if (bandIn(instance.root, name) !== null) {\n    sendJson(response, 409, { error: \"that conversation is too big to hand over\" });\n    return;\n  }\n  const done = await handOver("
      }
    ]
  },
  {
    "name": "the line the turn opens with is written inside it rather than handed in",
    "catches": "opens the panel with the words it was given",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      text: askedLine,",
        "to": "      text: handoverAsked(instance.config.human, name),"
      }
    ]
  },
  {
    "name": "the row says what to do about the share as well as saying it",
    "catches": "the room says the share and the tokens, and never a verdict",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "    shareSaid(session.context, session.window),",
        "to": "    shareSaid(session.context, session.window),\n    shareSaid(session.context, session.window) === null ? null : \"large\","
      }
    ]
  },
  {
    "name": "the page's own copy of the row says what to do about the share",
    "catches": "the page says the share and the tokens, and never a verdict",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "      shareSaid(row.context, row.window),",
        "to": "      shareSaid(row.context, row.window),\n      shareSaid(row.context, row.window) === null ? null : \"large\","
      }
    ]
  },
  {
    "name": "the block is said before the one about who has stopped",
    "catches": "it is the block after the quiet one and before the usage one",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const stopped = quietWrapper(instance, name);\n  if (stopped !== null) {\n    said.push(stopped);\n  }\n",
        "to": ""
      },
      {
        "file": "tools/chat/server.mjs",
        "from": "  const large = sizeWrapper(instance, name);\n  if (large !== null) {\n    said.push(large);\n  }\n",
        "to": "  const large = sizeWrapper(instance, name);\n  if (large !== null) {\n    said.push(large);\n  }\n  const stopped = quietWrapper(instance, name);\n  if (stopped !== null) {\n    said.push(stopped);\n  }\n"
      }
    ]
  },
  {
    "name": "the hour fires on the size as well as on the clock",
    "catches": "a conversation is still ended after an hour and not before, whatever it is carrying",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return ran !== null && Date.now() - ran > COLD_AFTER;",
        "to": "  return bandIn(root, name) !== null || (ran !== null && Date.now() - ran > COLD_AFTER);"
      }
    ]
  },
  {
    "name": "the park is drawn at the reading somebody is only told about",
    "catches": "is not yet said of a conversation that has only just gone quiet",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "const PARK_AFTER = (COLD_AFTER / 6) * 5;",
        "to": "const PARK_AFTER = QUIET_AFTER;"
      }
    ]
  },
  {
    "name": "the park is drawn past the hour it exists to get ahead of",
    "catches": "is said of a conversation left long enough that its cache is nearly certainly gone",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "const PARK_AFTER = (COLD_AFTER / 6) * 5;",
        "to": "const PARK_AFTER = (COLD_AFTER / 6) * 7;"
      }
    ]
  },
  {
    "name": "the reading is left open at the top, like the one beside it",
    "catches": "stops being said once the conversation is certainly cold",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return idle > PARK_AFTER && idle <= COLD_AFTER;",
        "to": "  return idle > PARK_AFTER;"
      }
    ]
  },
  {
    "name": "a session with no conversation is read as one that has been left a long time",
    "catches": "is never said of a session that has never answered",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  if (!hasThread(root, name)) {\n    return false;\n  }\n  const ran = ranAt(root, name);\n  if (ran === null) {\n    return false;\n  }\n  const idle = Date.now() - ran;",
        "to": "  const ran = ranAt(root, name);\n  const idle = ran === null ? COLD_AFTER : Date.now() - ran;"
      }
    ]
  },
  {
    "name": "the park is drawn a few minutes late, at a fraction that looks as plausible as the one chosen",
    "catches": "is said of a conversation left long enough that its cache is nearly certainly gone",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "const PARK_AFTER = (COLD_AFTER / 6) * 5;",
        "to": "const PARK_AFTER = (COLD_AFTER / 10) * 9;"
      }
    ]
  },
  {
    "name": "a session is offered a tool that hands somebody over",
    "catches": "serves a lead exactly the tools it says it serves",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    ...(instance.plugins ?? []).map((plugin) => ({",
        "to": "    {\n      name: \"handover\",\n      description: \"Hand another session over.\",\n      inputSchema: { type: \"object\", properties: { who: { type: \"string\" } }, required: [\"who\"] },\n      run: async () => answerFrom(\"handover\", \"done\"),\n    },\n    ...(instance.plugins ?? []).map((plugin) => ({"
      }
    ]
  },
  {
    "name": "being big is made a state the room can be in",
    "catches": "reaches every state the room can say",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "  {\n    named: \"idle\",\n    when: () => true,\n    say: (session) => idleSaid(session.ran),\n  },\n]);",
        "to": "  {\n    named: \"large\",\n    when: (session) => typeof session.context === \"number\" && session.context > 300000,\n    say: () => \"large\",\n  },\n  {\n    named: \"idle\",\n    when: () => true,\n    say: (session) => idleSaid(session.ran),\n  },\n]);"
      }
    ]
  },
  {
    "name": "the roster takes any directory under work/, whatever it is called",
    "catches": "a directory whose name nobody could have is not on the roster",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "entry.isDirectory() && isName(entry.name)",
        "to": "entry.isDirectory()"
      }
    ]
  },
  {
    "name": "the roster keeps the directories nobody could be called and drops the people",
    "catches": "a directory whose name is fine is still on the roster",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "entry.isDirectory() && isName(entry.name)",
        "to": "entry.isDirectory() && !isName(entry.name)"
      }
    ]
  },
  {
    "name": "the roster takes any directory, and the route asks the roster",
    "catches": "the chat will not open a conversation for a directory that is not a person",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "entry.isDirectory() && isName(entry.name)",
        "to": "entry.isDirectory()"
      }
    ]
  },
  {
    "name": "the window is taken off the first model the frame named",
    "catches": "reads the window off the model the run answered on, whatever answered beside it",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  const size = named[ranOn]?.contextWindow;",
        "to": "  const size = Object.values(named)[0]?.contextWindow;"
      }
    ]
  },
  {
    "name": "the window falls back to the one number anybody measured",
    "catches": "says nothing about the share when the window is not a number",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return typeof size === \"number\" && size > 0 ? size : null;",
        "to": "  return typeof size === \"number\" && size > 0 ? size : 200_000;"
      }
    ]
  },
  {
    "name": "an empty modelUsage is read as no modelUsage",
    "catches": "says nothing about the share when the frame named no model",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  const size = named[ranOn]?.contextWindow;",
        "to": "  if (Object.keys(named).length === 0) {\n    return 200_000;\n  }\n  const size = named[ranOn]?.contextWindow;"
      }
    ]
  },
  {
    "name": "a missing window is divided by all the same",
    "catches": "says nothing about a session with no reading",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  if (typeof context !== \"number\" || typeof window !== \"number\" || window <= 0) {\n    return null;\n  }\n  const share = context / window;",
        "to": "  if (typeof context !== \"number\") {\n    return null;\n  }\n  const share = context / window;"
      }
    ]
  },
  {
    "name": "the share is said instead of the tokens",
    "catches": "the room says the share beside the tokens and not instead of them",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "    typeof session.context === \"number\" ? `${session.context.toLocaleString(\"en-US\")} tokens` : null,\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the share is said on a row whose session was told no window",
    "catches": "the room says the tokens alone when the frame named no window",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "  if (typeof context !== \"number\" || typeof window !== \"number\" || window <= 0) {\n    return null;\n  }\n  return `${Math.round((context / window) * 100)}% of its window`;",
        "to": "  if (typeof context !== \"number\") {\n    return null;\n  }\n  return `${Math.round((context / (window ?? 200_000)) * 100)}% of its window`;"
      }
    ]
  },
  {
    "name": "the block says the size and not the share",
    "catches": "says the share of the window and not only the tokens",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    const held = `${session.context.toLocaleString(\"en-US\")} tokens, ${shareSaid(session.context, session.window)}`;",
        "to": "    const held = `${session.context.toLocaleString(\"en-US\")} tokens`;"
      }
    ]
  },
  {
    "name": "a band is made a state the room can be in",
    "catches": "reaches every state the room can say",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "  {\n    named: \"cold\",\n    when: (session) => session.cold,\n    say: () => \"cold\",\n  },",
        "to": "  {\n    named: \"cold\",\n    when: (session) => session.cold,\n    say: () => \"cold\",\n  },\n  {\n    named: \"big\",\n    when: (session) => shareSaid(session.context, session.window) !== null,\n    say: () => \"big\",\n  },"
      }
    ]
  },
  {
    "name": "the pass says a band rather than having entered it",
    "catches": "says one thing for one crossing and not one every pass",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "    if (band !== null && band.strong && named !== (seen.get(session.name) ?? null)) {",
        "to": "    if (band !== null && band.strong) {"
      }
    ]
  },
  {
    "name": "the pass says a band that is only worth reading",
    "catches": "says nothing about a band that is only worth reading",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "    if (band !== null && band.strong && named !== (seen.get(session.name) ?? null)) {",
        "to": "    if (band !== null && named !== (seen.get(session.name) ?? null)) {"
      }
    ]
  },
  {
    "name": "the cadence is read from the constant and the instance's field ignored",
    "catches": "reads the room as often as the instance says to",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  return said * 1000;",
        "to": "  return EVERY;"
      }
    ]
  },
  {
    "name": "a cadence of zero is read as never rather than as no turn",
    "catches": "reads the room at the usual pace when the instance buys no turn",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  if (typeof said !== \"number\" || said === 0) {\n    return EVERY;\n  }",
        "to": "  if (typeof said !== \"number\") {\n    return EVERY;\n  }\n  if (said === 0) {\n    return null;\n  }"
      }
    ]
  },
  {
    "name": "a cadence in milliseconds is accepted",
    "catches": "refuses a cadence that is not a whole number of seconds, naming the field",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  if (typeof seconds !== \"number\" || !Number.isInteger(seconds) || seconds < 0) {",
        "to": "  if (false) {"
      }
    ]
  },
  {
    "name": "a band that another band shadows",
    "catches": "reaches every band the reading can say",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  Object.freeze({ named: \"0.85\", above: 0.85, strong: false }),",
        "to": "  Object.freeze({ named: \"0.85\", above: 0.9, strong: false }),"
      }
    ]
  },
  {
    "name": "the interval is not cleared where the server closes",
    "catches": "the chat can be stopped",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    clearInterval(watch);\n    forgetTheRoom();",
        "to": "    forgetTheRoom();"
      }
    ]
  },
  {
    "name": "the window is taken off whatever single model the frame named",
    "catches": "says nothing about the share when the frame named no model the run was on",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  const size = named[ranOn]?.contextWindow;",
        "to": "  const size = Object.values(named).at(-1)?.contextWindow;"
      }
    ]
  },
  {
    "name": "the window is looked up by the model the workspace asked for",
    "catches": "reads the window off the model the run said it was on, whatever it is called",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "      resolve(interpret(answer, err, limit, reading, ranOn));",
        "to": "      resolve(interpret(answer, err, limit, reading, model(instance, name)));"
      }
    ]
  },
  {
    "name": "the end of the line is cut off instead of the directories in it",
    "catches": "keeps the name of the script however deep the instance sits",
    "edits": [
      {
        "file": "tools/port.mjs",
        "from": "  const shortened = command\n    .split(\" \")\n    .map((word) => word.slice(word.lastIndexOf(\"/\") + 1) || word)\n    .join(\" \");",
        "to": "  const shortened = command;"
      }
    ]
  },
  {
    "name": "the rule composed for a write names the tool that asked rather than the one that governs writing",
    "catches": "composes the rule that governs writing, and never one that matches nothing",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  return `Edit(${subtree})`;",
        "to": "  return `Write(${subtree})`;"
      }
    ]
  },
  {
    "name": "the rule composed for a write is the path it was handed rather than the directory that path is in",
    "catches": "composes a rule for the directory the write was in",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  const held = path.posix.dirname(named);",
        "to": "  const held = named;"
      }
    ]
  },
  {
    "name": "the rule is spelled as the bare directory, which reads like one level and is a subtree",
    "catches": "composes the spelling that says a subtree out loud",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  const subtree = held === \".\" ? \"**\" : `${held}/**`;",
        "to": "  const subtree = held === \".\" ? \"**\" : held;"
      }
    ]
  },
  {
    "name": "a write at the root composes the dirname it was given, leading ./ and all",
    "catches": "composes a rule at the root with no ./ in front of it",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  const subtree = held === \".\" ? \"**\" : `${held}/**`;",
        "to": "  const subtree = `${held}/**`;"
      }
    ]
  },
  {
    "name": "a write that lands outside the instance composes a rule all the same",
    "catches": "composes no rule for a write outside the instance",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  return named === \"\" || named === \"..\" || named.startsWith(\"../\") || path.isAbsolute(named) ? null : named;",
        "to": "  return named;"
      }
    ]
  },
  {
    "name": "the composed path is the one the request named, which is a place on this machine",
    "catches": "names no place on this machine in the rule it composes",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  const named = path.relative(root, path.resolve(root, where)).split(path.sep).join(\"/\");",
        "to": "  const named = path.resolve(root, where);"
      },
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  return named === \"\" || named === \"..\" || named.startsWith(\"../\") || path.isAbsolute(named) ? null : named;",
        "to": "  return named === \"\" || named === \"..\" || named.startsWith(\"../\") ? null : named;"
      }
    ]
  },
  {
    "name": "a path rule is composed for anything whose input carries a path",
    "catches": "offers a button only where the request says what the class of calls is",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  if (request.tool === \"Write\" || request.tool === \"Edit\") {",
        "to": "  if (typeof request.input?.file_path === \"string\") {"
      }
    ]
  },
  {
    "name": "the tool that writes a file it has not read is left out of the pair",
    "catches": "composes the rule that governs writing, and never one that matches nothing",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  if (request.tool === \"Write\" || request.tool === \"Edit\") {",
        "to": "  if (request.tool === \"Edit\") {"
      }
    ]
  },
  {
    "name": "the tool that edits a file it has read is left out of the pair",
    "catches": "composes a rule for the directory the write was in",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  if (request.tool === \"Write\" || request.tool === \"Edit\") {",
        "to": "  if (request.tool === \"Write\") {"
      }
    ]
  },
  {
    "name": "the rule handed to the page is prettified on its way out",
    "catches": "shows the rule it is about to grant, word for word",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "    const shape = shapeOf(request, root);",
        "to": "    const shape = shapeOf(request, root)?.replace(\"/**\", \" and everything under it\") ?? null;"
      }
    ]
  },
  {
    "name": "the line accounting for a write says nothing about which write it was",
    "catches": "writes down who asked for it, when, and which write it was for",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      call: asked.input?.command ?? inside(asked.input?.file_path, instance.root),",
        "to": "      call: asked.input?.command ?? asked.input?.file_path,"
      }
    ]
  },
  {
    "name": "the question is handed to everybody rather than to the session that leads",
    "catches": "says nothing to a session that is not the one that leads",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "function permissionsWrapper(instance, name) {\n  if (name !== instance.config.leader) {\n    return null;\n  }",
        "to": "function permissionsWrapper(instance, name) {"
      }
    ]
  },
  {
    "name": "the question ignores the conversation and is asked on every turn",
    "catches": "says nothing once the conversation that leads has one turn behind it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (hasThread(instance.root, name)) {\n    return null;\n  }",
        "to": "  if (false) {\n    return null;\n  }"
      }
    ]
  },
  {
    "name": "the question ignores what the workspace has already settled",
    "catches": "says nothing once the workspace has settled something",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (hasSettledAnything(instance.root)) {\n    return null;\n  }",
        "to": "  if (false) {\n    return null;\n  }"
      }
    ]
  },
  {
    "name": "asking is made to grant something as well",
    "catches": "grants nothing by asking",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return [\n    \"<permissions>\",",
        "to": "  allowAsked(instance.root, { rule: \"Edit(**)\", session: name, call: \"asking\", day: \"2026-01-01\" });\n  return [\n    \"<permissions>\","
      }
    ]
  },
  {
    "name": "the question steps around what a write at the root would grant",
    "catches": "says what a write at the root would grant, in the rule the person would press",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    \"A write at the root offers them the widest rule there is, `Edit(**)`. Say so in the same breath rather than stepping around it: a rule at the root covers every per-desk rule this workspace hands out, so from that press onward the one-rule-per-desk narrowness is decorative. Not a refusal — a sentence, so that the press is an informed one.\",",
        "to": "    \"\","
      }
    ]
  },
  {
    "name": "the question is put behind the message rather than in front of it",
    "catches": "puts the question in front of the message rather than after it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return withWhatWasOverheard(said, message);",
        "to": "  const behind = said.filter((line) => line.startsWith(\"<permissions>\"));\n  return [withWhatWasOverheard(said.filter((line) => !line.startsWith(\"<permissions>\")), message), ...behind].join(\"\\n\\n\");"
      }
    ]
  },
  {
    "name": "the question leaves out that a call which never parked settled nothing",
    "catches": "says a trip only counts when the call actually stops",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    \"A TRIP ONLY COUNTS WHEN IT STOPS, and you cannot see whether it did. The frame settles a great deal on its own — `git status` and `ls` read and return, and nothing parks — so reach for the form of their command that parks rather than the form that reads. But a call coming back tells you nothing about which of the two happened: it returns the same whether the frame let it through, whether they pressed Allow for that one call, or whether they pressed the rule. You are not shown their panel and no press is reported back to you.\",",
        "to": "    \"\","
      }
    ]
  },
  {
    "name": "the question says nothing about an answer that allows commands but names none",
    "catches": "says what to do when the answer allows commands but names none",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    \"If their answer allows commands but names none — `no restrictions` is such an answer — ask which command they want tripped, or trip only the write. Improvising one is how a rule gets granted for a command nobody asked for, and that is the first line in a ledger nobody can account for.\",",
        "to": "    \"\","
      }
    ]
  },
  {
    "name": "the question leaves the lead to find something of its own to write",
    "catches": "says the write it trips is inert and never the answer itself",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    \"The write is the trip and not the record. Put something inert at the path — a placeholder saying what the file is for — and never what they said: nothing here writes their answer down, and neither do you.\",",
        "to": "    \"\","
      }
    ]
  },
  {
    "name": "the question still asks the lead to report what stopped, on a panel it is not shown",
    "catches": "says the panel is not shown to it and that the ledger is what says what was granted",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    \"WHAT WAS GRANTED IS A FILE, and reading it stops nobody. Every rule this workspace holds beyond a desk is one line in `.claude/allowed.md`, written in the same act as the rule itself and saying who asked, when, and what for. So when the calls are made, read it and report what is on its lines: that, and what you did, is the whole of what you know. Never tell them what did or did not stop — a lead reporting a press it cannot see is how a person is told that nothing was granted while the widest rule there is sits in their settings.\",",
        "to": "    \"\","
      }
    ]
  },
  {
    "name": "what a turn carried is cleared whatever the run amounted to",
    "catches": "hands the lead what it overheard again after a turn that was turned away",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (answer.refused === null && answer.failed !== true) {\n    heard(name, carried);\n  }",
        "to": "  heard(name, carried);"
      }
    ]
  },
  {
    "name": "everything waiting is cleared, not only what the turn carried",
    "catches": "keeps a line that arrived while the turn was going",
    "edits": [
      {
        "file": "tools/chat/overheard.mjs",
        "from": "  const left = (pending.get(name) ?? []).slice(lines.length);",
        "to": "  const left = [];"
      }
    ]
  },
  {
    "name": "the skill is written once and never corrected after that",
    "catches": "writes it again at every start, over whatever was there",
    "edits": [
      {
        "file": "tools/skills.mjs",
        "from": "  fs.writeFileSync(target, readTemplate(root, `${SKILL} skill`, SKILL_SOURCE));",
        "to": "  if (!fs.existsSync(target)) {\n    fs.writeFileSync(target, readTemplate(root, `${SKILL} skill`, SKILL_SOURCE));\n  }"
      }
    ]
  },
  {
    "name": "the skill is written where nothing looks for a skill",
    "catches": "writes it where a session reads a skill from",
    "edits": [
      {
        "file": "tools/skills.mjs",
        "from": "export const SKILL_TARGET = path.join(\".claude\", \"skills\", SKILL, SKILL_FILE);",
        "to": "export const SKILL_TARGET = path.join(\".claude\", \"skill\", SKILL, SKILL_FILE);"
      }
    ]
  },
  {
    "name": "the chat writes something of its own into the skill it was shipped",
    "catches": "writes what the payload ships and nothing of its own",
    "edits": [
      {
        "file": "tools/skills.mjs",
        "from": "  fs.writeFileSync(target, readTemplate(root, `${SKILL} skill`, SKILL_SOURCE));",
        "to": "  fs.writeFileSync(target, `<!-- written by the chat -->\\n${readTemplate(root, `${SKILL} skill`, SKILL_SOURCE)}`);"
      }
    ]
  },
  {
    "name": "a chat starts with no skill for its lead to read rather than refusing",
    "catches": "refuses to start at all where the payload has no such skill",
    "edits": [
      {
        "file": "tools/ovai.mjs",
        "from": "  ownSkills(root);",
        "to": "  void root;"
      }
    ]
  },
  {
    "name": "the skill goes by a name nothing sends a session to",
    "catches": "carries the name the rest of the workspace sends a session to",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "name: allowed",
        "to": "name: permissions"
      }
    ]
  },
  {
    "name": "the skill stops saying that the copy is rewritten at every start",
    "catches": "says in the file itself that an edit to the copy lasts until the next start",
    "edits": [
      {
        "file": "templates/skills/allowed/SKILL.md",
        "from": "This file is written by `ovai` and is replaced every time the chat starts.",
        "to": "This file is written by `ovai`."
      }
    ]
  },
  {
    "name": "the first turn describes the workspace instead of reading it",
    "catches": "sends the lead to the skill that reads the files rather than describing the workspace",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "`Run the \\`${ALLOWED}\\` skill first and read ${instance.config.human} what it answers:",
        "to": "`Tell ${instance.config.human} what this workspace allows:"
      }
    ]
  },
  {
    "name": "the first turn goes back to claiming that nothing has been granted here",
    "catches": "asserts nothing itself about what has been granted here",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "`Whatever it comes to, everything not settled by a rule stops a session mid-turn",
        "to": "`It has granted nothing beyond one desk each, so everything not settled by a rule stops a session mid-turn"
      }
    ]
  },
  {
    "name": "where the account stands while a run is going is never said",
    "catches": "says where the account stands before the run has finished",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "          wasTold(name, { windows: windowsIn(reading), at: Date.now() });",
        "to": "          void windowsIn;"
      }
    ]
  },
  {
    "name": "the first reading of a run is kept and every one after it ignored",
    "catches": "keeps the newest reading and not the first",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "          wasTold(name, { windows: windowsIn(reading), at: Date.now() });",
        "to": "          if (standings.get(name)?.windows === null) {\n            wasTold(name, { windows: windowsIn(reading), at: Date.now() });\n          }"
      }
    ]
  },
  {
    "name": "only the first window the reading named is published",
    "catches": "names every window the reading named, as a fraction",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "          wasTold(name, { windows: windowsIn(reading), at: Date.now() });",
        "to": "          wasTold(name, { windows: windowsIn(reading)?.slice(0, 1) ?? null, at: Date.now() });"
      }
    ]
  },
  {
    "name": "the reading is stamped with when the run started rather than when it arrived",
    "catches": "carries the moment the reading arrived",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "          wasTold(name, { windows: windowsIn(reading), at: Date.now() });",
        "to": "          wasTold(name, { windows: windowsIn(reading) });"
      }
    ]
  },
  {
    "name": "the model on the live reading is the seat's rather than the run's own word",
    "catches": "says which model the run itself said it was on",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "          wasTold(name, { ranModel: frame.model });",
        "to": "          wasTold(name, { ranModel: model(instance, name) });"
      }
    ]
  },
  {
    "name": "a run in flight does not say when it began",
    "catches": "says when the run began",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    standings.set(name, { name, ranModel: null, windows: null, at: null, since: Date.now() });",
        "to": "    standings.set(name, { name, ranModel: null, windows: null, at: null, since: null });"
      }
    ]
  },
  {
    "name": "when the run began walks forward with every reading",
    "catches": "says when the run began and not when its last reading arrived",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "          wasTold(name, { windows: windowsIn(reading), at: Date.now() });",
        "to": "          wasTold(name, { windows: windowsIn(reading), at: Date.now(), since: Date.now() });"
      }
    ]
  },
  {
    "name": "a reading outlives the run that was told it",
    "catches": "holds nothing once the run is over",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "function forgetTheStanding(name) {\n  standings.delete(name);\n}",
        "to": "function forgetTheStanding(name) {\n  void name;\n}"
      }
    ]
  },
  {
    "name": "the row says nothing about the run it has going",
    "catches": "says where the account stands while the run is still going",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    standing: standingNow(session.name),",
        "to": "    standing: null,"
      }
    ]
  },
  {
    "name": "the live reading names one window and stops",
    "catches": "names every window the reading named",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return { at: said.at, windows: said.windows, ranModel: said.ranModel, since: said.since };",
        "to": "  return { at: said.at, windows: (said.windows ?? []).slice(0, 1), ranModel: said.ranModel, since: said.since };"
      }
    ]
  },
  {
    "name": "the live reading is served without its age",
    "catches": "carries the age of that reading and not the number alone",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return { at: said.at, windows: said.windows, ranModel: said.ranModel, since: said.since };",
        "to": "  return { at: null, windows: said.windows, ranModel: said.ranModel, since: said.since };"
      }
    ]
  },
  {
    "name": "the live reading cannot say what is spending it",
    "catches": "says which model is spending it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return { at: said.at, windows: said.windows, ranModel: said.ranModel, since: said.since };",
        "to": "  return { at: said.at, windows: said.windows, ranModel: null, since: said.since };"
      }
    ]
  },
  {
    "name": "the live reading cannot say how long the run has been going",
    "catches": "says how long the run has been going, and not when its last reading came",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return { at: said.at, windows: said.windows, ranModel: said.ranModel, since: said.since };",
        "to": "  return { at: said.at, windows: said.windows, ranModel: said.ranModel, since: null };"
      }
    ]
  },
  {
    "name": "the live field falls back to the finished reading once the run is over",
    "catches": "carries it beside what the session was last told and never instead of it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    standing: standingNow(session.name),",
        "to": "    standing: standingNow(session.name) ?? quotaOn(instance.root, session.name),"
      }
    ]
  },
  {
    "name": "one run in flight puts its reading on every row",
    "catches": "says nothing on a session with no run in flight",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const said = standingsUnderway().find((one) => one.name === name);",
        "to": "  const said = standingsUnderway()[0];"
      }
    ]
  },
  {
    "name": "the page builds the live phrase and never puts it on the row",
    "catches": "prints the live reading on the row, beside the stored one",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "      standingSaid(row.standing),",
        "to": "      null,"
      }
    ]
  },
  {
    "name": "the page prints the live number without saying what is spending it",
    "catches": "prints every part of the live reading a person has to act on",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "    if (typeof standing.ranModel === \"string\" && standing.ranModel !== \"\") {\n      said.push(`on ${standing.ranModel}`);\n    }",
        "to": "    void 0;"
      }
    ]
  },
  {
    "name": "the page prints the live number without how long the run has been going",
    "catches": "prints every part of the live reading a person has to act on",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "    if (typeof standing.since === \"number\") {\n      said.push(`began ${ago(new Date(standing.since).toISOString())}`);\n    }",
        "to": "    void 0;"
      }
    ]
  },
  {
    "name": "the account is judged by the newest reading rather than the fullest",
    "catches": "reads the fullest reading of the window and not the freshest",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    other.ruled.fullness > one.ruled.fullness ||\n    (other.ruled.fullness === one.ruled.fullness && other.at > one.at)\n      ? other\n      : one,",
        "to": "    other.at > one.at ? other : one,"
      }
    ]
  },
  {
    "name": "the number comes from one reading and the name from another",
    "catches": "speaks for the reading it took the number from",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    on: winner.name,",
        "to": "    on: ofTheWindow.reduce((one, other) => (other.at > one.at ? other : one)).name,"
      }
    ]
  },
  {
    "name": "a reading the service named no moment for is dropped",
    "catches": "counts a reading the service named no moment for",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  const ended = (window) => window.resetsAt !== null && window.resetsAt * 1000 <= Date.now();",
        "to": "  const ended = (window) => window.resetsAt === null || window.resetsAt * 1000 <= Date.now();"
      }
    ]
  },
  {
    "name": "a tie between two readings goes to the older one",
    "catches": "speaks for the fresher of two readings that cannot be told apart",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "(other.ruled.fullness === one.ruled.fullness && other.at > one.at)",
        "to": "(other.ruled.fullness === one.ruled.fullness && other.at < one.at)"
      }
    ]
  },
  {
    "name": "only the runs that have finished are folded in",
    "catches": "reads the window a run in flight is filling",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    ...standingsUnderway().map((one) => ({ name: one.name, at: one.at, windows: one.windows, live: true })),",
        "to": "    ...[],"
      }
    ]
  },
  {
    "name": "a run still going is said to have last run",
    "catches": "says the run is going on rather than that it has finished",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "${standing.live ? `while ${standing.on} was running` : `when ${standing.on} last ran`}",
        "to": "when ${standing.on} last ran"
      }
    ]
  },
  {
    "name": "a message is held back when the account is past the stop line",
    "catches": "delivers a message",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "async function postMessage(instance, name, request, response) {\n  let text;",
        "to": "async function postMessage(instance, name, request, response) {\n  if (turnsGoing(name) === 0 && accountStanding(instance)?.stop) {\n    sendJson(response, 503, { error: \"the account is nearly gone\" });\n    return;\n  }\n  let text;"
      }
    ]
  },
  {
    "name": "a turn is refused a place in the queue when the account is past the stop line",
    "catches": "queues a turn behind another",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "async function postMessage(instance, name, request, response) {\n  let text;",
        "to": "async function postMessage(instance, name, request, response) {\n  if (turnsGoing(name) > 0 && accountStanding(instance)?.stop) {\n    sendJson(response, 503, { error: \"the account is nearly gone\" });\n    return;\n  }\n  let text;"
      }
    ]
  },
  {
    "name": "hiring is refused when the account is past the stop line",
    "catches": "hires",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "async function postSessions(instance, request, response) {\n  let name;",
        "to": "async function postSessions(instance, request, response) {\n  if (accountStanding(instance)?.stop) {\n    sendJson(response, 503, { error: \"the account is nearly gone\" });\n    return;\n  }\n  let name;"
      }
    ]
  },
  {
    "name": "a handover is refused when the account is past the stop line",
    "catches": "hands a session over",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "async function postHandover(instance, name, response) {\n  const done = await handOver(",
        "to": "async function postHandover(instance, name, response) {\n  if (accountStanding(instance)?.stop) {\n    sendJson(response, 503, { error: \"the account is nearly gone\" });\n    return;\n  }\n  const done = await handOver("
      }
    ]
  },
  {
    "name": "a session is called refused because the account is nearly gone",
    "catches": "says a session is refused only when the service refused it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    refused: refusedIn(instance.root, session.name),",
        "to": "    refused: refusedIn(instance.root, session.name) ?? (accountStanding(instance)?.stop ? { resetsAt: null } : null),"
      }
    ]
  },
  {
    "name": "a crossing is never recorded as having been said",
    "catches": "says one thing for one crossing and not one every pass",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    nowSeen(one.name, one.band);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the pass tells the lead's panel and not the lead's model",
    "catches": "tells the lead both ways, and buys no turn to do it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  overhear(instance.config.leader, watchedWrapper(said, at));\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the pass tells the lead's model and leaves no record on the panel",
    "catches": "tells the lead both ways, and buys no turn to do it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  append(instance.root, instance.config.leader, { from: THE_CHAT, text: said, watch: true });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "what the pass wrote is left unflagged",
    "catches": "tells the lead both ways, and buys no turn to do it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  append(instance.root, instance.config.leader, { from: THE_CHAT, text: said, watch: true });",
        "to": "  append(instance.root, instance.config.leader, { from: THE_CHAT, text: said });"
      }
    ]
  },
  {
    "name": "the pass buys a turn on the lead to say what it found",
    "catches": "buys no turn to say it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "function announce(instance, said, at) {\n  append(",
        "to": "function announce(instance, said, at) {\n  void deliver(instance, instance.config.leader, said, THE_CHAT);\n  append("
      }
    ]
  },
  {
    "name": "the whole pass is skipped while the lead is answering",
    "catches": "says what changed while the lead is answering something else",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "async function walkTheRoom(instance) {\n  const room = sessions(instance);",
        "to": "async function walkTheRoom(instance) {\n  if (turnsGoing(instance.config.leader) > 0) {\n    return;\n  }\n  const room = sessions(instance);"
      }
    ]
  },
  {
    "name": "the block handed to the lead does not say who is speaking",
    "catches": "says who is speaking, and that nobody typed it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    \"The chat is telling you this. Nobody typed it, and no turn was bought to say it: it waited here until you were asked something else.\",\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the block handed to the lead does not carry the moment",
    "catches": "carries the moment the room was read",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    `<watch read=\"${at}\">`,",
        "to": "    \"<watch>\","
      }
    ]
  },
  {
    "name": "the line about a band claims something was done about it",
    "catches": "says that nothing was stopped and no conversation was ended",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "Nothing has been stopped and no conversation has been ended by this: handing ${name} over is ${human}'s to press, on that session's panel.",
        "to": "${name} has been handed over."
      }
    ]
  },
  {
    "name": "a conversation nobody carried on is left for the next message",
    "catches": "ends a conversation that could no longer be carried on",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    (session) => turnsGoing(session.name) === 0 && hasGoneCold(instance.root, session.name),",
        "to": "    (session) => false && turnsGoing(session.name) === 0 && hasGoneCold(instance.root, session.name),"
      }
    ]
  },
  {
    "name": "a conversation that can still be carried on is ended too",
    "catches": "leaves a conversation that can still be carried on",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    (session) => turnsGoing(session.name) === 0 && hasGoneCold(instance.root, session.name),",
        "to": "    (session) => turnsGoing(session.name) === 0 && hasThread(instance.root, session.name),"
      },
      {
        "file": "tools/chat/server.mjs",
        "from": "      if (!hasGoneCold(instance.root, session.name)) {",
        "to": "      if (!hasThread(instance.root, session.name)) {"
      }
    ]
  },
  {
    "name": "a cold conversation is ended with a run going on it",
    "catches": "leaves a cold conversation alone while something is running on it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    (session) => turnsGoing(session.name) === 0 && hasGoneCold(instance.root, session.name),",
        "to": "    (session) => hasGoneCold(instance.root, session.name),"
      }
    ]
  },
  {
    "name": "a conversation is ended without saying so on its own panel",
    "catches": "says on that conversation's own panel where the memory stops",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      append(instance.root, session.name, { from: THE_CHAT, text: coldLine(session.name), cold: true });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the line about an ended conversation says what was saved rather than what was lost",
    "catches": "says what was lost and that nothing was asked of it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "Nothing was stopped and nothing was asked of ${name}: whatever it had not written to ${desk(name)} is gone, and the next message to it starts a new conversation that reads that desk first.",
        "to": "The next message to it starts a new conversation."
      }
    ]
  },
  {
    "name": "the record of the pass is never armed",
    "catches": "keeps one record of the pass, in counts and not in names",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  armTheWatch(every);\n  // AND SAID, HERE",
        "to": "  // AND SAID, HERE"
      }
    ]
  },
  {
    "name": "the record counts the crossings rather than the room",
    "catches": "keeps one record of the pass, in counts and not in names",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  tickRead({ sessions: room.length, decided, acted });",
        "to": "  tickRead({ sessions: crossed.length, decided, acted });"
      }
    ]
  },
  {
    "name": "the record is left behind when the chat closes",
    "catches": "forgets the record with the chat",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  seen.clear();\n  record = null;",
        "to": "  seen.clear();"
      }
    ]
  },
  {
    "name": "a message signed by the chat is read as the person's",
    "catches": "flags a message signed by the chat rather than reading it as the person's",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        ...(watching ? { watch: true } : {}),\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the record of a pass is added to the last rather than replacing it",
    "catches": "replaces the record each pass rather than adding to it",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  record = { ...record, at: Date.now(), sessions, decided, acted };",
        "to": "  record = { ...record, at: Date.now(), sessions, decided: record.decided + decided, acted: record.acted + acted };"
      }
    ]
  },
  {
    "name": "a pass counts an act it did not make",
    "catches": "counted only what it actually did",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      forget(instance.root, session.name);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a workspace that buys no turn has nothing armed for it",
    "catches": "arms a watch on an instance that asked for no turn",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  armTheWatch(every);\n  // AND SAID, HERE",
        "to": "  if (instance.config.watchEverySeconds !== 0) {\n    armTheWatch(every);\n  }\n  // AND SAID, HERE"
      }
    ]
  },
  {
    "name": "a window that lifts within the hour is treated like one that does not",
    "catches": "carries everybody when the window lifts within the hour",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        parking: standing.warm === false && buysATurn(instance.config),",
        "to": "        parking: standing.warm !== null && buysATurn(instance.config),"
      }
    ]
  },
  {
    "name": "an account that did not say when it lifts falls into the park-everybody branch",
    "catches": "carries everybody when nobody said when the window lifts",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        parking: standing.warm === false && buysATurn(instance.config),",
        "to": "        parking: standing.warm !== true && buysATurn(instance.config),"
      }
    ]
  },
  {
    "name": "the branches are the wrong way round: park when it lifts soon, carry when it lifts late",
    "catches": "parks every seat when the window lifts later than that",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        parking: standing.warm === false && buysATurn(instance.config),",
        "to": "        parking: standing.warm === true && buysATurn(instance.config),"
      }
    ]
  },
  {
    "name": "the emptiest seat is parked first",
    "catches": "parks in the order it must",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      return theirs - mine;",
        "to": "      return mine - theirs;"
      }
    ]
  },
  {
    "name": "a seat nobody has a reading for is parked first",
    "catches": "parks in the order it must",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      if (mine === null) {\n        return 1;\n      }\n      if (theirs === null) {\n        return -1;\n      }",
        "to": "      if (mine === null) {\n        return -1;\n      }\n      if (theirs === null) {\n        return 1;\n      }"
      }
    ]
  },
  {
    "name": "a tie goes to the later name",
    "catches": "parks in the order it must",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    return one.name < other.name ? -1 : 1;",
        "to": "    return one.name < other.name ? 1 : -1;"
      }
    ]
  },
  {
    "name": "the lead is parked where its share puts it",
    "catches": "parks in the order it must",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    if (one.name === lead || other.name === lead) {\n      return one.name === lead ? 1 : -1;\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a session sitting on a permission prompt is parked too",
    "catches": "leaves a session sitting on a permission prompt alone",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return parked(name, instance.root).length === 0;",
        "to": "  return true;"
      }
    ]
  },
  {
    "name": "a session with no thread is parked again on every pass",
    "catches": "does not park a session it has already parked",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (!hasThread(instance.root, name) || hasGoneCold(instance.root, name)) {\n    return false;\n  }\n",
        "to": "  if (hasGoneCold(instance.root, name)) {\n    return false;\n  }\n"
      }
    ]
  },
  {
    "name": "a handover never asks whether it is still wanted",
    "catches": "abandons a park whose seat stopped being worth parking before its turn ran",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    if (stillHolds !== null && !stillHolds()) {\n      return { abandoned: true };\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the park tells the handover it always still holds",
    "catches": "abandons a park whose seat stopped being worth parking before its turn ran",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      const done = await handOver(instance, session.name, parkAsked(session.name, full), () =>\n        worthParking(instance, session.name) && holdStands(instance.root, hold),\n      );",
        "to": "      const done = await handOver(instance, session.name, parkAsked(session.name, full), () => true);"
      }
    ]
  },
  {
    "name": "a park is said on the lead's panel and never to the lead's model",
    "catches": "says on the lead's panel that it parked somebody, both ways",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      announce(instance, parkedLine(session.name, full), at);",
        "to": "      append(instance.root, instance.config.leader, { from: THE_CHAT, text: parkedLine(session.name, full), watch: true });"
      }
    ]
  },
  {
    "name": "a park is said to the lead's model and never on the lead's panel",
    "catches": "says on the lead's panel that it parked somebody, both ways",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      announce(instance, parkedLine(session.name, full), at);",
        "to": "      overhear(instance.config.leader, watchedWrapper(parkedLine(session.name, full), at));"
      }
    ]
  },
  {
    "name": "the pass runs again while the last one is still going",
    "catches": "does not start a second pass while one is still going",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (passing) {\n    return;\n  }\n  passing = true;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a workspace that buys no turn is parked like any other",
    "catches": "parks nobody in a workspace that buys no turn",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        parking: standing.warm === false && buysATurn(instance.config),",
        "to": "        parking: standing.warm === false,"
      }
    ]
  },
  {
    "name": "a park is not counted as a decision",
    "catches": "counts what it decided and what it did",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      decided += 1;\n\n      // ASKED AGAIN INSIDE THE TURN",
        "to": "      // ASKED AGAIN INSIDE THE TURN"
      }
    ]
  },
  {
    "name": "a park that was abandoned is counted as done",
    "catches": "counts what it decided and what it did",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      if (done === OFFLINE || done.abandoned === true) {\n        continue;\n      }\n      if (done.refused === true) {\n        markRefused(instance.root, session.name);\n        continue;\n      }\n      markParked(instance.root, session.name);\n      acted += 1;\n",
        "to": "      acted += 1;\n      if (done === OFFLINE || done.abandoned === true) {\n        continue;\n      }\n      if (done.refused === true) {\n        markRefused(instance.root, session.name);\n        continue;\n      }\n      markParked(instance.root, session.name);\n"
      }
    ]
  },
  {
    "name": "a conversation nobody has a reading for is the emptiest one",
    "catches": "is nothing when there is no reading of the conversation",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  if (typeof context !== \"number\" || typeof window !== \"number\" || window <= 0) {\n    return null;\n  }\n  return context / window;",
        "to": "  if (typeof window !== \"number\" || window <= 0) {\n    return null;\n  }\n  return context / window;"
      }
    ]
  },
  {
    "name": "a conversation whose window is not known has a share anyway",
    "catches": "is nothing when the window is not known",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  if (typeof context !== \"number\" || typeof window !== \"number\" || window <= 0) {\n    return null;\n  }\n  return context / window;",
        "to": "  if (typeof context !== \"number\") {\n    return null;\n  }\n  return context / window;"
      }
    ]
  },
  {
    "name": "a window of nothing is divided by",
    "catches": "is nothing when the window is not positive",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  if (typeof context !== \"number\" || typeof window !== \"number\" || window <= 0) {\n    return null;\n  }\n  return context / window;",
        "to": "  if (typeof context !== \"number\" || typeof window !== \"number\") {\n    return null;\n  }\n  return context / window;"
      }
    ]
  },
  {
    "name": "the share is the window over the context",
    "catches": "is the share of the window the conversation fills",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return context / window;",
        "to": "  return window / context;"
      }
    ]
  },
  {
    "name": "the parks are decided from the readings and not from the hold",
    "catches": "keeps parking off the hold after the readings are gone",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (hold !== null && hold.parking) {",
        "to": "  if (hold !== null && hold.parking && accountStanding(instance)?.stop === true) {"
      }
    ]
  },
  {
    "name": "a seat is parked again whenever it has a thread again",
    "catches": "parks a seat once per hold",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      if (hold.parked.includes(session.name)) {\n        continue;\n      }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a parked seat is never entered in the hold",
    "catches": "remembers in the hold whom it parked",
    "edits": [
      {
        "file": "tools/chat/hold.mjs",
        "from": "  writeHold(root, { ...held, parked: [...held.parked, name], refused });",
        "to": ""
      }
    ]
  },
  {
    "name": "the hold is decided and never written down",
    "catches": "enters a hold when the account is at the stop line",
    "edits": [
      {
        "file": "tools/chat/hold.mjs",
        "from": "  writeHold(root, held);\n  return held;",
        "to": "  return held;"
      }
    ]
  },
  {
    "name": "a hold whose window has lifted stands forever",
    "catches": "ends the hold when the window lifts",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (hold !== null && holdLifted(hold)) {\n    // The seats",
        "to": "  if (false) {\n    // The seats"
      }
    ]
  },
  {
    "name": "the park never asks whether the hold still stands",
    "catches": "abandons a park whose hold ended before its turn ran",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        worthParking(instance, session.name) && holdStands(instance.root, hold),",
        "to": "        worthParking(instance, session.name),"
      }
    ]
  },
  {
    "name": "a hold with no lift moment is never released",
    "catches": "a hold with no lift moment ends when the readings stop saying stop",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    if (standing === null || !standing.stop) {\n      if (hold !== null) {\n        endHold(instance.root);\n        announce(instance, holdEndedLine(), at);\n        hold = null;\n      }\n    } else if",
        "to": "    if (standing === null || !standing.stop) {\n      // held\n    } else if"
      }
    ]
  },
  {
    "name": "a hold with no lift moment ignores a later reading that names one",
    "catches": "a hold with no lift moment is re-decided when a later reading names one",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    } else if (hold === null || standing.resetsAt !== null) {",
        "to": "    } else if (hold === null) {"
      }
    ]
  },
  {
    "name": "a hold is recorded only when somebody is parked",
    "catches": "records the hold when everybody is carried",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    } else if (hold === null || standing.resetsAt !== null) {",
        "to": "    } else if ((hold === null || standing.resetsAt !== null) && standing.warm === false) {"
      }
    ]
  },
  {
    "name": "a cold seat is still asked to hand over",
    "catches": "leaves a seat that has gone cold to the cold ending",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (!hasThread(instance.root, name) || hasGoneCold(instance.root, name)) {",
        "to": "  if (!hasThread(instance.root, name)) {"
      }
    ]
  },
  {
    "name": "the hold is entered and nothing is said",
    "catches": "says once, both ways, that the account is stopping and everybody is being handed over",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      announce(instance, holdEnteredLine(hold), at);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the carried branch is worded like the parking one",
    "catches": "says once that the account is stopping and nobody is being handed over",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const lifts = `That window lifts at ${atTime(hold.resetsAt)}`;\n  if (hold.warm) {",
        "to": "  const lifts = `That window lifts at ${atTime(hold.resetsAt)}`;\n  if (false) {"
      }
    ]
  },
  {
    "name": "a hold with no moment is worded as if it had one",
    "catches": "says once that the account is stopping and nobody is being handed over",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const opening = `The account has reached ${Math.round(hold.fullness * 100)}% of the usage window it is running in`;\n  if (hold.resetsAt === null) {",
        "to": "  const opening = `The account has reached ${Math.round(hold.fullness * 100)}% of the usage window it is running in`;\n  if (false) {"
      }
    ]
  },
  {
    "name": "a workspace that buys no turn is told everybody is being handed over",
    "catches": "says that nobody is handed over because this workspace buys no turn",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (hold.parking) {\n    return `${opening}. ${lifts}, later than",
        "to": "  if (true) {\n    return `${opening}. ${lifts}, later than"
      }
    ]
  },
  {
    "name": "a standing hold with no moment is entered again on every pass",
    "catches": "says each turn the hold took, once, in order",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    } else if (hold === null || standing.resetsAt !== null) {",
        "to": "    } else {"
      }
    ]
  },
  {
    "name": "the hold lifts and nothing is said",
    "catches": "says once that the window reopened, and who stands where",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    announce(instance, holdLiftedLine(hold), at);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the lifted line names nobody as handed over",
    "catches": "says once that the window reopened, and who stands where",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (hold.parked.length === 0) {\n    said.push(\"Nobody was handed over under it",
        "to": "  if (true) {\n    said.push(\"Nobody was handed over under it"
      }
    ]
  },
  {
    "name": "the lifted line forgets the seats still being turned away",
    "catches": "says once that the window reopened, and who stands where",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const refused = Object.keys(hold.refused);\n  if (refused.length > 0) {",
        "to": "  const refused = Object.keys(hold.refused);\n  if (false) {"
      }
    ]
  },
  {
    "name": "the lifted line is said before the seat that went cold under it",
    "catches": "says once what was lost on the seat it kept being turned away on, before the record goes",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    for (const name of Object.keys(hold.refused)) {\n      if (hasGoneCold(instance.root, name)) {\n        announce(instance, neverParkedLine(name, hold.refused[name]), at);\n        forgetRefused(instance.root, name);\n      }\n    }\n    hold = holdIn(instance.root);\n    endHold(instance.root);\n    announce(instance, holdLiftedLine(hold), at);\n",
        "to": "    announce(instance, holdLiftedLine(hold), at);\n    for (const name of Object.keys(hold.refused)) {\n      if (hasGoneCold(instance.root, name)) {\n        announce(instance, neverParkedLine(name, hold.refused[name]), at);\n        forgetRefused(instance.root, name);\n      }\n    }\n    hold = holdIn(instance.root);\n    endHold(instance.root);\n"
      }
    ]
  },
  {
    "name": "the seats turned away are not looked at before the record goes",
    "catches": "says once what was lost on the seat it kept being turned away on, before the record goes",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    for (const name of Object.keys(hold.refused)) {\n      if (hasGoneCold(instance.root, name)) {\n        announce(instance, neverParkedLine(name, hold.refused[name]), at);\n        forgetRefused(instance.root, name);\n      }\n    }\n    hold = holdIn(instance.root);\n    endHold(instance.root);\n",
        "to": "    endHold(instance.root);\n"
      }
    ]
  },
  {
    "name": "a seat still warm at the lift is said to have gone cold",
    "catches": "says once that the window reopened, and who stands where",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    for (const name of Object.keys(hold.refused)) {\n      if (hasGoneCold(instance.root, name)) {",
        "to": "    for (const name of Object.keys(hold.refused)) {\n      if (true) {"
      }
    ]
  },
  {
    "name": "a hold with no moment ends and nothing is said",
    "catches": "says each turn the hold took, once, in order",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        announce(instance, holdEndedLine(), at);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a refusal is never counted on the hold",
    "catches": "counts on the hold each time the account turns a park away",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      if (done.refused === true) {\n        markRefused(instance.root, session.name);\n        continue;\n      }",
        "to": "      if (done.refused === true) {\n        continue;\n      }"
      }
    ]
  },
  {
    "name": "the seat that went cold with its park refused is not said",
    "catches": "says once what was lost when the seat goes cold with its park still refused",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      if (hold.refused[session.name] !== undefined && hasGoneCold(instance.root, session.name)) {\n        announce(instance, neverParkedLine(session.name, hold.refused[session.name]), at);\n        forgetRefused(instance.root, session.name);\n        continue;\n      }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the count stays on the hold after it was said",
    "catches": "says once what was lost when the seat goes cold with its park still refused",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        announce(instance, neverParkedLine(session.name, hold.refused[session.name]), at);\n        forgetRefused(instance.root, session.name);\n        continue;",
        "to": "        announce(instance, neverParkedLine(session.name, hold.refused[session.name]), at);\n        continue;"
      }
    ]
  },
  {
    "name": "the hold is not served beside the rows",
    "catches": "serves the hold beside the rows, as the facts it was decided on",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      hold: holdSaid(holdIn(instance.root)),\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the served hold leaves out what it was decided on",
    "catches": "serves the hold beside the rows, as the facts it was decided on",
    "edits": [
      {
        "file": "tools/chat/hold.mjs",
        "from": "  return { resetsAt: hold.resetsAt, warm: hold.warm, parking: hold.parking };",
        "to": "  return { resetsAt: hold.resetsAt, parking: hold.parking };"
      }
    ]
  },
  {
    "name": "the hold forgets what the reading answered about the hour",
    "catches": "records the hold when everybody is carried",
    "edits": [
      {
        "file": "tools/chat/hold.mjs",
        "from": "    warm: typeof warm === \"boolean\" ? warm : null,\n    parking: parking === true,\n    parked: [],",
        "to": "    warm: null,\n    parking: parking === true,\n    parked: [],"
      }
    ]
  },
  {
    "name": "the terminal room leaves the hold out",
    "catches": "says the account is stopping, and what is being done, in the room a person reads at a terminal",
    "edits": [
      {
        "file": "tools/ovai.mjs",
        "from": "  for (const line of roomLines(body.sessions, body.offline === true, body.hold ?? null)) {",
        "to": "  for (const line of roomLines(body.sessions, body.offline === true)) {"
      }
    ]
  },
  {
    "name": "the lead's room tool leaves the hold out",
    "catches": "says it to the lead too, in the words the command prints",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return { text: roomLines(rows, offline(), holdSaid(holdIn(instance.root))).join(\"\\n\") };",
        "to": "  return { text: roomLines(rows, offline()).join(\"\\n\") };"
      }
    ]
  },
  {
    "name": "the account line is printed whether or not there is a hold",
    "catches": "says nothing about it while there is no hold, and leaves every row alone",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "  if (hold !== null && typeof hold === \"object\") {\n    above.push(holdSaid(hold));\n  }",
        "to": "  above.push(holdSaid(hold ?? {}));"
      }
    ]
  },
  {
    "name": "the room words a carried hold as the parking one",
    "catches": "says only what the hold does, one sentence for each thing it can be doing",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "  if (hold.warm === true) {\n    return `The account is nearly spent — ${lifts}, inside the hour",
        "to": "  if (false) {\n    return `The account is nearly spent — ${lifts}, inside the hour"
      }
    ]
  },
  {
    "name": "the room words a hold with no moment as if it had one",
    "catches": "says only what the hold does, one sentence for each thing it can be doing",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "  if (typeof hold.resetsAt !== \"number\") {\n    return \"The account is nearly spent and did not say when it lifts",
        "to": "  if (false) {\n    return \"The account is nearly spent and did not say when it lifts"
      }
    ]
  },
  {
    "name": "the room tells a workspace that buys no turn that everybody is being handed over",
    "catches": "says only what the hold does, one sentence for each thing it can be doing",
    "edits": [
      {
        "file": "tools/chat/room.mjs",
        "from": "  if (hold.parking === true) {\n    return `The account is nearly spent — ${lifts}, later than",
        "to": "  if (true) {\n    return `The account is nearly spent — ${lifts}, later than"
      }
    ]
  },
  {
    "name": "the page builds the account line and never puts it on the room",
    "catches": "builds the sentence from the served facts, in the page's own copy of the room",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "    if (hold !== null && typeof hold === \"object\") {\n      said.push(Object.assign(document.createElement(\"p\"), { textContent: holdSaid(hold) }));\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the page reads no hold off what the chat serves",
    "catches": "builds the sentence from the served facts, in the page's own copy of the room",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "  function showTheRoom({ offline, hold, sessions }) {",
        "to": "  function showTheRoom({ offline, sessions }) {\n    const hold = null;"
      }
    ]
  },
  {
    "name": "the bound on the attempts is read past",
    "catches": "asks a seat as many times as the workspace allows and then leaves it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      if (attempts !== null && (hold.refused[session.name] ?? 0) >= attempts) {",
        "to": "      if (false) {"
      }
    ]
  },
  {
    "name": "the bound on the attempts is off by one",
    "catches": "asks a seat as many times as the workspace allows and then leaves it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      if (attempts !== null && (hold.refused[session.name] ?? 0) >= attempts) {",
        "to": "      if (attempts !== null && (hold.refused[session.name] ?? 0) > attempts) {"
      }
    ]
  },
  {
    "name": "absent is read as one attempt",
    "catches": "counts on the hold each time the account turns a park away",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  return typeof said === \"number\" && Number.isInteger(said) && said >= 1 ? said : null;",
        "to": "  return typeof said === \"number\" && Number.isInteger(said) && said >= 1 ? said : 1;"
      }
    ]
  },
  {
    "name": "the bound is read off a constant",
    "catches": "reads the bound the instance wrote",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  return typeof said === \"number\" && Number.isInteger(said) && said >= 1 ? said : null;",
        "to": "  return 3;"
      }
    ]
  },
  {
    "name": "zero attempts is accepted as a bound",
    "catches": "refuses a bound that is not a whole number of at least one, naming the field",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  if (typeof attempts !== \"number\" || !Number.isInteger(attempts) || attempts < 1) {",
        "to": "  if (typeof attempts !== \"number\" || !Number.isInteger(attempts) || attempts < 0) {"
      }
    ]
  },
  {
    "name": "the chat starts on a bound nothing can read",
    "catches": "refuses to start a chat on a bound nothing can read, naming the field",
    "edits": [
      {
        "file": "tools/ovai.mjs",
        "from": "  const wrongAttempts = parkAttemptsProblem(config[PARK_ATTEMPTS]);\n  if (wrongAttempts !== null) {\n    throw new UsageError(`${configIn(root)}: ${wrongAttempts}`);\n  }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the watch is armed and nothing is said",
    "catches": "says on the terminal, at the moment of arming, how often the room is read",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  console.log(watchArmedLine(instance.config, every));\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a workspace that buys no turn is told the same as one that does",
    "catches": "says that a workspace which buys no turn is still read, and that nothing is spent on it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (!buysATurn(config)) {\n    return `${cadence}, and nothing is spent on it",
        "to": "  if (false) {\n    return `${cadence}, and nothing is spent on it"
      }
    ]
  },
  {
    "name": "the watch is left off /health",
    "catches": "serves the watch's own record on /health, armed and not yet fired",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      watch: watchSaid(theWatchRecord()),\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the cadence on the record is the constant and not the one armed",
    "catches": "serves the watch's own record on /health, armed and not yet fired",
    "edits": [
      {
        "file": "tools/chat/watch.mjs",
        "from": "  record = { everySeconds: every / 1000, armedAt: Date.now(), at: null, sessions: 0, decided: 0, acted: 0 };",
        "to": "  record = { everySeconds: EVERY / 1000, armedAt: Date.now(), at: null, sessions: 0, decided: 0, acted: 0 };"
      }
    ]
  },
  {
    "name": "the moment of the last pass is served as it is kept",
    "catches": "serves when the last pass finished, once one has",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    at: record.at === null ? null : new Date(record.at).toISOString(),",
        "to": "    at: record.at,"
      }
    ]
  }
]

[
  {
    "name": "the page goes out with its empty tag",
    "catches": "serves the page with its secret in the one tag that carries it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  const page = Buffer.from(template.replace(SECRET_TAG, `<meta name=\"openovai-secret\" content=\"${pageSecret()}\">`));",
        "to": "  const page = Buffer.from(template);"
      }
    ]
  },
  {
    "name": "the health says nothing about the port",
    "catches": "says in its health which instance it serves, on which port, at which version",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    sendJson(response, 200, { instance: instance.root, port, version: version(instance.root) ?? \"unknown\" });",
        "to": "    sendJson(response, 200, { instance: instance.root, version: version(instance.root) ?? \"unknown\" });"
      }
    ]
  },
  {
    "name": "the chat never records where it listens",
    "catches": "records where it is listening, for whoever starts a session",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "      record(instance.root, port);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the page is told a made-up User",
    "catches": "tells the page who the User and the Leader are, and every seat with its role and model",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    user: instance.config.user,\n    leader: instance.config.leader,\n    instance: shownRoot",
        "to": "    user: \"User\",\n    leader: instance.config.leader,\n    instance: shownRoot"
      }
    ]
  },
  {
    "name": "the Leader is listed last",
    "catches": "puts the Leader first and the rest in name order",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  return [leader, ...rest].map((name) => ({",
        "to": "  return [...rest, leader].map((name) => ({"
      }
    ]
  },
  {
    "name": "a panel opens for anybody at all",
    "catches": "will not open a panel for somebody who does not work here",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    if (!isSeat(instance, seat)) {\n      sendJson(response, 404,",
        "to": "    if (false) {\n      sendJson(response, 404,"
      }
    ]
  },
  {
    "name": "the roster takes any directory under desks/, whatever it is called",
    "catches": "is not on the roster",
    "edits": [
      {
        "file": "lib/desks.mjs",
        "from": "      .filter((entry) => entry.isDirectory() && isName(entry.name))",
        "to": "      .filter((entry) => entry.isDirectory())"
      }
    ]
  },
  {
    "name": "the page gate takes any bearer at all",
    "catches": "answers an unknown secret with one body, whatever was tried",
    "edits": [
      {
        "file": "lib/chat/secrets.mjs",
        "from": "  return given.length === expected.length && crypto.timingSafeEqual(given, expected);",
        "to": "  return presented !== \"\";"
      }
    ]
  },
  {
    "name": "an unknown secret on the tool route falls through to the routes below",
    "catches": "answers an unknown secret with one body, whatever was tried",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  if (who === null) {\n    sendJson(response, 401, UNKNOWN);\n    return;\n  }\n\n  if (TOOL_ROUTE.test(url.pathname)) {",
        "to": "  if (who === null && !TOOL_ROUTE.test(url.pathname)) {\n    sendJson(response, 401, UNKNOWN);\n    return;\n  }\n\n  if (who !== null && TOOL_ROUTE.test(url.pathname)) {"
      }
    ]
  },
  {
    "name": "the page secret resolves to a seat on the tool route",
    "catches": "never lets one kind of secret open the other side",
    "edits": [
      {
        "file": "lib/chat/secrets.mjs",
        "from": "  const known = issued.get(secret);\n  return known === undefined ? null : { seat: known.seat, role: known.role };",
        "to": "  const known = issued.get(secret);\n  if (known === undefined && secret === page) {\n    return { seat: \"page\", role: \"Leader\" };\n  }\n  return known === undefined ? null : { seat: known.seat, role: known.role };"
      }
    ]
  },
  {
    "name": "a session's secret opens the page routes",
    "catches": "never lets one kind of secret open the other side",
    "edits": [
      {
        "file": "lib/chat/secrets.mjs",
        "from": "  if (typeof presented !== \"string\") {\n    return false;\n  }\n  const expected",
        "to": "  if (typeof presented !== \"string\") {\n    return false;\n  }\n  if (issued.has(presented)) {\n    return true;\n  }\n  const expected"
      }
    ]
  },
  {
    "name": "a secret outlives its process",
    "catches": "forgets a secret when its process has gone, and the next process gets another",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    revoke(secret);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the secret is written under the instance when it is issued",
    "catches": "writes no secret anywhere under the instance",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  const secret = issue(seat, role);\n",
        "to": "  const secret = issue(seat, role);\n  fs.writeFileSync(path.join(instance.root, \"temp\", \"secret.txt\"), secret);\n"
      }
    ]
  },
  {
    "name": "every process is given the same secret",
    "catches": "mints one secret per process, 43 characters of base64url, and no two alike",
    "edits": [
      {
        "file": "lib/chat/secrets.mjs",
        "from": "  return crypto.randomBytes(32).toString(\"base64url\");",
        "to": "  return \"s\".repeat(43);"
      }
    ]
  },
  {
    "name": "the literal secret is written into the command line",
    "catches": "hands the process its secret in the environment and never on the command line",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    args.push(\"--mcp-config\", tools);",
        "to": "    args.push(\"--mcp-config\", tools.replace(`\\${${SECRET_IN_ENVIRONMENT}}`, secret));"
      }
    ]
  },
  {
    "name": "the process is not handed its secret at all",
    "catches": "opens the door to the secret it issued",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    env: { ...environment(instance.root, instance.config.auth, seat), [SECRET_IN_ENVIRONMENT]: secret },",
        "to": "    env: { ...environment(instance.root, instance.config.auth, seat) },"
      }
    ]
  },
  {
    "name": "a colleague is waited for as long as Claude Code's default",
    "catches": "points the process at this chat, with a whole turn to wait on a colleague",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "        timeout: A_WHOLE_TURN,\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the run is not verbose, so nothing but the result would come back",
    "catches": "runs it in print mode over the streaming protocol, asking here before using a tool",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    \"--verbose\",\n",
        "to": ""
      }
    ]
  },
  {
    "name": "everybody runs on the worker model",
    "catches": "runs each seat on the model its desk resolves to",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    modelFor(instance.root, seat, instance.config),",
        "to": "    instance.config.models.worker,"
      }
    ]
  },
  {
    "name": "the run is started without the list of what is not to be read",
    "catches": "hands the run the list of what is not to be read, as a settings document of its own",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  args.push(\"--settings\", ownInstructions(instance.root));\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the process starts wherever the chat happens to be",
    "catches": "starts the process in the instance, on the instance's own home",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    cwd: instance.root,\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a seat's role is read from the configuration on every call",
    "catches": "fixes the role at the start: who leads is read once, from the configuration then",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  return processes.get(seat)?.role ?? roleOf(instance, seat);",
        "to": "  return roleOf(instance, seat);"
      }
    ]
  },
  {
    "name": "a seat can be started twice",
    "catches": "refuses to start a seat that is already running, or one that does not exist",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  if (processes.has(seat)) {\n    throw new Error(`${seat} is already running`);\n  }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "tell takes anything shaped like a frame",
    "catches": "takes frames from frames.mjs and nothing else, however much a string looks like one",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "export function tell(seat, frame, { ask = null, written = nothing } = {}) {\n  if (!isFrame(frame)) {",
        "to": "export function tell(seat, frame, { ask = null, written = nothing } = {}) {\n  if (false) {"
      }
    ]
  },
  {
    "name": "the frame's kind goes on stdin instead of its text",
    "catches": "writes the frame as one line and answers with what the turn came to",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "content: frame.text }",
        "to": "content: frame.kind }"
      }
    ]
  },
  {
    "name": "a seat with no process is told anyway, into a queue nobody drains",
    "catches": "says when the seat itself has no process, on the panel too",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    log(\"dropped\", seat, null, `${frame.kind}, no process`);\n    return { refused: \"no process\" };",
        "to": "    log(\"dropped\", seat, null, `${frame.kind}, no process`);\n    const pending = held.get(seat) ?? [];\n    held.set(seat, pending);\n    return { delivered: true, answered: new Promise((resolve) => pending.push({ frame, resolve })) };"
      },
      {
        "file": "lib/chat/session.mjs",
        "from": "const processes = new Map();",
        "to": "const processes = new Map();\nconst held = new Map();"
      },
      {
        "file": "lib/chat/session.mjs",
        "from": "    queue: [...queue],\n    turn: null,",
        "to": "    queue: [...queue, ...(held.get(seat) ?? [])],\n    turn: null,"
      }
    ]
  },
  {
    "name": "a refusal to tell is not said in the log",
    "catches": "refuses a seat with no process, queues nothing, and says so in the log",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    log(\"dropped\", seat, null, `${frame.kind}, no process`);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "any frame at all ends the turn",
    "catches": "reads the answer among whatever else the run says",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "      if (frame.type === \"result\" && record.turn !== null) {",
        "to": "      if (record.turn !== null) {"
      }
    ]
  },
  {
    "name": "an empty answer is not said to be one",
    "catches": "says when a turn answered with nothing",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  return { text, failed, silent: !failed && said === 0 };",
        "to": "  return { text, failed, silent: false };"
      }
    ]
  },
  {
    "name": "a turn whose process ended is never settled",
    "catches": "settles every waiting turn when the process ends mid-turn, and holds nobody past the exit",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    for (const turn of left) {\n      turn.resolve(turn === running ? { ended: true, text: why, err: record.err.trim() } : { ended: true, unread: true, text: why });\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "what the process said on stderr is dropped",
    "catches": "says what the process said on stderr when it fell over saying something",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    gone(\n      record.ending === \"restart\"\n        ? `${seat} restarted`\n        : record.diedOf !== null\n          ? record.diedOf\n          : record.err.trim() === \"\"\n            ? `${seat} ended before answering`\n            : record.err.trim(),\n      signal ?? `exit ${code}`,\n    );",
        "to": "    gone(record.ending === \"restart\" ? `${seat} restarted` : record.diedOf !== null ? record.diedOf : `${seat} ended before answering`, signal ?? `exit ${code}`);"
      }
    ]
  },
  {
    "name": "ending a seat never closes its conversation",
    "catches": "closes the conversation, which ends a run between turns, and forgets the secret",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  child.stdin.end();\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a run that will not go is left where it is",
    "catches": "takes down a run that will not go once patience has run out",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    child.kill(\"SIGKILL\");\n",
        "to": ""
      }
    ]
  },
  {
    "name": "ending a seat with no process says it ended one",
    "catches": "answers false for a seat with no process",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  const record = processes.get(seat);\n  if (record === undefined) {\n    return false;\n  }\n  const { child } = record;",
        "to": "  const record = processes.get(seat);\n  if (record === undefined) {\n    return true;\n  }\n  const { child } = record;"
      }
    ]
  },
  {
    "name": "ending every seat ends none",
    "catches": "ends every seat at once",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  return Promise.all(runningSeats().map((seat) => end(seat, patience)));",
        "to": "  return Promise.all([]);"
      }
    ]
  },
  {
    "name": "room is the Leader's alone",
    "catches": "serves the Leader exactly what BUILT_IN names but done, and a Worker message, room, index, validate, write_desk and done",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  room: [LEADER, WORKER],",
        "to": "  room: [LEADER],"
      }
    ]
  },
  {
    "name": "message reads who is calling from a second argument",
    "catches": "binds who is calling into every tool, so none of them reads it from an argument",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "      async run(args) {\n        if (!offered(\"message\")) {",
        "to": "      async run(args, caller_) {\n        if (!offered(\"message\")) {"
      }
    ]
  },
  {
    "name": "from is whatever the arguments claim",
    "catches": "says who a message is from by the secret it came over, whatever the arguments claim",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        const told = deliver(instance, to, messageFrame(caller.seat, text, { urgent: urgent === true }));",
        "to": "        const told = deliver(instance, to, messageFrame(args?.from ?? caller.seat, text, { urgent: urgent === true }));"
      }
    ]
  },
  {
    "name": "the sender's side is not written on the panel",
    "catches": "comes back at once, with nothing of the reply in it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        append(root, to, { from: caller.seat, to, text, msg, ...marked });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "message waits for the addressee's turn",
    "catches": "comes back at once, with nothing of the reply in it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        showTheEnding(root, to, told.answered);\n",
        "to": "        showTheEnding(root, to, told.answered);\n        await told.answered;\n"
      }
    ]
  },
  {
    "name": "the reply is handed back in the call",
    "catches": "comes back at once, with nothing of the reply in it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        return { text: `sent to ${to}` };",
        "to": "        return { text: (await told.answered).text };"
      }
    ]
  },
  {
    "name": "the addressee's answer is written as a message back",
    "catches": "comes back at once, with nothing of the reply in it",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    said: ({ text }) => append(instance.root, seat, { from: seat, text }),",
        "to": "    said: ({ text }) => append(instance.root, seat, { from: seat, to: instance.config.leader, text, msg: crypto.randomUUID() }),"
      },
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "import { publish } from \"./events.mjs\";",
        "to": "import crypto from \"node:crypto\";\nimport { publish } from \"./events.mjs\";"
      }
    ]
  },
  {
    "name": "a Worker's message to the Leader is refused",
    "catches": "takes a message from the one it is itself messaging: nobody waits, so nothing can wait for itself",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        if (to === caller.seat) {",
        "to": "        if (to === caller.seat || to === instance.config.leader) {"
      }
    ]
  },
  {
    "name": "a process gone before answering leaves no row",
    "catches": "says on the addressee's panel when its process ended before answering; the call itself went",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        showTheEnding(root, to, told.answered);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the Leader's answer to a Worker goes back to the Worker",
    "catches": "records a Worker's call on its own log and the receipt on the Leader's, and hands nothing back",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        showTheEnding(root, to, told.answered);\n",
        "to": "        showTheEnding(root, to, told.answered);\n        told.answered.then((reply) => append(root, caller.seat, { from: to, to: caller.seat, text: reply.text, msg: crypto.randomUUID() }));\n"
      }
    ]
  },
  {
    "name": "the Leader leaving leaves no row",
    "catches": "leaves one row on the Leader's panel when its process is gone, and starts a fresh one for the event, the event its first line",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "  if (role === LEADER && ending !== \"restart\") {",
        "to": "  if (false) {"
      }
    ]
  },
  {
    "name": "the row says something else",
    "catches": "leaves one row on the Leader's panel when its process is gone, and starts a fresh one for the event, the event its first line",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "  return `${seat} has left — the next message starts a fresh session`;",
        "to": "  return `${seat} has left`;"
      }
    ]
  },
  {
    "name": "the row is a bubble, not a divider",
    "catches": "leaves one row on the Leader's panel when its process is gone, and starts a fresh one for the event, the event its first line",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    append(instance.root, seat, { from: SERVER, divider: true, text: hasLeft(seat) });",
        "to": "    append(instance.root, seat, { from: SERVER, text: hasLeft(seat) });"
      }
    ]
  },
  {
    "name": "the next Leader process resumes the one before it",
    "catches": "leaves one row on the Leader's panel when its process is gone, and starts a fresh one for the event, the event its first line",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    \"--model\",\n    modelFor(instance.root, seat, instance.config),\n  ];",
        "to": "    \"--model\",\n    modelFor(instance.root, seat, instance.config),\n    \"--continue\",\n  ];"
      }
    ]
  },
  {
    "name": "what a seat says lands only later",
    "catches": "lands on the panel as it is said, before the turn ends",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "              said({ text: block.text });",
        "to": "              setTimeout(() => said({ text: block.text }), 1000);"
      }
    ]
  },
  {
    "name": "silent is read off the last words, not the whole turn",
    "catches": "is silent only when it said nothing in the whole turn, not when its last words were none",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  return { text, failed, silent: !failed && said === 0 };",
        "to": "  return { text, failed, silent: !failed && text.trim() === \"\" };"
      }
    ]
  },
  {
    "name": "a message to nobody is passed on to see what happens",
    "catches": "refuses a message to nobody, and delivers nothing to anybody",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        if (!isSeat(instance, to)) {",
        "to": "        if (false) {"
      }
    ]
  },
  {
    "name": "a message to yourself is delivered",
    "catches": "refuses a message to yourself",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        if (to === caller.seat) {",
        "to": "        if (false) {"
      }
    ]
  },
  {
    "name": "a message of nothing but spaces is delivered",
    "catches": "refuses a message with nothing in it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        if (typeof text !== \"string\" || text.trim() === \"\") {",
        "to": "        if (typeof text !== \"string\") {"
      }
    ]
  },
  {
    "name": "a message starts the seat it is for",
    "catches": "never starts a seat for a message: no process is a refusal",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        const told = deliver(instance, to, messageFrame(caller.seat, text, { urgent: urgent === true }));",
        "to": "        if (!running(to)) {\n          startSeat(instance, to);\n        }\n        const told = deliver(instance, to, messageFrame(caller.seat, text, { urgent: urgent === true }));"
      }
    ]
  },
  {
    "name": "nobody in the room is you",
    "catches": "tells a session who works here, who runs, and which one it is",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "you: seat.name === caller.seat })",
        "to": "you: false })"
      }
    ]
  },
  {
    "name": "a tool of the instance's own is not told who is calling",
    "catches": "serves a tool of the instance's own after its own, handing it who is calling",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        return answerFrom(plugin.name, await plugin.run(args, { seat: caller.seat, role: caller.role, root, config: instance.config }));",
        "to": "        return answerFrom(plugin.name, await plugin.run(args));"
      }
    ]
  },
  {
    "name": "a tool of the instance's own answers in whatever shape it likes",
    "catches": "refuses for a tool of the instance's own that answers outside the shape",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        return answerFrom(plugin.name, await plugin.run(args, { seat: caller.seat, role: caller.role, root, config: instance.config }));",
        "to": "        return await plugin.run(args, { seat: caller.seat, role: caller.role, root, config: instance.config });"
      }
    ]
  },
  {
    "name": "the Leader is not told what the User typed to a Worker",
    "catches": "reaches the seat as the User's own frame, and the Leader as a server event",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  if (seat !== instance.config.leader) {\n    const leader = instance.config.leader;",
        "to": "  if (false) {\n    const leader = instance.config.leader;"
      }
    ]
  },
  {
    "name": "what the User typed is framed as a message from somebody called User",
    "catches": "reaches the seat as the User's own frame, and the Leader as a server event",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  const told = deliver(instance, seat, userFrame(text), { written: () => delivered(root, seat, index) });",
        "to": "  const told = deliver(instance, seat, messageFrame(\"User\", text), { written: () => delivered(root, seat, index) });"
      }
    ]
  },
  {
    "name": "what the User typed is not written on the panel",
    "catches": "lands on the seat's panel as the User's row, with the reply under it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  const index = append(root, seat, { from: \"user\", text });\n",
        "to": "  const index = -1;\n"
      }
    ]
  },
  {
    "name": "the reply never lands on the panel",
    "catches": "lands on the seat's panel as the User's row, with the reply under it",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    said: ({ text }) => append(instance.root, seat, { from: seat, text }),\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the Leader's panel does not say who was typed to",
    "catches": "shows the Leader what was typed to whom, and what the Leader made of it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    append(root, leader, { from: \"user\", typedTo: seat, text });",
        "to": "    append(root, leader, { from: \"user\", text });"
      }
    ]
  },
  {
    "name": "the Leader is told about its own turn as an event too",
    "catches": "frames a word to the Leader as the User's own, and tells nobody else",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  if (seat !== instance.config.leader) {\n    const leader = instance.config.leader;",
        "to": "  if (true) {\n    const leader = instance.config.leader;"
      }
    ]
  },
  {
    "name": "a seat with no process is not said so on the panel",
    "catches": "says when the seat itself has no process, on the panel too",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    append(root, seat, { from: SERVER, text: told.refused === \"no process\" ? `${seat} has no process` : told.refused, failed: true });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the page may type nothing but spaces",
    "catches": "refuses a message with nothing in it, or nothing it can read",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  if (typeof text !== \"string\" || text.trim() === \"\") {\n    sendJson(response, 400, { error: \"a message needs some text\" });",
        "to": "  if (typeof text !== \"string\") {\n    sendJson(response, 400, { error: \"a message needs some text\" });"
      }
    ]
  },
  {
    "name": "the server writes to a session's stdin on its own",
    "catches": "is session.mjs, through the frame writer, the two permission answers and the interrupt, and nobody else",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  sendJson(response, 200, typed(instance, seat, text));",
        "to": "  // running(seat) && processes.get(seat).child.stdin.write(text);\n  sendJson(response, 200, typed(instance, seat, text));"
      }
    ]
  },
  {
    "name": "the lifecycle writes to a session's stdin on its own",
    "catches": "is session.mjs, through the frame writer, the two permission answers and the interrupt, and nobody else",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "  const told = tell(seat, frame, { ask, written });",
        "to": "  // recordOf(seat)?.child.stdin.write(frame.text);\n  const told = tell(seat, frame, { ask, written });"
      }
    ]
  },
  {
    "name": "the answer that names a path carries the secret as it came",
    "catches": "says nothing about a request, and never a secret where an answer names the path",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  return pathname.replace(SECRET_IN_PATH, \"/mcp/<secret>\");",
        "to": "  return pathname;"
      }
    ]
  },
  {
    "name": "every request is a line the server says",
    "catches": "says nothing about a request, and never a secret where an answer names the path",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  const server = http.createServer((request, response) => {\n    handle(",
        "to": "  const server = http.createServer((request, response) => {\n    response.once(\"finish\", () => log(\"stop\", null, null, `${request.method} ${shownAs(new URL(request.url, `http://${HOST}`).pathname)} ${response.statusCode}`));\n    handle("
      }
    ]
  },
  {
    "name": "the page secret is the same every start",
    "catches": "starts with nothing when started again: the page secret of the last run opens nothing",
    "edits": [
      {
        "file": "lib/chat/secrets.mjs",
        "from": "    page = mint();",
        "to": "    page = \"p\".repeat(43);"
      }
    ]
  },
  {
    "name": "the page sends its calls without the secret",
    "catches": "reads its secret off the tag, sends it on every call, and reloads when it is no longer known",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "      headers: { ...(options.headers ?? {}), authorization: `Bearer ${secret}` },",
        "to": "      headers: { ...(options.headers ?? {}) },"
      }
    ]
  },
  {
    "name": "the page sits on a secret the server no longer knows",
    "catches": "reads its secret off the tag, sends it on every call, and reloads when it is no longer known",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    if (answered.status === 401) {\n      location.reload();\n      throw new Error(\"the server no longer knows this page\");\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the page fetches the room around its own gate",
    "catches": "reads its secret off the tag, sends it on every call, and reloads when it is no longer known",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "call(\"/sessions\").then(",
        "to": "fetch(\"/sessions\").then("
      }
    ]
  },
  {
    "name": "a new row is written and nobody is told",
    "catches": "a new row reaches the page without a poll",
    "edits": [
      {
        "file": "lib/chat/conversation.mjs",
        "from": "  publish(\"row\", { seat: session, index: messages.length - 1, row: entry });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the counts a page connects with are ignored",
    "catches": "a client connecting with counts gets only the rows after them",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    const since = counts.get(name) ?? 0;",
        "to": "    const since = 0;"
      }
    ]
  },
  {
    "name": "a turn taken is not told",
    "catches": "tells of a seat's process starting, a turn under way and over, and the process gone",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  record.turn.written();\n  record.changed(record);\n}",
        "to": "  record.turn.written();\n}"
      }
    ]
  },
  {
    "name": "a process gone is not told",
    "catches": "tells of a seat's process starting, a turn under way and over, and the process gone",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "function afterClose(instance, { seat, role, ending, carried, context = null, why = \"\", spoke = false }) {\n  seatChanged(instance, seat);",
        "to": "function afterClose(instance, { seat, role, ending, carried, context = null, why = \"\", spoke = false }) {"
      }
    ]
  },
  {
    "name": "the Leader's close is kept from the page",
    "catches": "the Leader's close is told like anybody's, and nothing follows it for the Leader",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "function afterClose(instance, { seat, role, ending, carried, context = null, why = \"\", spoke = false }) {\n  seatChanged(instance, seat);",
        "to": "function afterClose(instance, { seat, role, ending, carried, context = null, why = \"\", spoke = false }) {\n  if (role === WORKER) {\n    seatChanged(instance, seat);\n  }"
      }
    ]
  },
  {
    "name": "an interrupted turn is shown as a reply",
    "catches": "the page's stop glyph interrupts the turn, and the row says so rather than the seat",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "      append(root, seat, { from: seat, text: reply.text, interrupted: true });",
        "to": "      append(root, seat, { from: seat, text: reply.text });"
      }
    ]
  },
  {
    "name": "a call stop is parked and the page is not told",
    "catches": "a call stop is pushed as it is parked, and taken down as it is answered",
    "edits": [
      {
        "file": "lib/chat/permissions.mjs",
        "from": "    forSession(name).set(request.id, { request, answer, parkedAt: now, told: false });\n    asking(name);",
        "to": "    forSession(name).set(request.id, { request, answer, parkedAt: now, told: false });"
      }
    ]
  },
  {
    "name": "a call stop answered stays on the page",
    "catches": "a call stop is pushed as it is parked, and taken down as it is answered",
    "edits": [
      {
        "file": "lib/chat/permissions.mjs",
        "from": "  held.answer(decision);\n  asking(name);\n  return true;",
        "to": "  held.answer(decision);\n  return true;"
      }
    ]
  },
  {
    "name": "the stream takes the bearer too",
    "catches": "opens to the page secret on its query, and to nothing else — not even the bearer",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  const carried = url.pathname === EVENTS_ROUTE ? url.searchParams.get(\"page\") : bearer(request.headers.authorization);",
        "to": "  const carried = (url.pathname === EVENTS_ROUTE ? url.searchParams.get(\"page\") : null) ?? bearer(request.headers.authorization);"
      }
    ]
  },
  {
    "name": "the page installs in a browser tab",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "lib/chat/manifest.webmanifest",
        "from": "  \"display\": \"standalone\",",
        "to": "  \"display\": \"browser\","
      }
    ]
  },
  {
    "name": "the maskable icon loses its purpose",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "lib/chat/manifest.webmanifest",
        "from": "{ \"src\": \"/icons/512-maskable.png\", \"sizes\": \"512x512\", \"type\": \"image/png\", \"purpose\": \"maskable\" }",
        "to": "{ \"src\": \"/icons/512-maskable.png\", \"sizes\": \"512x512\", \"type\": \"image/png\" }"
      }
    ]
  },
  {
    "name": "the maskable icon is named but not served",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  \"/icons/512-maskable.png\": { file: path.join(HERE, \"icons\", \"512-maskable.png\"), type: \"image/png\" },\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the asking icon is not served",
    "catches": "serves the asking icon the tab shows while a panel asks, a 192 PNG",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  \"/icons/192-asking.png\": { file: path.join(HERE, \"icons\", \"192-asking.png\"), type: \"image/png\" },\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the asking icon is served as the wrong type",
    "catches": "serves the asking icon the tab shows while a panel asks, a 192 PNG",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  \"/icons/192-asking.png\": { file: path.join(HERE, \"icons\", \"192-asking.png\"), type: \"image/png\" },",
        "to": "  \"/icons/192-asking.png\": { file: path.join(HERE, \"icons\", \"192-asking.png\"), type: \"application/octet-stream\" },"
      }
    ]
  },
  {
    "name": "the manifest says a size the icon is not",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "lib/chat/manifest.webmanifest",
        "from": "{ \"src\": \"/icons/512-maskable.png\", \"sizes\": \"512x512\"",
        "to": "{ \"src\": \"/icons/512-maskable.png\", \"sizes\": \"256x256\""
      }
    ]
  },
  {
    "name": "the maskable icon is served as the wrong type",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  \"/icons/512-maskable.png\": { file: path.join(HERE, \"icons\", \"512-maskable.png\"), type: \"image/png\" },",
        "to": "  \"/icons/512-maskable.png\": { file: path.join(HERE, \"icons\", \"512-maskable.png\"), type: \"application/octet-stream\" },"
      }
    ]
  },
  {
    "name": "a lifecycle button on the page",
    "catches": "carries no lifecycle button and no lifecycle word, and the stop glyph is its one lifecycle control",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "<div id=\"stage\">",
        "to": "<button>Go online</button>\n<div id=\"stage\">"
      }
    ]
  },
  {
    "name": "a second button label in the script",
    "catches": "carries no lifecycle button and no lifecycle word, and the stop glyph is its one lifecycle control",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "    composer.append(box, stop);",
        "to": "    const send = document.createElement(\"button\");\n    send.type = \"submit\";\n    send.textContent = \"Send\";\n    composer.append(box, stop, send);"
      }
    ]
  },
  {
    "name": "the page says a session is on its way out",
    "catches": "never reads how a process is going: no ending on the page or in panels.mjs, no running read on the page",
    "edits": [
      {
        "file": "lib/chat/panels.mjs",
        "from": "  panel.busy = about.busy === true;\n  panel.running",
        "to": "  panel.busy = about.busy === true;\n  panel.title = about.ending === null ? panel.title : `${about.ending}`;\n  panel.running"
      }
    ]
  },
  {
    "name": "the page loads the parser from a CDN",
    "catches": "loads nothing from outside the server and prompts for nothing of its own",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "<title>OpenOv AI</title>",
        "to": "<title>OpenOv AI</title>\n<script src=\"https://cdn.jsdelivr.net/npm/marked\"></script>"
      }
    ]
  },
  {
    "name": "the page asks to notify",
    "catches": "loads nothing from outside the server and prompts for nothing of its own",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  connect();\n  setInterval(",
        "to": "  connect();\n  Notification.requestPermission();\n  setInterval("
      }
    ]
  },
  {
    "name": "the placement is inlined on the page",
    "catches": "draws from the two tested modules and the dialog module",
    "edits": [
      {
        "file": "lib/chat/page.html",
        "from": "  import { AMBER, CARD, CONNECTED, DISCONNECTED, GREEN, RED, REPLY, advance, anyAsking, applyEvent, composersEnabled, delivery, dot, following, fresh, head, keyAction, noticed, place, prune, quotaLine, quotaTitle, reference, spellReferences, stopEnabled, title, typingBeat } from \"./panels.mjs\";",
        "to": "  import { AMBER, CARD, CONNECTED, DISCONNECTED, GREEN, RED, REPLY, advance, anyAsking, applyEvent, composersEnabled, delivery, dot, following, fresh, head, keyAction, noticed, prune, quotaLine, quotaTitle, reference, spellReferences, stopEnabled, title, typingBeat } from \"./panels.mjs\";\n  const place = (seats, leader, order) => ({ left: order.filter((s) => s !== leader && seats.includes(s)), mid: leader, right: [] });"
      }
    ]
  },
  {
    "name": "a since on the rows is ignored",
    "catches": "answers a panel's rows from an index on",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  return Number.isInteger(from) && from > 0 ? rows.slice(from) : rows;",
        "to": "  return rows;"
      }
    ]
  },
  {
    "name": "the instance stops and the page is not told",
    "catches": "the page is told the instance is stopping, and every page route answers 503 from then on",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "  instance.stopping = true;\n  publish(\"stopping\", {});",
        "to": "  instance.stopping = true;"
      }
    ]
  },
  {
    "name": "the parser is served from somewhere else",
    "catches": "serves each module and the parser open, as the file, and nothing else on those routes",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  \"/marked.mjs\": { file: path.join(HERE, \"marked.mjs\"), type: \"text/javascript; charset=utf-8\" },",
        "to": "  \"/marked.mjs\": { file: path.join(HERE, \"render.mjs\"), type: \"text/javascript; charset=utf-8\" },"
      }
    ]
  },
  {
    "name": "a GET on an issued door is refused like a stranger",
    "catches": "answers GET on the door it issued with 405, and GET on an unknown one with the same 401",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    sendJson(response, 405, { error: \"method not allowed\" }, { allow: \"POST\" });",
        "to": "    sendJson(response, 401, UNKNOWN);"
      }
    ]
  },
  {
    "name": "the log masks the secret on the tool route only",
    "catches": "says nothing about a request, and never a secret where an answer names the path",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  return pathname.replace(SECRET_IN_PATH, \"/mcp/<secret>\");",
        "to": "  return TOOL_ROUTE.test(pathname) ? \"/mcp/<secret>\" : pathname;"
      }
    ]
  },
  {
    "name": "a tool line is written on the Leader's panel too",
    "catches": "writes no line for the Leader's own calls",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    called: logged(instance, seat, roleOf(instance, seat) === WORKER ? calledBy(instance, seat) : doingBy(instance, seat)),",
        "to": "    called: logged(instance, seat, calledBy(instance, seat)),"
      }
    ]
  },
  {
    "name": "a failed call leaves its line as it was: the amend is gone",
    "catches": "marks the line of a call that failed, in the file and on the stream, with the first line its result said as the reason",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "      if (roleOf(instance, seat) === WORKER) {\n        amend(instance.root, seat, id, why);\n      }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the amend tells the page but keeps the entry as it was on disk",
    "catches": "marks the line of a call that failed, in the file and on the stream, with the first line its result said as the reason",
    "edits": [
      {
        "file": "lib/chat/conversation.mjs",
        "from": "  if (why !== \"\") {\n    messages[index].why = mask(why);\n  }\n  fs.writeFileSync(file(root, session), `${JSON.stringify(messages, null, 2)}\\n`);\n",
        "to": "  if (why !== \"\") {\n    messages[index].why = mask(why);\n  }\n"
      }
    ]
  },
  {
    "name": "a subagent's calls draw as the seat's own: the parent check is gone",
    "catches": "draws nothing for a call made under another call",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "        if ((frame.parent_tool_use_id ?? null) === null) {",
        "to": "        {"
      }
    ]
  },
  {
    "name": "a line is written for every call, the silent ones included",
    "catches": "draws nothing for a search of the tool list or a session's own stop",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    if (text !== null) {\n      append(instance.root, seat, { from: seat, line: text, call: id });\n    }\n",
        "to": "    append(instance.root, seat, { from: seat, line: text, call: id });\n"
      }
    ]
  },
  {
    "name": "the caller's panel gets nothing for a message it sent",
    "catches": "writes the call on the caller's panel with its outcome, under the id the addressee's row carries",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        append(root, caller.seat, { from: caller.seat, to, text, outcome: \"sent\", msg, ...marked });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a message nobody could take is recorded as sent",
    "catches": "records a message nobody could take as not sent, on the caller's panel only",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "{ from: caller.seat, to, text, outcome: \"not sent\", why: `nobody called ${to} works here` }",
        "to": "{ from: caller.seat, to, text, outcome: \"sent\", why: `nobody called ${to} works here` }"
      }
    ]
  },
  {
    "name": "a message to a seat without a process is recorded as sent",
    "catches": "a message to a seat without a process is not sent, and says so on the caller's panel",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "{ from: caller.seat, to, text, outcome: \"not sent\", why: `${to} has no process` }",
        "to": "{ from: caller.seat, to, text, outcome: \"sent\", why: `${to} has no process` }"
      }
    ]
  },
  {
    "name": "the receipt loses its addressee",
    "catches": "comes back at once, with nothing of the reply in it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        append(root, to, { from: caller.seat, to, text, msg, ...marked });\n",
        "to": "        append(root, to, { from: caller.seat, text, msg, ...marked });\n"
      }
    ]
  },
  {
    "name": "a message to yourself leaves no row on the caller's log",
    "catches": "records a refused message on the caller's log with the reason",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "          append(root, caller.seat, { from: caller.seat, to, text, outcome: \"refused\", why: \"that is you\" });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the title's instance is the root as it is",
    "catches": "tells the page the instance as the title says it, and the usage windows as they stand",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  return root === home || root.startsWith(`${home}${path.sep}`) ? `~${root.slice(home.length)}` : root;",
        "to": "  return root;"
      }
    ]
  },
  {
    "name": "a root that merely begins with the home's letters is shortened",
    "catches": "tells the page the instance as the title says it, and the usage windows as they stand",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  return root === home || root.startsWith(`${home}${path.sep}`) ? `~${root.slice(home.length)}` : root;",
        "to": "  return root.startsWith(home) ? `~${root.slice(home.length)}` : root;"
      }
    ]
  },
  {
    "name": "the snapshot carries no usage windows",
    "catches": "tells the page the instance as the title says it, and the usage windows as they stand",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    quota: usage.reading(),\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the settings document goes back under Claude Code's directory",
    "catches": "hands the run the list of what is not to be read, as a settings document of its own",
    "edits": [
      {
        "file": "lib/instructions.mjs",
        "from": "const FILE = \"instructions.json\";",
        "to": "const FILE = path.join(\".local\", \"instructions.json\");"
      }
    ]
  },
  {
    "name": "the two ends of a message carry different ids",
    "catches": "writes the call on the caller's panel with its outcome, under the id the addressee's row carries",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        append(root, to, { from: caller.seat, to, text, msg, ...marked });\n",
        "to": "        append(root, to, { from: caller.seat, to, text, msg: crypto.randomUUID(), ...marked });\n"
      }
    ]
  },
  {
    "name": "a failed call is marked with no reason",
    "catches": "marks the line of a call that failed, in the file and on the stream, with the first line its result said as the reason",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "            failed({ id: block.tool_use_id, why: firstLineOf(block.content) });",
        "to": "            failed({ id: block.tool_use_id, why: \"\" });"
      }
    ]
  },
  {
    "name": "the reason is the whole result, blank lines and all",
    "catches": "marks the line of a call that failed, in the file and on the stream, with the first line its result said as the reason",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  const line = text.split(\"\\n\").map((one) => one.trim()).find((one) => one !== \"\") ?? \"\";",
        "to": "  const line = text.split(\"\\n\")[0] ?? \"\";"
      }
    ]
  },
  {
    "name": "the write says nothing",
    "catches": "says when a frame is written: at once on an idle seat, after the turn under way on a busy one, before the answer",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  record.turn.written();\n",
        "to": ""
      }
    ]
  },
  {
    "name": "written at the tell, before any write",
    "catches": "says when a frame is written: at once on an idle seat, after the turn under way on a busy one, before the answer",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    const turn = { frame, resolve, ask, written, order: arrival(), at: record.clock() };",
        "to": "    const turn = { frame, resolve, ask, written, order: arrival(), at: record.clock() };\n    written();"
      }
    ]
  },
  {
    "name": "the User's row is marked delivered as it is typed, before its frame went in",
    "catches": "a row typed behind a turn under way is told again as delivered only once that turn is over",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  const told = deliver(instance, seat, userFrame(text), { written: () => delivered(root, seat, index) });",
        "to": "  delivered(root, seat, index);\n  const told = deliver(instance, seat, userFrame(text));"
      }
    ]
  },
  {
    "name": "the User's row is never marked delivered",
    "catches": "lands on the seat's panel as the User's row, with the reply under it",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  const told = deliver(instance, seat, userFrame(text), { written: () => delivered(root, seat, index) });",
        "to": "  const told = deliver(instance, seat, userFrame(text));"
      }
    ]
  },
  {
    "name": "the delivered mark goes to the file and the page is not told",
    "catches": "a new row reaches the page without a poll",
    "edits": [
      {
        "file": "lib/chat/conversation.mjs",
        "from": "  messages[index].delivered = true;\n  messages[index].deliveredAt = new Date().toISOString();\n  fs.writeFileSync(file(root, session), `${JSON.stringify(messages, null, 2)}\\n`);\n  publish(\"row\", { seat: session, index, row: messages[index] });",
        "to": "  messages[index].delivered = true;\n  messages[index].deliveredAt = new Date().toISOString();\n  fs.writeFileSync(file(root, session), `${JSON.stringify(messages, null, 2)}\\n`);"
      }
    ]
  },
  {
    "name": "a frame to a running seat goes without its word",
    "catches": "lands on the seat's panel as the User's row, with the reply under it",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "  const told = tell(seat, frame, { ask, written });",
        "to": "  const told = tell(seat, frame, { ask });"
      }
    ]
  },
  {
    "name": "a frame is queued without a word in the log",
    "catches": "says in the log what was queued and what was written, by arrival, with how many wait",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  log(\"queued\", seat, null, `${frame.kind} #${order}, ${record.queue.length} waiting`);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the write line counts the frame just written among the waiting",
    "catches": "says in the log what was queued and what was written, by arrival, with how many wait",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  log(\"wrote\", record.seat, null, `${describe(record.turn)}, ${record.queue.length} waiting${released(record, passing)}`);",
        "to": "  log(\"wrote\", record.seat, null, `${describe(record.turn)}, ${record.queue.length + 1} waiting${released(record, passing)}`);"
      }
    ]
  },
  {
    "name": "a frame behind an open turn is held without a word in the log",
    "catches": "says in the log why a frame was not written: the turn open since when, and how many wait",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    log(\"unwritten\", record.seat, null, `turn open since ${new Date(record.turnBegan).toISOString()}, ${record.queue.length} waiting`);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a seat on its way out takes no turn and says nothing",
    "catches": "says in the log that a seat on its way out took no new turn, and what it was ending as",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    log(\"unwritten\", record.seat, null, `ending=${record.ending}, ${record.queue.length} waiting`);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the interrupt goes in without a word in the log",
    "catches": "says in the log that the interrupt was written, and how long the run took to answer it",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  log(\"interrupt\", seat, null, `written, turn ${describe(turn)}`);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "an answered interrupt is logged as one that was never answered",
    "catches": "says in the log that the interrupt was written, and how long the run took to answer it",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "said.then(() => { heard = true; })",
        "to": "said"
      }
    ]
  },
  {
    "name": "an unanswered interrupt is logged as answered at the patience",
    "catches": "says in the log when the run never answered the interrupt and the seat was freed anyway",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "`no result after ${patience} ms — seat freed`",
        "to": "`result in ${patience} ms`"
      }
    ]
  },
  {
    "name": "the line after a stop says nothing waits, whatever does",
    "catches": "a stop says on the panel how many wait and what goes in, as one, with when each was told",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        append(instance.root, seat, { from: SERVER, text: stoppedLine(waiting(seat)) });",
        "to": "        append(instance.root, seat, { from: SERVER, text: stoppedLine({ count: 0, next: null }) });"
      }
    ]
  },
  {
    "name": "the queue is read only after the next frame has gone in",
    "catches": "a stop says on the panel how many wait and what goes in, as one, with when each was told",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "      const interrupted = await interrupt(seat, { thenDrain: false });",
        "to": "      const interrupted = await interrupt(seat);"
      }
    ]
  },
  {
    "name": "a message frame forgets who it is from",
    "catches": "a stop says on the panel how many wait and what goes in, as one, with when each was told",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "`<message from=\"${token(\"from\", from)}\"${marked}>${neutralise(text)}</message>`, null, from, urgent === true);",
        "to": "`<message from=\"${token(\"from\", from)}\"${marked}>${neutralise(text)}</message>`, null, null, urgent === true);"
      }
    ]
  },
  {
    "name": "a turn is queued without the moment it was told",
    "catches": "a stop says on the panel how many wait and what goes in, as one, with when each was told",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    const turn = { frame, resolve, ask, written, order: arrival(), at: record.clock() };",
        "to": "    const turn = { frame, resolve, ask, written, order: arrival() };"
      }
    ]
  },
  {
    "name": "a stop with nothing to stop is silent on the panel",
    "catches": "a stop with no turn under way says so on the panel and in the log, and answers interrupted false",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        append(instance.root, seat, { from: SERVER, text: \"No turn to stop\" });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a stop with nothing to stop is silent in the log",
    "catches": "a stop with no turn under way says so on the panel and in the log, and answers interrupted false",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        log(\"stop\", seat, null, \"No turn to stop\");\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a permission request is parked without a word in the log",
    "catches": "a permission wait is logged as asked and answered, and one answered at once draws no row",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    log.log(\"asked\", seat, null, what);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the answer to a permission request is logged before anybody gave it",
    "catches": "a permission wait is logged as asked and answered, and one answered at once draws no row",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    waiting.then((decision) => {\n      const seconds = Math.round((clock() - began) / 1000);\n      log.log(\"answered\", seat, null, `${decision.behavior} after ${seconds} s`);",
        "to": "    log.log(\"answered\", seat, null, \"allow after 0 s\");\n    waiting.then((decision) => {\n      const seconds = Math.round((clock() - began) / 1000);"
      }
    ]
  },
  {
    "name": "the panel is told of a wait that was no wait",
    "catches": "a permission wait is logged as asked and answered, and one answered at once draws no row",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "      if (seconds >= WAIT_SAID_FROM) append(instance.root, seat, { from: SERVER, text: `Waited ${seconds} seconds for permission: ${what}` });\n",
        "to": "      append(instance.root, seat, { from: SERVER, text: `Waited ${seconds} seconds for permission: ${what}` });\n"
      }
    ]
  },
  {
    "name": "only a Worker's calls reach the log",
    "catches": "logs every seat's calls, the Leader's included, and a failed one with its reason under the call's id and name",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    called: logged(instance, seat, roleOf(instance, seat) === WORKER ? calledBy(instance, seat) : doingBy(instance, seat)),",
        "to": "    called: roleOf(instance, seat) === WORKER ? logged(instance, seat, calledBy(instance, seat)) : doingBy(instance, seat),"
      }
    ]
  },
  {
    "name": "a failed call is marked on the panel and said nowhere else",
    "catches": "logs every seat's calls, the Leader's included, and a failed one with its reason under the call's id and name",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "      log.failed(seat, { id, why });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a call that came back is never told to the log, so a failed one carries no name",
    "catches": "logs every seat's calls, the Leader's included, and a failed one with its reason under the call's id and name",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    returned: backFrom(seat, roleOf(instance, seat) === WORKER ? () => {} : returnedBy(instance, seat)),",
        "to": "    returned: roleOf(instance, seat) === WORKER ? () => {} : returnedBy(instance, seat),"
      },
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    log.called(seat, { id: call.id, name: call.name, what: named(call.name, call.input) });",
        "to": "    log.log(\"called\", seat, call.id, named(call.name, call.input));"
      }
    ]
  },
  {
    "name": "a call reaches the tool whatever its arguments are",
    "catches": "refuses a call whose arguments do not fit what the tool declared, in one line naming the argument, before the tool sees it",
    "edits": [
      {
        "file": "lib/chat/mcp.mjs",
        "from": "    const unfit = misfit(wanted.inputSchema, args);",
        "to": "    const unfit = null;"
      }
    ]
  },
  {
    "name": "the Leader's turn starts with no word: nothing is said until its first call",
    "catches": "is told on the seat at the turn, at each call and at each result, and gone with the turn — a search the line says nothing about leaves it as it was",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "  } else if (record.doing === null) {\n    record.doing = THINKING;",
        "to": "  } else if (false) {\n    record.doing = THINKING;"
      }
    ]
  },
  {
    "name": "the word outlives the turn",
    "catches": "is told on the seat at the turn, at each call and at each result, and gone with the turn — a search the line says nothing about leaves it as it was",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "  if (record.turn === null) {\n    record.doing = null;",
        "to": "  if (false) {\n    record.doing = null;"
      }
    ]
  },
  {
    "name": "a call's result leaves the word as the call",
    "catches": "is told on the seat at the turn, at each call and at each result, and gone with the turn — a search the line says nothing about leaves it as it was",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    if (record !== undefined && record.lastCall === id && record.doing !== THINKING) {",
        "to": "    if (record !== undefined && record.lastCall === null && record.doing !== THINKING) {"
      }
    ]
  },
  {
    "name": "a call the line says nothing about clears the word",
    "catches": "is told on the seat at the turn, at each call and at each result, and gone with the turn — a search the line says nothing about leaves it as it was",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "    const text = line(name, input);\n    if (text !== null) {\n      record.doing = text;",
        "to": "    const text = line(name, input);\n    if (true) {\n      record.doing = text;"
      }
    ]
  },
  {
    "name": "a Worker's seat says what it is at too",
    "catches": "is on the snapshot of a page opened mid-turn, and the Worker's seat never carries it",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "      if (roleOf(instance, seat) !== WORKER) {\n        atTurn(record);",
        "to": "      if (true) {\n        atTurn(record);"
      }
    ]
  },
  {
    "name": "a result comes back without the id of its call",
    "catches": "is told on the seat at the turn, at each call and at each result, and gone with the turn — a search the line says nothing about leaves it as it was",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "            returned({ id: block.tool_use_id });",
        "to": "            returned({ id: null });"
      }
    ]
  },
  {
    "name": "a session is started as whatever claude the PATH has, and a missing one is an ENOENT",
    "catches": "refuses to start a seat when the claude this instance runs on is not there, saying what fetches it",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "const child = spawn(claudeCommand(instance.root), args, {",
        "to": "const child = spawn(\"claude\", args, {"
      }
    ]
  },
  {
    "name": "a Leader that cannot be spawned is a thrown error rather than a refusal",
    "catches": "refuses a message to a Leader it cannot spawn, saying what fetches its claude",
    "edits": [
      {
        "file": "lib/chat/lifecycle.mjs",
        "from": "      if (!(error instanceof RuntimeError)) throw error;\n      return { refused: error.message };",
        "to": "      throw error;"
      }
    ]
  },
  {
    "name": "the row for a Leader that cannot be spawned says it has no process",
    "catches": "refuses a message to a Leader it cannot spawn, saying what fetches its claude",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "text: told.refused === \"no process\" ? `${seat} has no process` : told.refused, failed: true",
        "to": "text: `${seat} has no process`, failed: true"
      }
    ]
  },
  {
    "name": "what waited behind a turn goes in one at a time",
    "catches": "writes a frame told to an idle seat as a queue of one, what waited behind it as one queue, and answers every one of them with the one reply",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  record.turn = queueOf(passing);",
        "to": "  record.turn = queueOf([passing[0]]);\n  record.queue = passing.slice(1);"
      }
    ]
  },
  {
    "name": "a queue answers only the last turn in it",
    "catches": "writes a frame told to an idle seat as a queue of one, what waited behind it as one queue, and answers every one of them with the one reply",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    at: turns[0].at,\n    resolve: (reply) => turns.forEach((turn) => turn.resolve(reply)),",
        "to": "    at: turns[0].at,\n    resolve: (reply) => turns.at(-1).resolve(reply),"
      }
    ]
  },
  {
    "name": "the server's events are put on top when several go in",
    "catches": "keeps the batch in arrival order, every kind alike, a server event in its place among the rest",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    frame: queueFrame(turns.map((turn) => ({ frame: turn.frame, at: turn.at }))),",
        "to": "    frame: queueFrame([...turns.filter((turn) => turn.frame.kind === \"server-event\"), ...turns.filter((turn) => turn.frame.kind !== \"server-event\")].map((turn) => ({ frame: turn.frame, at: turn.at }))),"
      }
    ]
  },
  {
    "name": "the User's line jumps the queue",
    "catches": "puts what the User types into the batch where it arrived, behind a message told before it",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    record.queue.push(turn);",
        "to": "    if (frame.kind === \"user\") record.queue.unshift(turn); else record.queue.push(turn);"
      }
    ]
  },
  {
    "name": "the log numbers a queue by its first alone",
    "catches": "says in the log that several went in as one queue, numbered by the first and the last of them, each named",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "`#${turn.order}..#${turn.last}`",
        "to": "`#${turn.order}`"
      }
    ]
  },
  {
    "name": "what waits is named with the server's events first, not as it goes in",
    "catches": "a stop says on the panel how many wait and what goes in, as one, with when each was told",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  return queue.map((turn) =>",
        "to": "  return [...queue.filter((turn) => turn.frame.kind === \"server-event\"), ...queue.filter((turn) => turn.frame.kind !== \"server-event\")].map((turn) =>"
      }
    ]
  },
  {
    "name": "the stop line names only the first of several",
    "catches": "a stop says on the panel how many wait and what goes in, as one, with when each was told",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  return waits.length === 1 ? `Stopped; 1 waiting, next: ${named[0]}` : `Stopped; ${waits.length} waiting, going in as one: ${named.join(\", \")}`;",
        "to": "  return `Stopped; ${waits.length} waiting, next: ${named[0]}`;"
      }
    ]
  },
  {
    "name": "the next frame is written without waiting for the result",
    "catches": "writes a frame told to an idle seat as a queue of one, what waited behind it as one queue, and answers every one of them with the one reply",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  if (record.turn !== null) {\n    if (joined(record)) {\n      return;\n    }\n    log(\"unwritten\", record.seat, null, `turn open since ${new Date(record.turnBegan).toISOString()}, ${record.queue.length} waiting`);\n    return;\n  }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a frame alone goes in bare, without the envelope",
    "catches": "writes a frame told to an idle seat as a queue of one, what waited behind it as one queue, and answers every one of them with the one reply",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  const inner = items.map(",
        "to": "  if (items.length === 1) return items[0].frame;\n  const inner = items.map("
      }
    ]
  },
  {
    "name": "what a session left running is left running",
    "catches": "ends with its session, however the session ended, and is said in the log",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "    record.leftovers = endLeftovers(instance.root, seat).then((found) => {\n      if (found.length > 0) {\n        log(\"leftovers\", seat, null, `ended ${found.map(({ pid, command }) => `${command} (pid ${pid})`).join(\"; \")}`);\n      }\n    });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a session is started without the seat it is",
    "catches": "is told its seat, in its environment",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "environment(instance.root, instance.config.auth, seat)",
        "to": "environment(instance.root, instance.config.auth)"
      }
    ]
  },
  {
    "name": "a seat is started with the claude.ai account's connectors",
    "catches": "keeps the claude.ai account's connectors out of every start of a seat",
    "edits": [
      {
        "file": "lib/claude.mjs",
        "from": "    env.ENABLE_CLAUDEAI_MCP_SERVERS = \"false\";\n",
        "to": ""
      }
    ]
  },
  {
    "name": "every frame of a turn that stopped gets its own row",
    "catches": "says a turn of several frames stopped once, not once a frame",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "    if (shownEndings.has(reply)) {\n      return;\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a first turn dead at the service leaves the seat sitting",
    "catches": "ends a seat whose first turn came back an error from the service, saying what the service said",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "          record.ending = \"first-turn-failed\";\n          record.diedOf = answer.text;\n          end(seat);\n          return;\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a turn dead at the service ends the seat whether or not it has ever answered",
    "catches": "keeps a seat whose later turn came back an error: the words are logged and the seat lives",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "        if (dead && !record.spoke) {",
        "to": "        if (dead) {"
      }
    ]
  },
  {
    "name": "done is offered to the Leader too",
    "catches": "serves the Leader exactly what BUILT_IN names but done, and a Worker message, room, index, validate, write_desk and done",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "  done: [WORKER],",
        "to": "  done: [LEADER, WORKER],"
      }
    ]
  },
  {
    "name": "an urgent message waits for the turn's end like any other",
    "catches": "joins the Worker's turn at its call, with what was queued ahead of it, and one result answers them all",
    "edits": [
      {
        "file": "lib/chat/session.mjs",
        "from": "  return frame.kind === \"user\" || frame.urgent === true;",
        "to": "  return frame.kind === \"user\";"
      }
    ]
  },
  {
    "name": "a Worker may send an urgent message",
    "catches": "refuses a Worker's and one that is not true or false",
    "edits": [
      {
        "file": "lib/chat/server.mjs",
        "from": "        if (urgent !== undefined && caller.role !== LEADER) {",
        "to": "        if (false) {"
      }
    ]
  },
  {
    "name": "an urgent message is not marked for the Worker",
    "catches": "joins the Worker's turn at its call, with what was queued ahead of it, and one result answers them all",
    "edits": [
      {
        "file": "lib/chat/frames.mjs",
        "from": "  const marked = urgent === true ? ` urgent=\"true\"` : \"\";",
        "to": "  const marked = \"\";"
      }
    ]
  }
]

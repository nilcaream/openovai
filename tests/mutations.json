[
  {
    "name": "the page goes out with its empty tag",
    "catches": "serves the page with its secret in the one tag that carries it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const page = Buffer.from(template.replace(SECRET_TAG, `<meta name=\"openovai-secret\" content=\"${pageSecret()}\">`));",
        "to": "  const page = Buffer.from(template);"
      }
    ]
  },
  {
    "name": "the health says nothing about the port",
    "catches": "says in its health which instance it serves, on which port, at which version",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    sendJson(response, 200, { instance: instance.root, port, version: version(instance.root) ?? \"unknown\" });",
        "to": "    sendJson(response, 200, { instance: instance.root, version: version(instance.root) ?? \"unknown\" });"
      }
    ]
  },
  {
    "name": "the chat never records where it listens",
    "catches": "records where it is listening, for whoever starts a session",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      record(instance.root, port);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the page is told a made-up User",
    "catches": "tells the page who the User and the Leader are, and every seat with its role and model",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    user: instance.config.user,\n    leader: instance.config.leader,\n    // Who a row",
        "to": "    user: \"User\",\n    leader: instance.config.leader,\n    // Who a row"
      }
    ]
  },
  {
    "name": "the Leader is listed last",
    "catches": "puts the Leader first and the rest in name order",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return [leader, ...rest].map((name) => ({",
        "to": "  return [...rest, leader].map((name) => ({"
      }
    ]
  },
  {
    "name": "a panel opens for anybody at all",
    "catches": "will not open a panel for somebody who does not work here",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    if (!isSeat(instance, seat)) {\n      sendJson(response, 404,",
        "to": "    if (false) {\n      sendJson(response, 404,"
      }
    ]
  },
  {
    "name": "the roster takes any directory under work/, whatever it is called",
    "catches": "is not on the roster",
    "edits": [
      {
        "file": "tools/desks.mjs",
        "from": "      .filter((entry) => entry.isDirectory() && isName(entry.name))",
        "to": "      .filter((entry) => entry.isDirectory())"
      }
    ]
  },
  {
    "name": "the page gate takes any bearer at all",
    "catches": "answers an unknown secret with one body, whatever was tried",
    "edits": [
      {
        "file": "tools/chat/secrets.mjs",
        "from": "  return given.length === expected.length && crypto.timingSafeEqual(given, expected);",
        "to": "  return presented !== \"\";"
      }
    ]
  },
  {
    "name": "an unknown secret on the tool route falls through to the routes below",
    "catches": "answers an unknown secret with one body, whatever was tried",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (who === null) {\n    sendJson(response, 401, UNKNOWN);\n    return;\n  }\n\n  if (request.method === \"POST\" && TOOL_ROUTE.test(url.pathname)) {",
        "to": "  if (who === null && !TOOL_ROUTE.test(url.pathname)) {\n    sendJson(response, 401, UNKNOWN);\n    return;\n  }\n\n  if (who !== null && request.method === \"POST\" && TOOL_ROUTE.test(url.pathname)) {"
      }
    ]
  },
  {
    "name": "the page secret resolves to a seat on the tool route",
    "catches": "never lets one kind of secret open the other side",
    "edits": [
      {
        "file": "tools/chat/secrets.mjs",
        "from": "  const known = issued.get(secret);\n  return known === undefined ? null : { seat: known.seat, role: known.role };",
        "to": "  const known = issued.get(secret);\n  if (known === undefined && secret === page) {\n    return { seat: \"page\", role: \"Leader\" };\n  }\n  return known === undefined ? null : { seat: known.seat, role: known.role };"
      }
    ]
  },
  {
    "name": "a session's secret opens the page routes",
    "catches": "never lets one kind of secret open the other side",
    "edits": [
      {
        "file": "tools/chat/secrets.mjs",
        "from": "  if (typeof presented !== \"string\") {\n    return false;\n  }\n  const expected",
        "to": "  if (typeof presented !== \"string\") {\n    return false;\n  }\n  if (issued.has(presented)) {\n    return true;\n  }\n  const expected"
      }
    ]
  },
  {
    "name": "a secret outlives its process",
    "catches": "forgets a secret when its process has gone, and the next process gets another",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    revoke(secret);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the secret is written under the instance when it is issued",
    "catches": "writes no secret anywhere under the instance",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  const secret = issue(seat, role);\n",
        "to": "  const secret = issue(seat, role);\n  fs.writeFileSync(path.join(instance.root, \"chat\", \"secret.txt\"), secret);\n"
      }
    ]
  },
  {
    "name": "every process is given the same secret",
    "catches": "mints one secret per process, 43 characters of base64url, and no two alike",
    "edits": [
      {
        "file": "tools/chat/secrets.mjs",
        "from": "  return crypto.randomBytes(32).toString(\"base64url\");",
        "to": "  return \"s\".repeat(43);"
      }
    ]
  },
  {
    "name": "the literal secret is written into the command line",
    "catches": "hands the process its secret in the environment and never on the command line",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    args.push(\"--mcp-config\", tools);",
        "to": "    args.push(\"--mcp-config\", tools.replace(`\\${${SECRET_IN_ENVIRONMENT}}`, secret));"
      }
    ]
  },
  {
    "name": "the process is not handed its secret at all",
    "catches": "opens the door to the secret it issued",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    env: { ...environment(instance.root, instance.config.auth), [SECRET_IN_ENVIRONMENT]: secret },",
        "to": "    env: { ...environment(instance.root, instance.config.auth) },"
      }
    ]
  },
  {
    "name": "a colleague is waited for as long as Claude Code's default",
    "catches": "points the process at this chat, with a whole turn to wait on a colleague",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "        timeout: A_WHOLE_TURN,\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the run is not verbose, so nothing but the result would come back",
    "catches": "runs it in print mode over the streaming protocol, asking here before using a tool",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    \"--verbose\",\n",
        "to": ""
      }
    ]
  },
  {
    "name": "everybody runs on the worker model",
    "catches": "runs each seat on the model its desk resolves to",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    modelFor(instance.root, seat, instance.config),",
        "to": "    instance.config.models.worker,"
      }
    ]
  },
  {
    "name": "the persona goes out without the hard rules",
    "catches": "renders the persona at every start, with the hard rules after it",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  const { text, version } = withHardRules(persona(instance.root, seat, instance.config), instance.root, roleOf(instance, seat));",
        "to": "  const { version } = withHardRules(persona(instance.root, seat, instance.config), instance.root, roleOf(instance, seat));\n  const text = persona(instance.root, seat, instance.config);"
      }
    ]
  },
  {
    "name": "the run is started without the list of what is not to be read",
    "catches": "hands the run the list of what is not to be read, as a settings document of its own",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  args.push(\"--settings\", ownInstructions(instance.root));\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the process starts wherever the chat happens to be",
    "catches": "starts the process in the instance, on the instance's own home",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    cwd: instance.root,\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a seat's role is read from the configuration on every call",
    "catches": "fixes the role at the start: who leads is read once, from the configuration then",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return processes.get(seat)?.role ?? roleOf(instance, seat);",
        "to": "  return roleOf(instance, seat);"
      }
    ]
  },
  {
    "name": "a seat can be started twice",
    "catches": "refuses to start a seat that is already running, or one that does not exist",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  if (processes.has(seat)) {\n    throw new Error(`${seat} is already running`);\n  }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a missing Claude Code is reported in the system's words",
    "catches": "says so when Claude Code is not on the PATH, and the seat is not running",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    gone(error.code === \"ENOENT\" ? \"Claude Code is not on the PATH of the process serving this page\" : error.message);",
        "to": "    gone(error.message);"
      }
    ]
  },
  {
    "name": "tell takes anything shaped like a frame",
    "catches": "takes frames from frames.mjs and nothing else, however much a string looks like one",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "export function tell(seat, frame, { ahead = false, ask = null } = {}) {\n  if (!isFrame(frame)) {",
        "to": "export function tell(seat, frame, { ahead = false, ask = null } = {}) {\n  if (false) {"
      }
    ]
  },
  {
    "name": "the frame's kind goes on stdin instead of its text",
    "catches": "writes the frame as one line and answers with what the turn came to",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": ".map((frame) => frame.text).join(\"\\n\");",
        "to": ".map((frame) => frame.kind).join(\"\\n\");"
      }
    ]
  },
  {
    "name": "the next frame is written without waiting for the result",
    "catches": "writes one frame per turn, the next only once the last has answered",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  if (record.turn !== null || record.queue.length === 0 || record.ending !== null) {",
        "to": "  if (record.queue.length === 0 || record.ending !== null) {"
      }
    ]
  },
  {
    "name": "ahead is behind",
    "catches": "puts a frame told ahead in front of what is waiting, behind the turn under way",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "      record.queue.unshift(turn);",
        "to": "      record.queue.push(turn);"
      }
    ]
  },
  {
    "name": "a seat with no process is told anyway, into a queue nobody drains",
    "catches": "says when the seat itself has no process, on the panel too",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    console.log(`no process: ${seat} (${frame.kind})`);\n    return { refused: \"no process\" };",
        "to": "    console.log(`no process: ${seat} (${frame.kind})`);\n    const pending = held.get(seat) ?? [];\n    held.set(seat, pending);\n    return { delivered: true, answered: new Promise((resolve) => pending.push({ frame, resolve })) };"
      },
      {
        "file": "tools/chat/session.mjs",
        "from": "const processes = new Map();",
        "to": "const processes = new Map();\nconst held = new Map();"
      },
      {
        "file": "tools/chat/session.mjs",
        "from": "    queue: [...queue],\n    turn: null,",
        "to": "    queue: [...queue, ...(held.get(seat) ?? [])],\n    turn: null,"
      }
    ]
  },
  {
    "name": "a refusal to tell is not said in the log",
    "catches": "refuses a seat with no process, queues nothing, and says so in the log",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    console.log(`no process: ${seat} (${frame.kind})`);\n",
        "to": ""
      }
    ]
  },
  {
    "name": "any frame at all ends the turn",
    "catches": "reads the answer among whatever else the run says",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "      if (frame.type === \"result\" && record.turn !== null) {",
        "to": "      if (record.turn !== null) {"
      }
    ]
  },
  {
    "name": "an empty answer is not said to be one",
    "catches": "says when a turn answered with nothing",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return { text, failed, silent: !failed && text.trim() === \"\" };",
        "to": "  return { text, failed, silent: false };"
      }
    ]
  },
  {
    "name": "a turn whose process ended is never settled",
    "catches": "settles every waiting turn when the process ends mid-turn, and holds nobody past the exit",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    for (const turn of left) {\n      turn.resolve({ ended: true, text: why });\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "what the process said on stderr is dropped",
    "catches": "says what the process said on stderr when it fell over saying something",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    gone(\n      record.ending === \"restart\"\n        ? `${seat} restarted`\n        : record.err.trim() === \"\"\n          ? `${seat} ended before answering`\n          : record.err.trim(),\n    );",
        "to": "    gone(record.ending === \"restart\" ? `${seat} restarted` : `${seat} ended before answering`);"
      }
    ]
  },
  {
    "name": "ending a seat never closes its conversation",
    "catches": "closes the conversation, which ends a run between turns, and forgets the secret",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  child.stdin.end();\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a run that will not go is left where it is",
    "catches": "takes down a run that will not go once patience has run out",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "    child.kill(\"SIGKILL\");\n",
        "to": ""
      }
    ]
  },
  {
    "name": "ending a seat with no process says it ended one",
    "catches": "answers false for a seat with no process",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  const record = processes.get(seat);\n  if (record === undefined) {\n    return false;\n  }\n  const { child } = record;",
        "to": "  const record = processes.get(seat);\n  if (record === undefined) {\n    return true;\n  }\n  const { child } = record;"
      }
    ]
  },
  {
    "name": "ending every seat ends none",
    "catches": "ends every seat at once",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  return Promise.all(runningSeats().map((seat) => end(seat, patience)));",
        "to": "  return Promise.all([]);"
      }
    ]
  },
  {
    "name": "room is the Leader's alone",
    "catches": "serves the Leader exactly what BUILT_IN names, and a Worker the same but park, hire and permission",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  room: [LEADER, WORKER],",
        "to": "  room: [LEADER],"
      }
    ]
  },
  {
    "name": "message reads who is calling from a second argument",
    "catches": "binds who is calling into every tool, so none of them reads it from an argument",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      async run(args) {\n        if (!offered(\"message\")) {",
        "to": "      async run(args, caller_) {\n        if (!offered(\"message\")) {"
      }
    ]
  },
  {
    "name": "from is whatever the arguments claim",
    "catches": "says who a message is from by the secret it came over, whatever the arguments claim",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        const told = deliver(instance, to, messageFrame(caller.seat, text));",
        "to": "        const told = deliver(instance, to, messageFrame(args?.from ?? caller.seat, text));"
      }
    ]
  },
  {
    "name": "message answers before the reply is in",
    "catches": "waits for the reply and hands it back",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        const reply = await whileWaitingFor(caller.seat, to, () => told.answered);",
        "to": "        const reply = { text: \"\", failed: false, silent: true };"
      }
    ]
  },
  {
    "name": "the reply is handed back as the session wrote it",
    "catches": "neutralises the reply like a body, so nobody poses as the User through a tool result",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        return { text: neutralise(reply.text) };",
        "to": "        return { text: reply.text };"
      }
    ]
  },
  {
    "name": "the sender's side is not written on the panel",
    "catches": "writes both sides onto the addressee's panel",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        append(root, to, { from: caller.seat, text });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a message to nobody is passed on to see what happens",
    "catches": "refuses a message to nobody, and writes nothing anywhere",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        if (!isName(to) || !isSeat(instance, to)) {",
        "to": "        if (false) {"
      }
    ]
  },
  {
    "name": "a message to yourself is delivered",
    "catches": "refuses a message to yourself",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        if (to === caller.seat) {",
        "to": "        if (false) {"
      }
    ]
  },
  {
    "name": "a message of nothing but spaces is delivered",
    "catches": "refuses a message with nothing in it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        if (typeof text !== \"string\" || text.trim() === \"\") {",
        "to": "        if (typeof text !== \"string\") {"
      }
    ]
  },
  {
    "name": "a message starts the seat it is for",
    "catches": "never starts a seat for a message: no process is a refusal",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        const told = deliver(instance, to, messageFrame(caller.seat, text));",
        "to": "        if (!running(to)) {\n          startSeat(instance, to);\n        }\n        const told = deliver(instance, to, messageFrame(caller.seat, text));"
      }
    ]
  },
  {
    "name": "a reply that never came is handed back as one",
    "catches": "refuses when the reply never comes because the process ended, and holds nobody past the exit",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        if (reply.ended === true) {\n          append(root, to, { from: THE_CHAT, text: `${to} stopped before answering: ${reply.text}`, failed: true });\n          return { refused: `${to} stopped before answering` };\n        }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a message that would wait for the sender's own turn is taken",
    "catches": "refuses a message that would wait for the sender's own turn",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        if (wouldWaitForItself(caller.seat, to)) {",
        "to": "        if (false) {"
      }
    ]
  },
  {
    "name": "the walk stops after one step",
    "catches": "refuses a message that would wait for the sender further up the chain",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  for (let step = 0; step <= waitingOn.size; step += 1) {",
        "to": "  for (let step = 0; step < 1; step += 1) {"
      }
    ]
  },
  {
    "name": "nobody in the room is you",
    "catches": "tells a session who works here, who runs, and which one it is",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "you: seat.name === caller.seat })",
        "to": "you: false })"
      }
    ]
  },
  {
    "name": "a tool of the instance's own is not told who is calling",
    "catches": "serves a tool of the instance's own after its own, handing it who is calling",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        return answerFrom(plugin.name, await plugin.run(args, { seat: caller.seat, role: caller.role, root, config: instance.config }));",
        "to": "        return answerFrom(plugin.name, await plugin.run(args));"
      }
    ]
  },
  {
    "name": "a tool of the instance's own answers in whatever shape it likes",
    "catches": "refuses for a tool of the instance's own that answers outside the shape",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "        return answerFrom(plugin.name, await plugin.run(args, { seat: caller.seat, role: caller.role, root, config: instance.config }));",
        "to": "        return await plugin.run(args, { seat: caller.seat, role: caller.role, root, config: instance.config });"
      }
    ]
  },
  {
    "name": "the Leader is not told what the User typed to a Worker",
    "catches": "reaches the seat as the User's own frame, and the Leader as a server event",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (seat !== instance.config.leader) {\n    const leader = instance.config.leader;",
        "to": "  if (false) {\n    const leader = instance.config.leader;"
      }
    ]
  },
  {
    "name": "what the User typed is framed as a message from somebody called User",
    "catches": "reaches the seat as the User's own frame, and the Leader as a server event",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const told = deliver(instance, seat, userFrame(text));",
        "to": "  const told = deliver(instance, seat, messageFrame(\"User\", text));"
      }
    ]
  },
  {
    "name": "what the User typed is not written on the panel",
    "catches": "lands on the seat's panel as the User's row, with the reply under it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  append(root, seat, { from: \"user\", text });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the reply never lands on the panel",
    "catches": "lands on the seat's panel as the User's row, with the reply under it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    append(root, seat, {\n      from: seat,\n      text: reply.text,",
        "to": "    return;\n    append(root, seat, {\n      from: seat,\n      text: reply.text,"
      }
    ]
  },
  {
    "name": "the Leader's panel does not say who was typed to",
    "catches": "shows the Leader what was typed to whom, and what the Leader made of it",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    append(root, leader, { from: \"user\", typedTo: seat, text });",
        "to": "    append(root, leader, { from: \"user\", text });"
      }
    ]
  },
  {
    "name": "the Leader is told about its own turn as an event too",
    "catches": "frames a word to the Leader as the User's own, and tells nobody else",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (seat !== instance.config.leader) {\n    const leader = instance.config.leader;",
        "to": "  if (true) {\n    const leader = instance.config.leader;"
      }
    ]
  },
  {
    "name": "a seat with no process is not said so on the panel",
    "catches": "says when the seat itself has no process, on the panel too",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    append(root, seat, { from: THE_CHAT, text: `${seat} has no process`, failed: true });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the page may type nothing but spaces",
    "catches": "refuses a message with nothing in it, or nothing it can read",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  if (typeof text !== \"string\" || text.trim() === \"\") {\n    sendJson(response, 400, { error: \"a message needs some text\" });",
        "to": "  if (typeof text !== \"string\") {\n    sendJson(response, 400, { error: \"a message needs some text\" });"
      }
    ]
  },
  {
    "name": "the server writes to a session's stdin on its own",
    "catches": "is session.mjs, through the frame writer, the two permission answers and the interrupt, and nobody else",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  sendJson(response, 200, typed(instance, seat, text));",
        "to": "  // running(seat) && processes.get(seat).child.stdin.write(text);\n  sendJson(response, 200, typed(instance, seat, text));"
      }
    ]
  },
  {
    "name": "the lifecycle writes to a session's stdin on its own",
    "catches": "is session.mjs, through the frame writer, the two permission answers and the interrupt, and nobody else",
    "edits": [
      {
        "file": "tools/chat/lifecycle.mjs",
        "from": "  const told = tell(seat, frame, { ahead, ask });",
        "to": "  // recordOf(seat)?.child.stdin.write(frame.text);\n  const told = tell(seat, frame, { ahead, ask });"
      }
    ]
  },
  {
    "name": "the chat starts without saying what it does not read",
    "catches": "says at the start what instructions above it are not read, and where it listens",
    "edits": [
      {
        "file": "tools/ovai.mjs",
        "from": "  console.log(describeInstructions(instructionsAbove(root).existing));\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the request log carries the path as it came",
    "catches": "logs the route and never a secret",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return TOOL_ROUTE.test(pathname) ? \"/mcp/<secret>\" : pathname;",
        "to": "  return pathname;"
      }
    ]
  },
  {
    "name": "the page secret is the same every start",
    "catches": "starts with nothing when started again: the old page secret opens nothing",
    "edits": [
      {
        "file": "tools/chat/secrets.mjs",
        "from": "    page = mint();",
        "to": "    page = \"p\".repeat(43);"
      }
    ]
  },
  {
    "name": "the page sends its calls without the secret",
    "catches": "reads its secret off the tag, sends it on every call, and reloads when it is no longer known",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "      headers: { ...(options.headers ?? {}), authorization: `Bearer ${secret}` },",
        "to": "      headers: { ...(options.headers ?? {}) },"
      }
    ]
  },
  {
    "name": "the page sits on a secret the server no longer knows",
    "catches": "reads its secret off the tag, sends it on every call, and reloads when it is no longer known",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "    if (answered.status === 401) {\n      location.reload();\n      throw new Error(\"the server no longer knows this page\");\n    }\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the page fetches the room around its own gate",
    "catches": "reads its secret off the tag, sends it on every call, and reloads when it is no longer known",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "call(\"/sessions\").then(",
        "to": "fetch(\"/sessions\").then("
      }
    ]
  },
  {
    "name": "a new row is written and nobody is told",
    "catches": "a new row reaches the page without a poll",
    "edits": [
      {
        "file": "tools/chat/conversation.mjs",
        "from": "  publish(\"row\", { seat: session, index: messages.length - 1, row: entry });\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the counts a page connects with are ignored",
    "catches": "a client connecting with counts gets only the rows after them",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    const since = counts.get(name) ?? 0;",
        "to": "    const since = 0;"
      }
    ]
  },
  {
    "name": "a turn taken is not told",
    "catches": "tells of a seat's process starting, a turn under way and over, and the process gone",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "  record.child.stdin.write(`${JSON.stringify({ type: \"user\", message: { role: \"user\", content } })}\\n`);\n  if (prefix.length > 0) {\n    record.rules = prefix.at(-1).version;\n    writeDeskHeader(record.root, record.seat, { rules: record.rules });\n  }\n  record.changed(record);\n}",
        "to": "  record.child.stdin.write(`${JSON.stringify({ type: \"user\", message: { role: \"user\", content } })}\\n`);\n  if (prefix.length > 0) {\n    record.rules = prefix.at(-1).version;\n    writeDeskHeader(record.root, record.seat, { rules: record.rules });\n  }\n}"
      }
    ]
  },
  {
    "name": "a process gone is not told",
    "catches": "tells of a seat's process starting, a turn under way and over, and the process gone",
    "edits": [
      {
        "file": "tools/chat/lifecycle.mjs",
        "from": "function afterClose(instance, { seat, role, ending, carried }) {\n  seatChanged(instance, seat);",
        "to": "function afterClose(instance, { seat, role, ending, carried }) {"
      }
    ]
  },
  {
    "name": "the Leader's close is kept from the page",
    "catches": "the Leader's close is told like anybody's, and nothing follows it for the Leader",
    "edits": [
      {
        "file": "tools/chat/lifecycle.mjs",
        "from": "function afterClose(instance, { seat, role, ending, carried }) {\n  seatChanged(instance, seat);",
        "to": "function afterClose(instance, { seat, role, ending, carried }) {\n  if (role === WORKER) {\n    seatChanged(instance, seat);\n  }"
      }
    ]
  },
  {
    "name": "an interrupted turn is shown as a reply",
    "catches": "the page's STOP interrupts the turn, and the row says so rather than the seat",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      ...(reply.interrupted ? { interrupted: true } : {}),\n",
        "to": ""
      }
    ]
  },
  {
    "name": "a call stop is parked and the page is not told",
    "catches": "a call stop is pushed as it is parked, and taken down as it is answered",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "    forSession(name).set(request.id, { request, answer, parkedAt: now, told: false });\n    asking(name);",
        "to": "    forSession(name).set(request.id, { request, answer, parkedAt: now, told: false });"
      }
    ]
  },
  {
    "name": "a call stop answered stays on the page",
    "catches": "a call stop is pushed as it is parked, and taken down as it is answered",
    "edits": [
      {
        "file": "tools/chat/permissions.mjs",
        "from": "  held.answer(decision);\n  asking(name);\n  return true;",
        "to": "  held.answer(decision);\n  return true;"
      }
    ]
  },
  {
    "name": "the stream takes the bearer too",
    "catches": "opens to the page secret on its query, and to nothing else — not even the bearer",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  const carried = url.pathname === EVENTS_ROUTE ? url.searchParams.get(\"page\") : bearer(request.headers.authorization);",
        "to": "  const carried = (url.pathname === EVENTS_ROUTE ? url.searchParams.get(\"page\") : null) ?? bearer(request.headers.authorization);"
      }
    ]
  },
  {
    "name": "the stream's address is logged with its query",
    "catches": "logs the route and never a secret",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "      console.log(`${request.method} ${shownAs(new URL(request.url, `http://${HOST}`).pathname)} ${response.statusCode}`);",
        "to": "      console.log(`${request.method} ${request.url} ${response.statusCode}`);"
      }
    ]
  },
  {
    "name": "the page installs in a browser tab",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "tools/chat/manifest.webmanifest",
        "from": "  \"display\": \"standalone\",",
        "to": "  \"display\": \"browser\","
      }
    ]
  },
  {
    "name": "the maskable icon loses its purpose",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "tools/chat/manifest.webmanifest",
        "from": "{ \"src\": \"/icons/512-maskable.png\", \"sizes\": \"512x512\", \"type\": \"image/png\", \"purpose\": \"maskable\" }",
        "to": "{ \"src\": \"/icons/512-maskable.png\", \"sizes\": \"512x512\", \"type\": \"image/png\" }"
      }
    ]
  },
  {
    "name": "the maskable icon is named but not served",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  \"/icons/512-maskable.png\": { file: path.join(HERE, \"icons\", \"512-maskable.png\"), type: \"image/png\" },\n",
        "to": ""
      }
    ]
  },
  {
    "name": "the manifest says a size the icon is not",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "tools/chat/manifest.webmanifest",
        "from": "{ \"src\": \"/icons/512-maskable.png\", \"sizes\": \"512x512\"",
        "to": "{ \"src\": \"/icons/512-maskable.png\", \"sizes\": \"256x256\""
      }
    ]
  },
  {
    "name": "the maskable icon is served as the wrong type",
    "catches": "installs as a standalone app: a manifest with three icons that are PNGs of the size they say, one of them maskable, and no service worker",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  \"/icons/512-maskable.png\": { file: path.join(HERE, \"icons\", \"512-maskable.png\"), type: \"image/png\" },",
        "to": "  \"/icons/512-maskable.png\": { file: path.join(HERE, \"icons\", \"512-maskable.png\"), type: \"application/octet-stream\" },"
      }
    ]
  },
  {
    "name": "a lifecycle button on the page",
    "catches": "carries no lifecycle button and no lifecycle word, and STOP is its one lifecycle button",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "<div id=\"stage\">",
        "to": "<button>Go online</button>\n<div id=\"stage\">"
      }
    ]
  },
  {
    "name": "a second button label in the script",
    "catches": "carries no lifecycle button and no lifecycle word, and STOP is its one lifecycle button",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "    composer.append(box);",
        "to": "    const send = document.createElement(\"button\");\n    send.type = \"submit\";\n    send.textContent = \"Send\";\n    composer.append(box, send);"
      }
    ]
  },
  {
    "name": "the page says a session is on its way out",
    "catches": "never reads how a process is going: no ending on the page or in panels.mjs, no running read on the page",
    "edits": [
      {
        "file": "tools/chat/panels.mjs",
        "from": "  panel.busy = about.busy === true;\n  panel.running",
        "to": "  panel.busy = about.busy === true;\n  panel.title = about.ending === null ? panel.title : `${about.ending}`;\n  panel.running"
      }
    ]
  },
  {
    "name": "the page loads the parser from a CDN",
    "catches": "loads nothing from outside the server and prompts for nothing of its own",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "<title>OpenOv AI</title>",
        "to": "<title>OpenOv AI</title>\n<script src=\"https://cdn.jsdelivr.net/npm/marked\"></script>"
      }
    ]
  },
  {
    "name": "the page asks to notify",
    "catches": "loads nothing from outside the server and prompts for nothing of its own",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "  connect();\n  setInterval(",
        "to": "  connect();\n  Notification.requestPermission();\n  setInterval("
      }
    ]
  },
  {
    "name": "the placement is inlined on the page",
    "catches": "draws from the two tested modules and the dialog module",
    "edits": [
      {
        "file": "tools/chat/page.html",
        "from": "  import { applyEvent, composersEnabled, fresh, head, keyAction, place, prune, statusLine, stopEnabled, title } from \"./panels.mjs\";",
        "to": "  import { applyEvent, composersEnabled, fresh, head, keyAction, prune, statusLine, stopEnabled, title } from \"./panels.mjs\";\n  const place = (seats, leader) => ({ left: seats.filter((s) => s !== leader), mid: leader, right: [] });"
      }
    ]
  },
  {
    "name": "a since on the rows is ignored",
    "catches": "answers a panel's rows from an index on",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return Number.isInteger(from) && from > 0 ? rows.slice(from) : rows;",
        "to": "  return rows;"
      }
    ]
  },
  {
    "name": "the instance stops and the page is not told",
    "catches": "the page is told the instance is stopping, and every page route answers 503 from then on",
    "edits": [
      {
        "file": "tools/chat/lifecycle.mjs",
        "from": "  instance.stopping = true;\n  publish(\"stopping\", {});",
        "to": "  instance.stopping = true;"
      }
    ]
  },
  {
    "name": "the parser is served from somewhere else",
    "catches": "serves each module and the parser open, as the file, and nothing else on those routes",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  \"/marked.mjs\": { file: path.join(HERE, \"marked.mjs\"), type: \"text/javascript; charset=utf-8\" },",
        "to": "  \"/marked.mjs\": { file: path.join(HERE, \"render.mjs\"), type: \"text/javascript; charset=utf-8\" },"
      }
    ]
  },
  {
    "name": "a GET on an issued door is refused like a stranger",
    "catches": "answers GET on the door it issued with 405, and GET on an unknown one with the same 401",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "    sendJson(response, 405, { error: \"method not allowed\" }, { allow: \"POST\" });",
        "to": "    sendJson(response, 401, UNKNOWN);"
      }
    ]
  },
  {
    "name": "the log masks the secret on the tool route only",
    "catches": "prints no secret in the request log, wherever a path carries one",
    "edits": [
      {
        "file": "tools/chat/server.mjs",
        "from": "  return pathname.replace(SECRET_IN_PATH, \"/mcp/<secret>\");",
        "to": "  return TOOL_ROUTE.test(pathname) ? \"/mcp/<secret>\" : pathname;"
      }
    ]
  },
  {
    "name": "a tool line is written on the Leader's panel too",
    "catches": "writes no line for the Leader's own calls",
    "edits": [
      {
        "file": "tools/chat/lifecycle.mjs",
        "from": "    ...(roleOf(instance, seat) === WORKER ? { called:",
        "to": "    ...(true ? { called:"
      }
    ]
  },
  {
    "name": "a failed call leaves its line as it was: the amend is gone",
    "catches": "marks the line of a call that failed, in the file and on the stream",
    "edits": [
      {
        "file": "tools/chat/lifecycle.mjs",
        "from": "failed: ({ id }) => amend(instance.root, seat, id) }",
        "to": "failed: () => {} }"
      }
    ]
  },
  {
    "name": "the amend tells the page but keeps the old entry on disk",
    "catches": "marks the line of a call that failed, in the file and on the stream",
    "edits": [
      {
        "file": "tools/chat/conversation.mjs",
        "from": "  messages[index].err = true;\n  fs.writeFileSync(file(root, session), `${JSON.stringify(messages, null, 2)}\\n`);\n",
        "to": "  messages[index].err = true;\n"
      }
    ]
  },
  {
    "name": "a subagent's calls draw as the seat's own: the parent check is gone",
    "catches": "draws nothing for a call made under another call",
    "edits": [
      {
        "file": "tools/chat/session.mjs",
        "from": "        if ((frame.parent_tool_use_id ?? null) === null) {",
        "to": "        {"
      }
    ]
  },
  {
    "name": "a line is written for every call, the silent ones included",
    "catches": "draws nothing for a search of the tool list or a session's own stop",
    "edits": [
      {
        "file": "tools/chat/lifecycle.mjs",
        "from": "    if (text !== null) {\n      append(instance.root, seat, { from: seat, line: text, call: id });\n    }\n",
        "to": "    append(instance.root, seat, { from: seat, line: text, call: id });\n"
      }
    ]
  }
]
